import "./chunk-PZ5AY32C.js";

// node_modules/valibot/dist/index.mjs
var store$4;
function getGlobalConfig(config$1) {
  return {
    lang: config$1?.lang ?? store$4?.lang,
    message: config$1?.message,
    abortEarly: config$1?.abortEarly ?? store$4?.abortEarly,
    abortPipeEarly: config$1?.abortPipeEarly ?? store$4?.abortPipeEarly
  };
}
var store$3;
function getGlobalMessage(lang) {
  return store$3?.get(lang);
}
var store$2;
function getSchemaMessage(lang) {
  return store$2?.get(lang);
}
var store$1;
function getSpecificMessage(reference, lang) {
  return store$1?.get(reference)?.get(lang);
}
function _stringify(input) {
  const type = typeof input;
  if (type === "string") return `"${input}"`;
  if (type === "number" || type === "bigint" || type === "boolean") return `${input}`;
  if (type === "object" || type === "function") return (input && Object.getPrototypeOf(input)?.constructor?.name) ?? "null";
  return type;
}
function _addIssue(context, label, dataset, config$1, other) {
  const input = other && "input" in other ? other.input : dataset.value;
  const expected = other?.expected ?? context.expects ?? null;
  const received = other?.received ?? _stringify(input);
  const issue = {
    kind: context.kind,
    type: context.type,
    input,
    expected,
    received,
    message: `Invalid ${label}: ${expected ? `Expected ${expected} but r` : "R"}eceived ${received}`,
    requirement: context.requirement,
    path: other?.path,
    issues: other?.issues,
    lang: config$1.lang,
    abortEarly: config$1.abortEarly,
    abortPipeEarly: config$1.abortPipeEarly
  };
  const isSchema = context.kind === "schema";
  const message$1 = other?.message ?? context.message ?? getSpecificMessage(context.reference, issue.lang) ?? (isSchema ? getSchemaMessage(issue.lang) : null) ?? config$1.message ?? getGlobalMessage(issue.lang);
  if (message$1 !== void 0) issue.message = typeof message$1 === "function" ? message$1(issue) : message$1;
  if (isSchema) dataset.typed = false;
  if (dataset.issues) dataset.issues.push(issue);
  else dataset.issues = [issue];
}
function _getStandardProps(context) {
  return {
    version: 1,
    vendor: "valibot",
    validate(value$1) {
      return context["~run"]({ value: value$1 }, getGlobalConfig());
    }
  };
}
function _joinExpects(values$1, separator) {
  const list = [...new Set(values$1)];
  if (list.length > 1) return `(${list.join(` ${separator} `)})`;
  return list[0] ?? "never";
}
function getDotPath(issue) {
  if (issue.path) {
    let key = "";
    for (const item of issue.path) if (typeof item.key === "string" || typeof item.key === "number") if (key) key += `.${item.key}`;
    else key += item.key;
    else return null;
    return key;
  }
  return null;
}
var ValiError = class extends Error {
  /**
  * Creates a Valibot error with useful information.
  *
  * @param issues The error issues.
  */
  constructor(issues) {
    super(issues[0].message);
    this.name = "ValiError";
    this.issues = issues;
  }
};
var IP_REGEX = /^(?:(?:[1-9]|1\d|2[0-4])?\d|25[0-5])(?:\.(?:(?:[1-9]|1\d|2[0-4])?\d|25[0-5])){3}$|^(?:(?:[\da-f]{1,4}:){7}[\da-f]{1,4}|(?:[\da-f]{1,4}:){1,7}:|(?:[\da-f]{1,4}:){1,6}:[\da-f]{1,4}|(?:[\da-f]{1,4}:){1,5}(?::[\da-f]{1,4}){1,2}|(?:[\da-f]{1,4}:){1,4}(?::[\da-f]{1,4}){1,3}|(?:[\da-f]{1,4}:){1,3}(?::[\da-f]{1,4}){1,4}|(?:[\da-f]{1,4}:){1,2}(?::[\da-f]{1,4}){1,5}|[\da-f]{1,4}:(?::[\da-f]{1,4}){1,6}|:(?:(?::[\da-f]{1,4}){1,7}|:)|fe80:(?::[\da-f]{0,4}){0,4}%[\da-z]+|::(?:f{4}(?::0{1,4})?:)?(?:(?:25[0-5]|(?:2[0-4]|1?\d)?\d)\.){3}(?:25[0-5]|(?:2[0-4]|1?\d)?\d)|(?:[\da-f]{1,4}:){1,4}:(?:(?:25[0-5]|(?:2[0-4]|1?\d)?\d)\.){3}(?:25[0-5]|(?:2[0-4]|1?\d)?\d))$/iu;
function check(requirement, message$1) {
  return {
    kind: "validation",
    type: "check",
    reference: check,
    async: false,
    expects: null,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !this.requirement(dataset.value)) _addIssue(this, "input", dataset, config$1);
      return dataset;
    }
  };
}
function ip(message$1) {
  return {
    kind: "validation",
    type: "ip",
    reference: ip,
    async: false,
    expects: null,
    requirement: IP_REGEX,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !this.requirement.test(dataset.value)) _addIssue(this, "IP", dataset, config$1);
      return dataset;
    }
  };
}
function length(requirement, message$1) {
  return {
    kind: "validation",
    type: "length",
    reference: length,
    async: false,
    expects: `${requirement}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && dataset.value.length !== this.requirement) _addIssue(this, "length", dataset, config$1, { received: `${dataset.value.length}` });
      return dataset;
    }
  };
}
function maxLength(requirement, message$1) {
  return {
    kind: "validation",
    type: "max_length",
    reference: maxLength,
    async: false,
    expects: `<=${requirement}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && dataset.value.length > this.requirement) _addIssue(this, "length", dataset, config$1, { received: `${dataset.value.length}` });
      return dataset;
    }
  };
}
function maxValue(requirement, message$1) {
  return {
    kind: "validation",
    type: "max_value",
    reference: maxValue,
    async: false,
    expects: `<=${requirement instanceof Date ? requirement.toJSON() : _stringify(requirement)}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !(dataset.value <= this.requirement)) _addIssue(this, "value", dataset, config$1, { received: dataset.value instanceof Date ? dataset.value.toJSON() : _stringify(dataset.value) });
      return dataset;
    }
  };
}
function minLength(requirement, message$1) {
  return {
    kind: "validation",
    type: "min_length",
    reference: minLength,
    async: false,
    expects: `>=${requirement}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && dataset.value.length < this.requirement) _addIssue(this, "length", dataset, config$1, { received: `${dataset.value.length}` });
      return dataset;
    }
  };
}
function minValue(requirement, message$1) {
  return {
    kind: "validation",
    type: "min_value",
    reference: minValue,
    async: false,
    expects: `>=${requirement instanceof Date ? requirement.toJSON() : _stringify(requirement)}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !(dataset.value >= this.requirement)) _addIssue(this, "value", dataset, config$1, { received: dataset.value instanceof Date ? dataset.value.toJSON() : _stringify(dataset.value) });
      return dataset;
    }
  };
}
function parseJson(config$1, message$1) {
  return {
    kind: "transformation",
    type: "parse_json",
    reference: parseJson,
    config: config$1,
    message: message$1,
    async: false,
    "~run"(dataset, config$2) {
      try {
        dataset.value = JSON.parse(dataset.value, this.config?.reviver);
      } catch (error) {
        if (error instanceof Error) {
          _addIssue(this, "JSON", dataset, config$2, { received: `"${error.message}"` });
          dataset.typed = false;
        } else throw error;
      }
      return dataset;
    }
  };
}
function regex(requirement, message$1) {
  return {
    kind: "validation",
    type: "regex",
    reference: regex,
    async: false,
    expects: `${requirement}`,
    requirement,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !this.requirement.test(dataset.value)) _addIssue(this, "format", dataset, config$1);
      return dataset;
    }
  };
}
function safeInteger(message$1) {
  return {
    kind: "validation",
    type: "safe_integer",
    reference: safeInteger,
    async: false,
    expects: null,
    requirement: Number.isSafeInteger,
    message: message$1,
    "~run"(dataset, config$1) {
      if (dataset.typed && !this.requirement(dataset.value)) _addIssue(this, "safe integer", dataset, config$1);
      return dataset;
    }
  };
}
function stringifyJson(config$1, message$1) {
  return {
    kind: "transformation",
    type: "stringify_json",
    reference: stringifyJson,
    message: message$1,
    config: config$1,
    async: false,
    "~run"(dataset, config$2) {
      try {
        const output = JSON.stringify(dataset.value, this.config?.replacer, this.config?.space);
        if (output === void 0) {
          _addIssue(this, "JSON", dataset, config$2);
          dataset.typed = false;
        }
        dataset.value = output;
      } catch (error) {
        if (error instanceof Error) {
          _addIssue(this, "JSON", dataset, config$2, { received: `"${error.message}"` });
          dataset.typed = false;
        } else throw error;
      }
      return dataset;
    }
  };
}
function toNumber(message$1) {
  return {
    kind: "transformation",
    type: "to_number",
    reference: toNumber,
    async: false,
    message: message$1,
    "~run"(dataset, config$1) {
      try {
        dataset.value = Number(dataset.value);
        if (isNaN(dataset.value)) {
          _addIssue(this, "number", dataset, config$1);
          dataset.typed = false;
        }
      } catch {
        _addIssue(this, "number", dataset, config$1);
        dataset.typed = false;
      }
      return dataset;
    }
  };
}
function toString(message$1) {
  return {
    kind: "transformation",
    type: "to_string",
    reference: toString,
    async: false,
    message: message$1,
    "~run"(dataset, config$1) {
      try {
        dataset.value = String(dataset.value);
      } catch {
        _addIssue(this, "string", dataset, config$1);
        dataset.typed = false;
      }
      return dataset;
    }
  };
}
function transform(operation) {
  return {
    kind: "transformation",
    type: "transform",
    reference: transform,
    async: false,
    operation,
    "~run"(dataset) {
      dataset.value = this.operation(dataset.value);
      return dataset;
    }
  };
}
function getFallback(schema, dataset, config$1) {
  return typeof schema.fallback === "function" ? schema.fallback(dataset, config$1) : schema.fallback;
}
function getDefault(schema, dataset, config$1) {
  return typeof schema.default === "function" ? schema.default(dataset, config$1) : schema.default;
}
function array(item, message$1) {
  return {
    kind: "schema",
    type: "array",
    reference: array,
    expects: "Array",
    async: false,
    item,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (Array.isArray(input)) {
        dataset.typed = true;
        dataset.value = [];
        for (let key = 0; key < input.length; key++) {
          const value$1 = input[key];
          const itemDataset = this.item["~run"]({ value: value$1 }, config$1);
          if (itemDataset.issues) {
            const pathItem = {
              type: "array",
              origin: "value",
              input,
              key,
              value: value$1
            };
            for (const issue of itemDataset.issues) {
              if (issue.path) issue.path.unshift(pathItem);
              else issue.path = [pathItem];
              dataset.issues?.push(issue);
            }
            if (!dataset.issues) dataset.issues = itemDataset.issues;
            if (config$1.abortEarly) {
              dataset.typed = false;
              break;
            }
          }
          if (!itemDataset.typed) dataset.typed = false;
          dataset.value.push(itemDataset.value);
        }
      } else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function boolean(message$1) {
  return {
    kind: "schema",
    type: "boolean",
    reference: boolean,
    expects: "boolean",
    async: false,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "boolean") dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function literal(literal_, message$1) {
  return {
    kind: "schema",
    type: "literal",
    reference: literal,
    expects: _stringify(literal_),
    async: false,
    literal: literal_,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === this.literal) dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function null_(message$1) {
  return {
    kind: "schema",
    type: "null",
    reference: null_,
    expects: "null",
    async: false,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === null) dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function nullable(wrapped, default_) {
  return {
    kind: "schema",
    type: "nullable",
    reference: nullable,
    expects: `(${wrapped.expects} | null)`,
    async: false,
    wrapped,
    default: default_,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === null) {
        if (this.default !== void 0) dataset.value = getDefault(this, dataset, config$1);
        if (dataset.value === null) {
          dataset.typed = true;
          return dataset;
        }
      }
      return this.wrapped["~run"](dataset, config$1);
    }
  };
}
function nullish(wrapped, default_) {
  return {
    kind: "schema",
    type: "nullish",
    reference: nullish,
    expects: `(${wrapped.expects} | null | undefined)`,
    async: false,
    wrapped,
    default: default_,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === null || dataset.value === void 0) {
        if (this.default !== void 0) dataset.value = getDefault(this, dataset, config$1);
        if (dataset.value === null || dataset.value === void 0) {
          dataset.typed = true;
          return dataset;
        }
      }
      return this.wrapped["~run"](dataset, config$1);
    }
  };
}
function number(message$1) {
  return {
    kind: "schema",
    type: "number",
    reference: number,
    expects: "number",
    async: false,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "number" && !isNaN(dataset.value)) dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function object(entries$1, message$1) {
  return {
    kind: "schema",
    type: "object",
    reference: object,
    expects: "Object",
    async: false,
    entries: entries$1,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (input && typeof input === "object") {
        dataset.typed = true;
        dataset.value = {};
        for (const key in this.entries) {
          const valueSchema = this.entries[key];
          if (key in input || (valueSchema.type === "exact_optional" || valueSchema.type === "optional" || valueSchema.type === "nullish") && valueSchema.default !== void 0) {
            const value$1 = key in input ? input[key] : getDefault(valueSchema);
            const valueDataset = valueSchema["~run"]({ value: value$1 }, config$1);
            if (valueDataset.issues) {
              const pathItem = {
                type: "object",
                origin: "value",
                input,
                key,
                value: value$1
              };
              for (const issue of valueDataset.issues) {
                if (issue.path) issue.path.unshift(pathItem);
                else issue.path = [pathItem];
                dataset.issues?.push(issue);
              }
              if (!dataset.issues) dataset.issues = valueDataset.issues;
              if (config$1.abortEarly) {
                dataset.typed = false;
                break;
              }
            }
            if (!valueDataset.typed) dataset.typed = false;
            dataset.value[key] = valueDataset.value;
          } else if (valueSchema.fallback !== void 0) dataset.value[key] = getFallback(valueSchema);
          else if (valueSchema.type !== "exact_optional" && valueSchema.type !== "optional" && valueSchema.type !== "nullish") {
            _addIssue(this, "key", dataset, config$1, {
              input: void 0,
              expected: `"${key}"`,
              path: [{
                type: "object",
                origin: "key",
                input,
                key,
                value: input[key]
              }]
            });
            if (config$1.abortEarly) break;
          }
        }
      } else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function optional(wrapped, default_) {
  return {
    kind: "schema",
    type: "optional",
    reference: optional,
    expects: `(${wrapped.expects} | undefined)`,
    async: false,
    wrapped,
    default: default_,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === void 0) {
        if (this.default !== void 0) dataset.value = getDefault(this, dataset, config$1);
        if (dataset.value === void 0) {
          dataset.typed = true;
          return dataset;
        }
      }
      return this.wrapped["~run"](dataset, config$1);
    }
  };
}
function picklist(options, message$1) {
  return {
    kind: "schema",
    type: "picklist",
    reference: picklist,
    expects: _joinExpects(options.map(_stringify), "|"),
    async: false,
    options,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (this.options.includes(dataset.value)) dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function string(message$1) {
  return {
    kind: "schema",
    type: "string",
    reference: string,
    expects: "string",
    async: false,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "string") dataset.typed = true;
      else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function tuple(items, message$1) {
  return {
    kind: "schema",
    type: "tuple",
    reference: tuple,
    expects: "Array",
    async: false,
    items,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (Array.isArray(input)) {
        dataset.typed = true;
        dataset.value = [];
        for (let key = 0; key < this.items.length; key++) {
          const value$1 = input[key];
          const itemDataset = this.items[key]["~run"]({ value: value$1 }, config$1);
          if (itemDataset.issues) {
            const pathItem = {
              type: "array",
              origin: "value",
              input,
              key,
              value: value$1
            };
            for (const issue of itemDataset.issues) {
              if (issue.path) issue.path.unshift(pathItem);
              else issue.path = [pathItem];
              dataset.issues?.push(issue);
            }
            if (!dataset.issues) dataset.issues = itemDataset.issues;
            if (config$1.abortEarly) {
              dataset.typed = false;
              break;
            }
          }
          if (!itemDataset.typed) dataset.typed = false;
          dataset.value.push(itemDataset.value);
        }
      } else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function _subIssues(datasets) {
  let issues;
  if (datasets) for (const dataset of datasets) if (issues) issues.push(...dataset.issues);
  else issues = dataset.issues;
  return issues;
}
function union(options, message$1) {
  return {
    kind: "schema",
    type: "union",
    reference: union,
    expects: _joinExpects(options.map((option) => option.expects), "|"),
    async: false,
    options,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      let validDataset;
      let typedDatasets;
      let untypedDatasets;
      for (const schema of this.options) {
        const optionDataset = schema["~run"]({ value: dataset.value }, config$1);
        if (optionDataset.typed) if (optionDataset.issues) if (typedDatasets) typedDatasets.push(optionDataset);
        else typedDatasets = [optionDataset];
        else {
          validDataset = optionDataset;
          break;
        }
        else if (untypedDatasets) untypedDatasets.push(optionDataset);
        else untypedDatasets = [optionDataset];
      }
      if (validDataset) return validDataset;
      if (typedDatasets) {
        if (typedDatasets.length === 1) return typedDatasets[0];
        _addIssue(this, "type", dataset, config$1, { issues: _subIssues(typedDatasets) });
        dataset.typed = true;
      } else if (untypedDatasets?.length === 1) return untypedDatasets[0];
      else _addIssue(this, "type", dataset, config$1, { issues: _subIssues(untypedDatasets) });
      return dataset;
    }
  };
}
function variant(key, options, message$1) {
  return {
    kind: "schema",
    type: "variant",
    reference: variant,
    expects: "Object",
    async: false,
    key,
    options,
    message: message$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (input && typeof input === "object") {
        let outputDataset;
        let maxDiscriminatorPriority = 0;
        let invalidDiscriminatorKey = this.key;
        let expectedDiscriminators = [];
        const parseOptions = (variant$1, allKeys) => {
          for (const schema of variant$1.options) {
            if (schema.type === "variant") parseOptions(schema, new Set(allKeys).add(schema.key));
            else {
              let keysAreValid = true;
              let currentPriority = 0;
              for (const currentKey of allKeys) {
                const discriminatorSchema = schema.entries[currentKey];
                if (currentKey in input ? discriminatorSchema["~run"]({
                  typed: false,
                  value: input[currentKey]
                }, { abortEarly: true }).issues : discriminatorSchema.type !== "exact_optional" && discriminatorSchema.type !== "optional" && discriminatorSchema.type !== "nullish") {
                  keysAreValid = false;
                  if (invalidDiscriminatorKey !== currentKey && (maxDiscriminatorPriority < currentPriority || maxDiscriminatorPriority === currentPriority && currentKey in input && !(invalidDiscriminatorKey in input))) {
                    maxDiscriminatorPriority = currentPriority;
                    invalidDiscriminatorKey = currentKey;
                    expectedDiscriminators = [];
                  }
                  if (invalidDiscriminatorKey === currentKey) expectedDiscriminators.push(schema.entries[currentKey].expects);
                  break;
                }
                currentPriority++;
              }
              if (keysAreValid) {
                const optionDataset = schema["~run"]({ value: input }, config$1);
                if (!outputDataset || !outputDataset.typed && optionDataset.typed) outputDataset = optionDataset;
              }
            }
            if (outputDataset && !outputDataset.issues) break;
          }
        };
        parseOptions(this, /* @__PURE__ */ new Set([this.key]));
        if (outputDataset) return outputDataset;
        _addIssue(this, "type", dataset, config$1, {
          input: input[invalidDiscriminatorKey],
          expected: _joinExpects(expectedDiscriminators, "|"),
          path: [{
            type: "object",
            origin: "value",
            input,
            key: invalidDiscriminatorKey,
            value: input[invalidDiscriminatorKey]
          }]
        });
      } else _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
function omit(schema, keys) {
  const entries$1 = { ...schema.entries };
  for (const key of keys) delete entries$1[key];
  return {
    ...schema,
    entries: entries$1,
    get "~standard"() {
      return _getStandardProps(this);
    }
  };
}
function parse(schema, input, config$1) {
  const dataset = schema["~run"]({ value: input }, getGlobalConfig(config$1));
  if (dataset.issues) throw new ValiError(dataset.issues);
  return dataset.value;
}
function pipe(...pipe$1) {
  return {
    ...pipe$1[0],
    pipe: pipe$1,
    get "~standard"() {
      return _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      for (const item of pipe$1) if (item.kind !== "metadata") {
        if (dataset.issues && (item.kind === "schema" || item.kind === "transformation")) {
          dataset.typed = false;
          break;
        }
        if (!dataset.issues || !config$1.abortEarly && !config$1.abortPipeEarly) dataset = item["~run"](dataset, config$1);
      }
      return dataset;
    }
  };
}
function summarize(issues) {
  let summary = "";
  for (const issue of issues) {
    if (summary) summary += "\n";
    summary += `× ${issue.message}`;
    const dotPath = getDotPath(issue);
    if (dotPath) summary += `
  → at ${dotPath}`;
  }
  return summary;
}

// node_modules/@nktkas/hyperliquid/esm/_base.js
var HyperliquidError = class extends Error {
  constructor(message, options) {
    super(message, options);
    this.name = "HyperliquidError";
  }
};
var ValidationError = class extends HyperliquidError {
  cause;
  constructor(message, options) {
    super(message, options);
    this.name = "ValidationError";
    this.cause = options.cause;
  }
};
function parse2(schema, input) {
  try {
    return parse(schema, input);
  } catch (error) {
    const valiError = error;
    throw new ValidationError(summarize(valiError.issues), { cause: valiError });
  }
}

// node_modules/@nktkas/hyperliquid/esm/transport/_base.js
var TransportError = class extends HyperliquidError {
  constructor(message, options) {
    super(message, options);
    this.name = "TransportError";
  }
};

// node_modules/@nktkas/hyperliquid/esm/transport/_polyfills.js
var Promise_ = (() => {
  return {
    /** @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/withResolvers */
    withResolvers: Promise.withResolvers ? () => Promise.withResolvers() : () => {
      let resolve;
      let reject;
      const promise = new Promise((res, rej) => (resolve = res, reject = rej));
      return { promise, resolve, reject };
    }
  };
})();
var DOMException_ = (() => {
  return globalThis.DOMException || class DOMExceptionPolyfill extends Error {
    constructor(message = "", name = "Error") {
      super(message);
      this.name = name;
    }
  };
})();
var AbortSignal_ = (() => {
  return {
    /** @see https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/any_static */
    any: AbortSignal.any ? (signals) => AbortSignal.any(signals) : (signals) => {
      const controller = new AbortController();
      for (const signal of signals) {
        if (signal.aborted) {
          controller.abort(signal.reason);
          break;
        }
        signal.addEventListener("abort", () => controller.abort(signal.reason), { once: true, signal: controller.signal });
      }
      return controller.signal;
    },
    /** @see https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/timeout_static */
    timeout: AbortSignal.timeout ? (ms) => AbortSignal.timeout(ms) : (ms) => {
      const controller = new AbortController();
      setTimeout(() => controller.abort(new DOMException_("Signal timed out.", "TimeoutError")), ms);
      return controller.signal;
    }
  };
})();
var CustomEvent_ = (() => {
  return globalThis.CustomEvent || class CustomEventPolyfill extends Event {
    detail;
    constructor(type, eventInitDict) {
      super(type, eventInitDict);
      this.detail = eventInitDict?.detail ?? null;
    }
  };
})();

// node_modules/@nktkas/hyperliquid/esm/transport/http/mod.js
var MAINNET_API_URL = "https://api.hyperliquid.xyz";
var TESTNET_API_URL = "https://api.hyperliquid-testnet.xyz";
var MAINNET_RPC_URL = "https://rpc.hyperliquid.xyz";
var TESTNET_RPC_URL = "https://rpc.hyperliquid-testnet.xyz";
var HttpRequestError = class extends TransportError {
  /** The HTTP response that caused the error. */
  response;
  /**
   * Creates a new HTTP request error.
   *
   * @param args The error arguments.
   * @param args.response The HTTP response that caused the error.
   * @param args.message Optional message to append after the status line.
   * @param options The error options.
   */
  constructor(args, options) {
    const { response, message: detail } = args ?? {};
    let message;
    if (response) {
      message = `${response.status} ${response.statusText}`.trim();
      if (detail)
        message += ` - ${detail}`;
    } else {
      message = `Unknown HTTP request error: ${options?.cause}`;
    }
    super(message, options);
    this.name = "HttpRequestError";
    this.response = response;
  }
};
var HttpTransport = class {
  /** Indicates this transport uses testnet endpoint. */
  isTestnet;
  /** Request timeout in ms. Set to `null` to disable. */
  timeout;
  /** Custom API URL for requests. */
  apiUrl;
  /** Custom RPC URL for explorer requests. */
  rpcUrl;
  /** A custom {@link https://developer.mozilla.org/en-US/docs/Web/API/RequestInit | RequestInit} that is merged with a fetch request. */
  fetchOptions;
  /**
   * Creates a new HTTP transport instance.
   *
   * @param options Configuration options for the HTTP transport layer.
   */
  constructor(options) {
    this.isTestnet = options?.isTestnet ?? false;
    this.timeout = options?.timeout === void 0 ? 1e4 : options.timeout;
    this.apiUrl = options?.apiUrl ?? (this.isTestnet ? TESTNET_API_URL : MAINNET_API_URL);
    this.rpcUrl = options?.rpcUrl ?? (this.isTestnet ? TESTNET_RPC_URL : MAINNET_RPC_URL);
    this.fetchOptions = options?.fetchOptions ?? {};
  }
  /**
   * Sends a request to the Hyperliquid API via {@link https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API | fetch}.
   *
   * @param endpoint The API endpoint to send the request to.
   * @param payload The payload to send with the request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return A promise that resolves with parsed JSON response body.
   *
   * @throws {HttpRequestError} Thrown when the HTTP request fails.
   */
  async request(endpoint, payload, signal) {
    try {
      const url = new URL(endpoint, endpoint === "explorer" ? this.rpcUrl : this.apiUrl);
      const init = this._mergeRequestInit({
        body: JSON.stringify(payload),
        headers: {
          "Accept-Encoding": "gzip, deflate, br, zstd",
          "Content-Type": "application/json"
        },
        keepalive: true,
        method: "POST",
        signal: this.timeout ? AbortSignal_.timeout(this.timeout) : void 0
      }, this.fetchOptions, { signal });
      const response = await fetch(url, init);
      if (!response.ok || !response.headers.get("Content-Type")?.includes("application/json")) {
        const clone2 = response.clone();
        const body2 = await response.text().catch(() => void 0);
        throw new HttpRequestError({ response: clone2, message: body2 });
      }
      const clone = response.clone();
      const body = await response.json();
      if (body?.type === "error") {
        throw new HttpRequestError({ response: clone, message: body?.message });
      }
      return body;
    } catch (error) {
      if (error instanceof TransportError)
        throw error;
      throw new HttpRequestError(void 0, { cause: error });
    }
  }
  /** Merges multiple `HeadersInit` into one {@link https://developer.mozilla.org/en-US/docs/Web/API/Headers/Headers | Headers}. */
  _mergeHeadersInit(...inits) {
    const merged = new Headers();
    for (const headers of inits) {
      const entries = Symbol.iterator in headers ? headers : Object.entries(headers);
      for (const [key, value] of entries) {
        merged.set(key, value);
      }
    }
    return merged;
  }
  /** Merges multiple {@link https://developer.mozilla.org/en-US/docs/Web/API/RequestInit | RequestInit} into one {@link https://developer.mozilla.org/en-US/docs/Web/API/RequestInit | RequestInit}. */
  _mergeRequestInit(...inits) {
    const merged = {};
    const headersList = [];
    const signals = [];
    for (const init of inits) {
      Object.assign(merged, init);
      if (init.headers)
        headersList.push(init.headers);
      if (init.signal)
        signals.push(init.signal);
    }
    if (headersList.length > 0)
      merged.headers = this._mergeHeadersInit(...headersList);
    if (signals.length > 0)
      merged.signal = signals.length > 1 ? AbortSignal_.any(signals) : signals[0];
    return merged;
  }
};

// node_modules/@nktkas/rews/esm/mod.js
var ReconnectingWebSocketError = class extends Error {
  /**
   * Error code indicating the type of reconnection error:
   * - `RECONNECTION_LIMIT`: Maximum reconnection attempts reached.
   * - `TERMINATED_BY_USER`: Closed via `close()` method.
   * - `UNKNOWN_ERROR`: Unhandled error in user-provided functions.
   */
  code;
  /**
   * Create a new {@link ReconnectingWebSocketError}.
   *
   * @param code Error code indicating the type of reconnection failure.
   * @param cause Underlying error that caused the reconnection failure.
   */
  constructor(code, cause) {
    super(`Error when reconnecting WebSocket: ${code}`);
    this.name = "ReconnectingWebSocketError";
    this.cause = cause;
    this.code = code;
  }
};
var ReconnectingWebSocket = class _ReconnectingWebSocket extends EventTarget {
  // ============================================================
  // State and initialization
  // ============================================================
  // --- Protected state -------------------------------------------
  /** Current underlying WebSocket instance. */
  _socket;
  /** URL provider for creating new connections. */
  _urlProvider;
  /** Protocols provider for creating new connections. */
  _protocolsProvider;
  /** Binary data type for the WebSocket. */
  _binaryType = "blob";
  /** Attribute-style listener for the `close` event. */
  _onclose = null;
  /** Attribute-style listener for the `error` event. */
  _onerror = null;
  /** Attribute-style listener for the `message` event. */
  _onmessage = null;
  /** Attribute-style listener for the `open` event. */
  _onopen = null;
  /** Current reconnection attempt number. */
  _attempt = 0;
  /** Buffer for messages sent while disconnected. */
  _messageBuffer = [];
  /** Map of currently active attribute-style listeners. */
  _attributeListeners = {};
  /** Controller used to signal permanent termination. */
  _abortController = new AbortController();
  // --- Public state ----------------------------------------------
  /** Reconnection configuration options. */
  reconnectOptions;
  /** Whether the instance has been permanently terminated. */
  get isTerminated() {
    return this._abortController.signal.aborted;
  }
  /** Termination reason, or `undefined` if not yet terminated. */
  get terminationReason() {
    return this._abortController.signal.reason;
  }
  /** AbortSignal that is aborted when the instance is permanently terminated. */
  get terminationSignal() {
    return this._abortController.signal;
  }
  /**
   * Create a new ReconnectingWebSocket.
   *
   * @param url URL or factory function for the WebSocket connection.
   * @param protocolsOrOptions Subprotocol(s), factory function, or options object.
   * @param maybeOptions Configuration options when protocols are provided as second argument.
   */
  constructor(url, protocolsOrOptions, maybeOptions) {
    super();
    const isProtocols = protocolsOrOptions === void 0 || typeof protocolsOrOptions === "string" || typeof protocolsOrOptions === "function" || Array.isArray(protocolsOrOptions);
    const protocols = isProtocols ? protocolsOrOptions : void 0;
    const options = isProtocols ? maybeOptions : protocolsOrOptions;
    if (!globalThis.WebSocket && !options?.WebSocket) {
      throw new TypeError("No WebSocket implementation found. Please provide a custom WebSocket constructor in the options.");
    }
    this._urlProvider = url;
    this._protocolsProvider = protocols;
    this.reconnectOptions = {
      WebSocket: options?.WebSocket ?? WebSocket,
      maxRetries: options?.maxRetries ?? 3,
      connectionTimeout: options?.connectionTimeout === void 0 ? 1e4 : options.connectionTimeout,
      reconnectionDelay: options?.reconnectionDelay ?? ((n) => Math.min(2 ** n * 150, 1e4))
    };
    this._runLoop();
  }
  // ============================================================
  // Reconnection lifecycle
  // ============================================================
  /**
   * Create a new WebSocket instance using the current URL and protocols providers.
   *
   * @return Configured WebSocket instance.
   */
  _createSocket() {
    const url = typeof this._urlProvider === "function" ? this._urlProvider() : this._urlProvider;
    const protocols = typeof this._protocolsProvider === "function" ? this._protocolsProvider() : this._protocolsProvider;
    const socket = createSocketWithTimeout(() => new this.reconnectOptions.WebSocket(url, protocols), this.reconnectOptions.connectionTimeout);
    socket.binaryType = this._binaryType;
    return socket;
  }
  /** Run the main reconnection loop. */
  async _runLoop() {
    try {
      while (true) {
        this._socket = this._createSocket();
        await this._awaitSocketLifecycle();
        if (this.isTerminated)
          break;
        const attempt = this._attempt;
        if (attempt >= this.reconnectOptions.maxRetries) {
          this._cleanup("RECONNECTION_LIMIT");
          break;
        }
        this._attempt++;
        const delay = typeof this.reconnectOptions.reconnectionDelay === "number" ? this.reconnectOptions.reconnectionDelay : this.reconnectOptions.reconnectionDelay(attempt);
        await sleep(delay, this._abortController.signal);
      }
    } catch (error) {
      this._cleanup("UNKNOWN_ERROR", error);
    }
  }
  /** Await the full lifecycle of the current socket until it closes. */
  _awaitSocketLifecycle() {
    return new Promise((resolve) => {
      const ac = new AbortController();
      const { signal } = ac;
      this._socket.addEventListener("open", () => {
        this._attempt = 0;
        let sentCount = 0;
        try {
          for (; sentCount < this._messageBuffer.length; sentCount++) {
            this._socket.send(this._messageBuffer[sentCount]);
          }
          this._messageBuffer = [];
        } catch {
          this._messageBuffer.splice(0, sentCount);
          this._socket.close();
          return;
        }
        this.dispatchEvent(new Event("open"));
      }, { signal });
      this._socket.addEventListener("message", (e) => {
        this.dispatchEvent(new MessageEvent("message", { data: e.data, origin: e.origin }));
      }, { signal });
      this._socket.addEventListener("error", () => {
        this.dispatchEvent(new Event("error"));
      }, { signal });
      this._socket.addEventListener("close", (e) => {
        ac.abort();
        this.dispatchEvent(new CloseEvent("close", {
          code: e.code,
          reason: e.reason,
          wasClean: e.wasClean
        }));
        resolve();
      }, { signal });
    });
  }
  /**
   * Permanently terminate the instance and clean up resources.
   *
   * @param code Error code indicating the type of termination.
   * @param cause Underlying error that triggered cleanup.
   */
  _cleanup(code, cause) {
    if (this.isTerminated)
      return;
    const error = new ReconnectingWebSocketError(code, cause);
    this._abortController.abort(error);
    this._socket?.close();
    this._messageBuffer = [];
    this.dispatchEvent(new CustomEvent("terminate", { detail: error }));
  }
  // ============================================================
  // WebSocket property implementations
  // ============================================================
  // --- Properties ------------------------------------------------
  get url() {
    return this._socket.url;
  }
  get readyState() {
    if (this.isTerminated)
      return _ReconnectingWebSocket.CLOSED;
    return this._socket.readyState;
  }
  get bufferedAmount() {
    return this._socket.bufferedAmount;
  }
  get extensions() {
    return this._socket.extensions;
  }
  get protocol() {
    return this._socket.protocol;
  }
  get binaryType() {
    return this._binaryType;
  }
  set binaryType(value) {
    this._binaryType = value;
    this._socket.binaryType = value;
  }
  // --- Constants -------------------------------------------------
  /** Connection is being established. */
  CONNECTING = 0;
  /** Connection is open and ready to communicate. */
  OPEN = 1;
  /** Connection is in the process of closing. */
  CLOSING = 2;
  /** Connection is closed. */
  CLOSED = 3;
  /** Connection is being established. */
  static CONNECTING = 0;
  /** Connection is open and ready to communicate. */
  static OPEN = 1;
  /** Connection is in the process of closing. */
  static CLOSING = 2;
  /** Connection is closed. */
  static CLOSED = 3;
  // --- Event handlers --------------------------------------------
  /** Attribute-style handler for `close` events. */
  get onclose() {
    return this._onclose;
  }
  /**
   * Set the attribute-style handler for `close` events.
   *
   * @param handler Event handler function, or `null` to remove.
   */
  set onclose(handler) {
    this._onclose = handler;
    this._setAttributeListener("close", handler ? (event) => handler.call(this, event) : null);
  }
  /** Attribute-style handler for `error` events. */
  get onerror() {
    return this._onerror;
  }
  /**
   * Set the attribute-style handler for `error` events.
   *
   * @param handler Event handler function, or `null` to remove.
   */
  set onerror(handler) {
    this._onerror = handler;
    this._setAttributeListener("error", handler ? (event) => handler.call(this, event) : null);
  }
  /** Attribute-style handler for `message` events. */
  get onmessage() {
    return this._onmessage;
  }
  /**
   * Set the attribute-style handler for `message` events.
   *
   * @param handler Event handler function, or `null` to remove.
   */
  set onmessage(handler) {
    this._onmessage = handler;
    this._setAttributeListener("message", handler ? (event) => handler.call(this, event) : null);
  }
  /** Attribute-style handler for `open` events. */
  get onopen() {
    return this._onopen;
  }
  /**
   * Set the attribute-style handler for `open` events.
   *
   * @param handler Event handler function, or `null` to remove.
   */
  set onopen(handler) {
    this._onopen = handler;
    this._setAttributeListener("open", handler ? (event) => handler.call(this, event) : null);
  }
  /**
   * Set or remove an attribute-style event listener.
   *
   * @param type Event type to manage.
   * @param listener Listener function, or `null` to remove.
   */
  _setAttributeListener(type, listener) {
    const previous = this._attributeListeners[type];
    if (previous)
      super.removeEventListener(type, previous);
    if (listener) {
      this._attributeListeners[type] = listener;
      super.addEventListener(type, listener);
    } else {
      delete this._attributeListeners[type];
    }
  }
  // --- Public methods --------------------------------------------
  /**
   * Close the WebSocket connection.
   *
   * @param code Status code for the closure.
   * @param reason Human-readable reason for the closure.
   * @param permanently If `true`, permanently close and stop reconnection.
   *                    If `false`, close only the current socket without affecting reconnection (does not work with `maxRetries=0`).
   *                    Default: `true`.
   */
  close(code, reason, permanently = true) {
    const wasConnecting = this._socket.readyState === _ReconnectingWebSocket.CONNECTING;
    this._socket.close(code, reason);
    if (permanently)
      this._cleanup("TERMINATED_BY_USER");
    if (wasConnecting) {
      this._socket.dispatchEvent(new CloseEvent("close", { code: 1006, reason: "", wasClean: false }));
    }
  }
  /**
   * Send data to the server.
   *
   * If the connection is not open,
   * the data is buffered and sent when the connection is established.
   *
   * @param data Data payload to send.
   */
  send(data) {
    if (this._socket.readyState !== _ReconnectingWebSocket.OPEN && !this.isTerminated) {
      this._messageBuffer.push(data);
    } else {
      this._socket.send(data);
    }
  }
};
function createSocketWithTimeout(socketFactory, timeout) {
  const socket = socketFactory();
  if (timeout === null)
    return socket;
  const timer = setTimeout(() => {
    const wasConnecting = socket.readyState === ReconnectingWebSocket.CONNECTING;
    socket.close(3008, "Timeout");
    if (wasConnecting) {
      socket.dispatchEvent(new CloseEvent("close", { code: 3008, reason: "Timeout", wasClean: false }));
    }
  }, timeout);
  const cleanup = () => clearTimeout(timer);
  for (const type of ["open", "close", "error"]) {
    socket.addEventListener(type, cleanup, { once: true });
  }
  return socket;
}
function sleep(ms, signal) {
  return new Promise((resolve, reject) => {
    if (signal?.aborted)
      return reject(signal.reason);
    const onAbort = () => {
      clearTimeout(timer);
      reject(signal?.reason);
    };
    const timer = setTimeout(() => {
      signal?.removeEventListener("abort", onAbort);
      resolve();
    }, ms);
    signal?.addEventListener("abort", onAbort, { once: true });
  });
}

// node_modules/@nktkas/hyperliquid/esm/transport/websocket/_hyperliquidEventTarget.js
function isHyperliquidEvent(msg) {
  return typeof msg === "object" && msg !== null && "channel" in msg && typeof msg.channel === "string" && "data" in msg;
}
function isPongEvent(msg) {
  return typeof msg === "object" && msg !== null && "channel" in msg && msg.channel === "pong";
}
function isExplorerBlockEvent(msg) {
  return Array.isArray(msg) && msg.length > 0 && typeof msg[0] === "object" && msg[0] !== null && "blockTime" in msg[0] && "hash" in msg[0] && "height" in msg[0] && "numTxs" in msg[0] && "proposer" in msg[0];
}
function isExplorerTxsEvent(msg) {
  return Array.isArray(msg) && msg.length > 0 && typeof msg[0] === "object" && msg[0] !== null && "action" in msg[0] && "block" in msg[0] && "error" in msg[0] && "hash" in msg[0] && "time" in msg[0] && "user" in msg[0];
}
var HyperliquidEventTarget = class extends EventTarget {
  constructor(socket) {
    super();
    socket.addEventListener("message", (event) => {
      try {
        const msg = JSON.parse(event.data);
        if (isHyperliquidEvent(msg)) {
          this.dispatchEvent(new CustomEvent_(msg.channel, { detail: msg.data }));
        } else if (isPongEvent(msg)) {
          this.dispatchEvent(new CustomEvent_("pong", { detail: void 0 }));
        } else if (isExplorerBlockEvent(msg)) {
          this.dispatchEvent(new CustomEvent_("explorerBlock_", { detail: msg }));
        } else if (isExplorerTxsEvent(msg)) {
          this.dispatchEvent(new CustomEvent_("explorerTxs_", { detail: msg }));
        }
      } catch {
      }
    });
  }
};

// node_modules/@nktkas/hyperliquid/esm/transport/websocket/_postRequest.js
var WebSocketRequestError = class extends TransportError {
  constructor(message, options) {
    super(message, options);
    this.name = "WebSocketRequestError";
  }
};
var WebSocketPostRequest = class _WebSocketPostRequest {
  /** Timeout for requests in ms. Set to `null` to disable. */
  timeout;
  _socket;
  _lastId = 0;
  _queue = [];
  /**
   * Creates a new WebSocket async request handler.
   *
   * @param socket WebSocket connection instance for sending requests to the Hyperliquid WebSocket API
   * @param hlEvents Used to recognize Hyperliquid responses and match them with sent requests
   * @param timeout Timeout for requests in ms. Set to `null` to disable.
   */
  constructor(socket, hlEvents, timeout) {
    this.timeout = timeout;
    this._socket = socket;
    hlEvents.addEventListener("subscriptionResponse", (event) => {
      this._queue.find((x) => typeof x.id === "string" && isSubset(JSON.parse(x.id), event.detail))?.resolve(event.detail);
    });
    hlEvents.addEventListener("post", (event) => {
      if (event.detail.response.type === "error") {
        this._queue.find((x) => x.id === event.detail.id)?.reject(new WebSocketRequestError(event.detail.response.payload));
        return;
      }
      const data = event.detail.response.type === "info" ? event.detail.response.payload.data : event.detail.response.payload;
      this._queue.find((x) => x.id === event.detail.id)?.resolve(data);
    });
    hlEvents.addEventListener("pong", () => {
      this._queue.find((x) => x.id === "ping")?.resolve();
    });
    hlEvents.addEventListener("error", (event) => {
      try {
        const idMatch = event.detail.match(/id=(\d+)$/);
        if (idMatch) {
          const id2 = parseInt(idMatch[1], 10);
          this._queue.find((x) => x.id === id2)?.reject(new WebSocketRequestError(event.detail));
          return;
        }
        const request = event.detail.match(/{.*}/)?.[0];
        if (!request)
          return;
        const parsedRequest = JSON.parse(request);
        if ("id" in parsedRequest && typeof parsedRequest.id === "number") {
          this._queue.find((x) => x.id === parsedRequest.id)?.reject(new WebSocketRequestError(event.detail));
          return;
        }
        if ("subscription" in parsedRequest && typeof parsedRequest.subscription === "object" && parsedRequest.subscription !== null) {
          const id2 = _WebSocketPostRequest.requestToId(parsedRequest);
          this._queue.find((x) => x.id === id2)?.reject(new WebSocketRequestError(event.detail));
          return;
        }
        if (event.detail.startsWith("Already subscribed") || event.detail.startsWith("Invalid subscription")) {
          const id2 = _WebSocketPostRequest.requestToId({
            method: "subscribe",
            subscription: parsedRequest
          });
          this._queue.find((x) => x.id === id2)?.reject(new WebSocketRequestError(event.detail));
          return;
        }
        if (event.detail.startsWith("Already unsubscribed")) {
          const id2 = _WebSocketPostRequest.requestToId({
            method: "unsubscribe",
            subscription: parsedRequest
          });
          this._queue.find((x) => x.id === id2)?.reject(new WebSocketRequestError(event.detail));
          return;
        }
        const id = _WebSocketPostRequest.requestToId(parsedRequest);
        this._queue.find((x) => x.id === id)?.reject(new WebSocketRequestError(event.detail));
      } catch {
      }
    });
    const handleClose = () => {
      this._queue.forEach(({ reject }) => {
        reject(new WebSocketRequestError("WebSocket connection closed"));
      });
      this._queue = [];
    };
    socket.addEventListener("close", handleClose);
    socket.addEventListener("error", handleClose);
  }
  async request(method, payloadOrSignal, maybeSignal) {
    try {
      const payload = payloadOrSignal instanceof AbortSignal ? void 0 : payloadOrSignal;
      const userSignal = payloadOrSignal instanceof AbortSignal ? payloadOrSignal : maybeSignal;
      const timeoutSignal = this.timeout ? AbortSignal_.timeout(this.timeout) : void 0;
      const signal = userSignal && timeoutSignal ? AbortSignal_.any([userSignal, timeoutSignal]) : userSignal ?? timeoutSignal;
      if (signal?.aborted)
        return Promise.reject(signal.reason);
      if (this._socket.terminationSignal.aborted) {
        return Promise.reject(this._socket.terminationSignal.reason);
      }
      let id;
      let request;
      if (method === "post") {
        id = ++this._lastId;
        request = { method, id, request: payload };
      } else if (method === "ping") {
        id = "ping";
        request = { method };
      } else {
        request = { method, subscription: payload };
        id = _WebSocketPostRequest.requestToId(request);
      }
      this._socket.send(JSON.stringify(request));
      const { promise, resolve, reject } = Promise_.withResolvers();
      this._queue.push({ id, resolve, reject });
      const onAbort = () => reject(signal?.reason);
      signal?.addEventListener("abort", onAbort, { once: true });
      return await promise.finally(() => {
        const index = this._queue.findIndex((item) => item.id === id);
        if (index !== -1)
          this._queue.splice(index, 1);
        signal?.removeEventListener("abort", onAbort);
      });
    } catch (error) {
      if (error instanceof TransportError)
        throw error;
      throw new WebSocketRequestError(`Unknown error while making a WebSocket request: ${error}`, { cause: error });
    }
  }
  /** Normalizes an object and then converts it to a string. */
  static requestToId(value) {
    const sortedKeys = recursiveSortObjectKeys(value);
    const lowercasedHex = recursiveHexToLowercase(sortedKeys);
    return JSON.stringify(lowercasedHex);
  }
};
function recursiveSortObjectKeys(obj) {
  if (Array.isArray(obj)) {
    return obj.map(recursiveSortObjectKeys);
  }
  if (typeof obj === "object" && obj !== null) {
    const result = {};
    for (const key of Object.keys(obj).sort()) {
      result[key] = recursiveSortObjectKeys(obj[key]);
    }
    return result;
  }
  return obj;
}
function recursiveHexToLowercase(value) {
  if (typeof value === "string" && /^0[xX][0-9a-fA-F]+$/.test(value)) {
    return value.toLowerCase();
  }
  if (Array.isArray(value)) {
    return value.map(recursiveHexToLowercase);
  }
  if (typeof value === "object" && value !== null) {
    const result = {};
    for (const key in value) {
      result[key] = recursiveHexToLowercase(value[key]);
    }
    return result;
  }
  return value;
}
function isSubset(subset, superset) {
  if (typeof subset === "string" && typeof superset === "string") {
    const hexRegex = /^0x[0-9a-f]+$/i;
    return hexRegex.test(subset) && hexRegex.test(superset) ? subset.toLowerCase() === superset.toLowerCase() : subset === superset;
  }
  if (typeof subset !== "object" || typeof superset !== "object" || subset === null || superset === null) {
    return subset === superset;
  }
  if (Array.isArray(subset)) {
    return Array.isArray(superset) && subset.length === superset.length && subset.every((item, i) => isSubset(item, superset[i]));
  }
  const sub = subset;
  const sup = superset;
  return Object.keys(sub).every((key) => key in sup && isSubset(sub[key], sup[key]));
}

// node_modules/@nktkas/hyperliquid/esm/transport/websocket/_subscriptionManager.js
var MAX_SUBSCRIPTIONS = 1e3;
var MAX_UNIQUE_USER_SUBSCRIPTIONS = 10;
var WebSocketSubscriptionManager = class {
  /** Enable automatic re-subscription to Hyperliquid subscription after reconnection. */
  resubscribe;
  _socket;
  _postRequest;
  _hlEvents;
  _subscriptions = /* @__PURE__ */ new Map();
  constructor(socket, postRequest, hlEvents, resubscribe) {
    this._socket = socket;
    this._postRequest = postRequest;
    this._hlEvents = hlEvents;
    this.resubscribe = resubscribe;
    socket.addEventListener("open", () => this._handleOpen());
    socket.addEventListener("close", () => this._handleClose());
    socket.addEventListener("error", () => this._handleClose());
  }
  // ============================================================
  // Public methods
  // ============================================================
  /**
   * Subscribes to a Hyperliquid event channel.
   * Sends a subscription request to the server and listens for events.
   *
   * @param channel The event channel to listen to.
   * @param payload A payload to send with the subscription request.
   * @param listener A function to call when the event is dispatched.
   * @return A promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {WebSocketRequestError} An error that occurs when a WebSocket request fails.
   */
  async subscribe(channel, payload, listener) {
    const id = WebSocketPostRequest.requestToId(payload);
    let subscription = this._subscriptions.get(id);
    if (!subscription) {
      if (this._subscriptions.size >= MAX_SUBSCRIPTIONS) {
        throw new WebSocketRequestError("Cannot subscribe to more than 1000 channels");
      }
      if (this._hasUserParam(payload) && this._countUniqueUserSubscriptions() >= MAX_UNIQUE_USER_SUBSCRIPTIONS) {
        throw new WebSocketRequestError("Cannot track more than 10 unique users");
      }
      const promise = this._postRequest.request("subscribe", payload).finally(() => subscription.promiseFinished = true);
      subscription = {
        listeners: /* @__PURE__ */ new Map(),
        promise,
        promiseFinished: false,
        failureController: new AbortController()
      };
      this._subscriptions.set(id, subscription);
    }
    let unsubscribe = subscription.listeners.get(listener);
    if (!unsubscribe) {
      unsubscribe = async () => {
        this._hlEvents.removeEventListener(channel, listener);
        const subscription2 = this._subscriptions.get(id);
        subscription2?.listeners.delete(listener);
        if (subscription2?.listeners.size === 0) {
          this._subscriptions.delete(id);
          if (this._socket.readyState === 1) {
            await this._postRequest.request("unsubscribe", payload);
          }
        }
      };
      this._hlEvents.addEventListener(channel, listener);
      subscription.listeners.set(listener, unsubscribe);
    }
    await subscription.promise;
    return {
      unsubscribe,
      failureSignal: subscription.failureController.signal
    };
  }
  // ============================================================
  // Socket event handlers
  // ============================================================
  /** Resubscribes to all existing subscriptions if auto-resubscribe is enabled. */
  _handleOpen() {
    if (this.resubscribe) {
      for (const [id, subscription] of this._subscriptions.entries()) {
        if (subscription.promiseFinished) {
          subscription.promise = this._postRequest.request("subscribe", JSON.parse(id)).catch((error) => subscription.failureController.abort(error)).finally(() => subscription.promiseFinished = true);
          subscription.promiseFinished = false;
        }
      }
    }
  }
  /** Cleans up subscriptions if resubscribe is disabled or socket is terminated. */
  _handleClose() {
    if (!this.resubscribe || this._socket.terminationSignal.aborted) {
      for (const subscriptionInfo of this._subscriptions.values()) {
        for (const [_, unsubscribe] of subscriptionInfo.listeners) {
          unsubscribe();
        }
      }
    }
  }
  // ============================================================
  // Subscription limit checks
  // ============================================================
  /** Checks if a payload contains a user parameter. */
  _hasUserParam(payload) {
    return typeof payload === "object" && payload !== null && "user" in payload;
  }
  /** Counts the number of unique users across all active subscriptions. */
  _countUniqueUserSubscriptions() {
    const users = /* @__PURE__ */ new Set();
    for (const id of this._subscriptions.keys()) {
      try {
        const payload = JSON.parse(id);
        if (this._hasUserParam(payload)) {
          users.add(payload.user);
        }
      } catch {
      }
    }
    return users.size;
  }
};

// node_modules/@nktkas/hyperliquid/esm/transport/websocket/mod.js
var MAINNET_API_WS_URL = "wss://api.hyperliquid.xyz/ws";
var TESTNET_API_WS_URL = "wss://api.hyperliquid-testnet.xyz/ws";
var MAINNET_RPC_WS_URL = "wss://rpc.hyperliquid.xyz/ws";
var TESTNET_RPC_WS_URL = "wss://rpc.hyperliquid-testnet.xyz/ws";
var WebSocketTransport = class {
  /** Indicates this transport uses testnet endpoint. */
  isTestnet;
  /** The WebSocket that is used for communication. */
  socket;
  /** Enable automatic re-subscription to Hyperliquid subscription after reconnection. */
  get resubscribe() {
    return this._subscriptionManager.resubscribe;
  }
  set resubscribe(value) {
    this._subscriptionManager.resubscribe = value;
  }
  /** Timeout for requests in ms. Set to `null` to disable. */
  get timeout() {
    return this._postRequest.timeout;
  }
  set timeout(value) {
    this._postRequest.timeout = value;
  }
  _postRequest;
  _hlEvents;
  _subscriptionManager;
  _keepAliveInterval;
  /**
   * Creates a new WebSocket transport instance.
   *
   * @param options Configuration options for the WebSocket transport layer.
   */
  constructor(options) {
    this.isTestnet = options?.isTestnet ?? false;
    this.socket = new ReconnectingWebSocket(options?.url ?? (this.isTestnet ? TESTNET_API_WS_URL : MAINNET_API_WS_URL), options?.reconnect);
    this._hlEvents = new HyperliquidEventTarget(this.socket);
    this._postRequest = new WebSocketPostRequest(this.socket, this._hlEvents, options?.timeout === void 0 ? 1e4 : options.timeout);
    this._subscriptionManager = new WebSocketSubscriptionManager(this.socket, this._postRequest, this._hlEvents, options?.resubscribe ?? true);
    this._initKeepAlive();
  }
  // ============================================================
  // Public methods
  // ============================================================
  /**
   * Sends a request to the Hyperliquid API via WebSocket.
   *
   * @param endpoint The API endpoint to send the request to.
   * @param payload The payload to send with the request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return A promise that resolves with parsed JSON response body.
   *
   * @throws {WebSocketRequestError} An error that occurs when a WebSocket request fails.
   */
  async request(endpoint, payload, signal) {
    const payload_ = { type: endpoint === "exchange" ? "action" : endpoint, payload };
    return await this._postRequest.request("post", payload_, signal);
  }
  /**
   * Subscribes to a Hyperliquid event channel.
   * Sends a subscription request to the server and listens for events.
   *
   * @param channel The event channel to listen to.
   * @param payload A payload to send with the subscription request.
   * @param listener A function to call when the event is dispatched.
   * @return A promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {WebSocketRequestError} An error that occurs when a WebSocket request fails.
   */
  subscribe(channel, payload, listener) {
    return this._subscriptionManager.subscribe(channel, payload, listener);
  }
  /**
   * Waits until the WebSocket connection is ready.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the promise.
   * @return A promise that resolves when the connection is ready.
   *
   * @throws {WebSocketRequestError} When the connection cannot be established.
   */
  ready(signal) {
    return new Promise((resolve, reject) => {
      const combinedSignal = signal ? AbortSignal_.any([this.socket.terminationSignal, signal]) : this.socket.terminationSignal;
      if (combinedSignal.aborted) {
        return reject(new WebSocketRequestError("Failed to establish WebSocket connection", { cause: combinedSignal.reason }));
      }
      if (this.socket.readyState === ReconnectingWebSocket.OPEN)
        return resolve();
      const handleOpen = () => {
        combinedSignal.removeEventListener("abort", handleAbort);
        resolve();
      };
      const handleAbort = () => {
        this.socket.removeEventListener("open", handleOpen);
        return reject(new WebSocketRequestError("Failed to establish WebSocket connection", { cause: combinedSignal.reason }));
      };
      this.socket.addEventListener("open", handleOpen, { once: true });
      combinedSignal.addEventListener("abort", handleAbort, { once: true });
    });
  }
  /**
   * Closes the WebSocket connection and waits until it is fully closed.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the promise.
   * @return A promise that resolves when the connection is fully closed.
   *
   * @throws {WebSocketRequestError} When the connection cannot be closed.
   */
  close(signal) {
    return new Promise((resolve, reject) => {
      if (signal?.aborted) {
        return reject(new WebSocketRequestError("Failed to close WebSocket connection", { cause: signal.reason }));
      }
      if (this.socket.readyState === ReconnectingWebSocket.CLOSED)
        return resolve();
      const handleClose = () => {
        signal?.removeEventListener("abort", handleAbort);
        resolve();
      };
      const handleAbort = () => {
        return reject(new WebSocketRequestError("Failed to close WebSocket connection", { cause: signal?.reason }));
      };
      this.socket.addEventListener("close", handleClose, { once: true, signal });
      this.socket.addEventListener("error", handleClose, { once: true, signal });
      signal?.addEventListener("abort", handleAbort, { once: true });
      this.socket.close();
    });
  }
  // ============================================================
  // Keep-Alive Logic
  // ============================================================
  _initKeepAlive() {
    const start = () => {
      if (this._keepAliveInterval)
        return;
      this._keepAliveInterval = setInterval(() => {
        this.socket.send('{"method":"ping"}');
      }, 3e4);
    };
    const stop = () => {
      clearInterval(this._keepAliveInterval);
      this._keepAliveInterval = void 0;
    };
    this.socket.addEventListener("open", start);
    this.socket.addEventListener("close", stop);
    this.socket.addEventListener("error", stop);
  }
};

// node_modules/@nktkas/hyperliquid/esm/utils/_format.js
function formatDecimalString(value) {
  return value.trim().replace(/^(-?)0+(?=\d)/, "$1").replace(/\.0*$|(\.\d+?)0+$/, "$1").replace(/^(-?)\./, "$10.").replace(/^-?$/, "0").replace(/^-0$/, "0");
}

// node_modules/@nktkas/hyperliquid/esm/api/_schemas.js
var UnsignedDecimal = (() => {
  return pipe(
    union([string(), number()]),
    toString(),
    string(),
    // HACK: for correct JSONSchema generation
    transform((value) => formatDecimalString(value)),
    regex(/^[0-9]+(\.[0-9]+)?$/)
  );
})();
var Decimal = (() => {
  return pipe(
    union([string(), number()]),
    toString(),
    string(),
    // HACK: for correct JSONSchema generation
    transform((value) => formatDecimalString(value)),
    regex(/^-?[0-9]+(\.[0-9]+)?$/)
  );
})();
var Integer = (() => {
  return pipe(
    union([string(), number()]),
    toNumber(),
    number(),
    // HACK: for correct JSONSchema generation
    safeInteger()
  );
})();
var UnsignedInteger = (() => {
  return pipe(
    union([string(), number()]),
    toNumber(),
    number(),
    // HACK: for correct JSONSchema generation
    safeInteger(),
    minValue(0)
  );
})();
var Hex = (() => {
  return pipe(string(), regex(/^0[xX][0-9a-fA-F]+$/), transform((value) => value.toLowerCase()));
})();
var Address = (() => {
  return pipe(Hex, length(42));
})();
var Cloid = (() => {
  return pipe(Hex, length(34));
})();
var Percent = (() => {
  return pipe(string(), regex(/^[0-9]+(\.[0-9]+)?%$/), transform((value) => value));
})();

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/_base/commonSchemas.js
var SignatureSchema = (() => {
  return object({
    /** First 32-byte component. */
    r: pipe(Hex, length(66)),
    /** Second 32-byte component. */
    s: pipe(Hex, length(66)),
    /** Recovery identifier. */
    v: picklist([27, 28])
  });
})();
var HyperliquidChainSchema = (() => {
  return picklist(["Mainnet", "Testnet"]);
})();

// node_modules/@noble/hashes/_u64.js
var U32_MASK64 = BigInt(2 ** 32 - 1);
var _32n = BigInt(32);
function fromBig(n, le = false) {
  if (le)
    return { h: Number(n & U32_MASK64), l: Number(n >> _32n & U32_MASK64) };
  return { h: Number(n >> _32n & U32_MASK64) | 0, l: Number(n & U32_MASK64) | 0 };
}
function split(lst, le = false) {
  const len = lst.length;
  let Ah = new Uint32Array(len);
  let Al = new Uint32Array(len);
  for (let i = 0; i < len; i++) {
    const { h, l } = fromBig(lst[i], le);
    [Ah[i], Al[i]] = [h, l];
  }
  return [Ah, Al];
}
var rotlSH = (h, l, s) => h << s | l >>> 32 - s;
var rotlSL = (h, l, s) => l << s | h >>> 32 - s;
var rotlBH = (h, l, s) => l << s - 32 | h >>> 64 - s;
var rotlBL = (h, l, s) => h << s - 32 | l >>> 64 - s;

// node_modules/@noble/hashes/utils.js
function isBytes(a) {
  return a instanceof Uint8Array || ArrayBuffer.isView(a) && a.constructor.name === "Uint8Array";
}
function anumber(n, title = "") {
  if (!Number.isSafeInteger(n) || n < 0) {
    const prefix = title && `"${title}" `;
    throw new Error(`${prefix}expected integer >= 0, got ${n}`);
  }
}
function abytes(value, length2, title = "") {
  const bytes = isBytes(value);
  const len = value?.length;
  const needsLen = length2 !== void 0;
  if (!bytes || needsLen && len !== length2) {
    const prefix = title && `"${title}" `;
    const ofLen = needsLen ? ` of length ${length2}` : "";
    const got = bytes ? `length=${len}` : `type=${typeof value}`;
    throw new Error(prefix + "expected Uint8Array" + ofLen + ", got " + got);
  }
  return value;
}
function aexists(instance, checkFinished = true) {
  if (instance.destroyed)
    throw new Error("Hash instance has been destroyed");
  if (checkFinished && instance.finished)
    throw new Error("Hash#digest() has already been called");
}
function aoutput(out, instance) {
  abytes(out, void 0, "digestInto() output");
  const min = instance.outputLen;
  if (out.length < min) {
    throw new Error('"digestInto() output" expected to be of length >=' + min);
  }
}
function u32(arr) {
  return new Uint32Array(arr.buffer, arr.byteOffset, Math.floor(arr.byteLength / 4));
}
function clean(...arrays) {
  for (let i = 0; i < arrays.length; i++) {
    arrays[i].fill(0);
  }
}
var isLE = (() => new Uint8Array(new Uint32Array([287454020]).buffer)[0] === 68)();
function byteSwap(word) {
  return word << 24 & 4278190080 | word << 8 & 16711680 | word >>> 8 & 65280 | word >>> 24 & 255;
}
function byteSwap32(arr) {
  for (let i = 0; i < arr.length; i++) {
    arr[i] = byteSwap(arr[i]);
  }
  return arr;
}
var swap32IfBE = isLE ? (u) => u : byteSwap32;
var hasHexBuiltin = (() => (
  // @ts-ignore
  typeof Uint8Array.from([]).toHex === "function" && typeof Uint8Array.fromHex === "function"
))();
var hexes = Array.from({ length: 256 }, (_, i) => i.toString(16).padStart(2, "0"));
function bytesToHex(bytes) {
  abytes(bytes);
  if (hasHexBuiltin)
    return bytes.toHex();
  let hex = "";
  for (let i = 0; i < bytes.length; i++) {
    hex += hexes[bytes[i]];
  }
  return hex;
}
var asciis = { _0: 48, _9: 57, A: 65, F: 70, a: 97, f: 102 };
function asciiToBase16(ch) {
  if (ch >= asciis._0 && ch <= asciis._9)
    return ch - asciis._0;
  if (ch >= asciis.A && ch <= asciis.F)
    return ch - (asciis.A - 10);
  if (ch >= asciis.a && ch <= asciis.f)
    return ch - (asciis.a - 10);
  return;
}
function hexToBytes(hex) {
  if (typeof hex !== "string")
    throw new Error("hex string expected, got " + typeof hex);
  if (hasHexBuiltin)
    return Uint8Array.fromHex(hex);
  const hl = hex.length;
  const al = hl / 2;
  if (hl % 2)
    throw new Error("hex string expected, got unpadded hex of length " + hl);
  const array2 = new Uint8Array(al);
  for (let ai = 0, hi = 0; ai < al; ai++, hi += 2) {
    const n1 = asciiToBase16(hex.charCodeAt(hi));
    const n2 = asciiToBase16(hex.charCodeAt(hi + 1));
    if (n1 === void 0 || n2 === void 0) {
      const char = hex[hi] + hex[hi + 1];
      throw new Error('hex string expected, got non-hex character "' + char + '" at index ' + hi);
    }
    array2[ai] = n1 * 16 + n2;
  }
  return array2;
}
function concatBytes(...arrays) {
  let sum = 0;
  for (let i = 0; i < arrays.length; i++) {
    const a = arrays[i];
    abytes(a);
    sum += a.length;
  }
  const res = new Uint8Array(sum);
  for (let i = 0, pad = 0; i < arrays.length; i++) {
    const a = arrays[i];
    res.set(a, pad);
    pad += a.length;
  }
  return res;
}
function createHasher(hashCons, info = {}) {
  const hashC = (msg, opts) => hashCons(opts).update(msg).digest();
  const tmp = hashCons(void 0);
  hashC.outputLen = tmp.outputLen;
  hashC.blockLen = tmp.blockLen;
  hashC.create = (opts) => hashCons(opts);
  Object.assign(hashC, info);
  return Object.freeze(hashC);
}
var oidNist = (suffix) => ({
  oid: Uint8Array.from([6, 9, 96, 134, 72, 1, 101, 3, 4, 2, suffix])
});

// node_modules/@noble/hashes/sha3.js
var _0n = BigInt(0);
var _1n = BigInt(1);
var _2n = BigInt(2);
var _7n = BigInt(7);
var _256n = BigInt(256);
var _0x71n = BigInt(113);
var SHA3_PI = [];
var SHA3_ROTL = [];
var _SHA3_IOTA = [];
for (let round = 0, R = _1n, x = 1, y = 0; round < 24; round++) {
  [x, y] = [y, (2 * x + 3 * y) % 5];
  SHA3_PI.push(2 * (5 * y + x));
  SHA3_ROTL.push((round + 1) * (round + 2) / 2 % 64);
  let t = _0n;
  for (let j = 0; j < 7; j++) {
    R = (R << _1n ^ (R >> _7n) * _0x71n) % _256n;
    if (R & _2n)
      t ^= _1n << (_1n << BigInt(j)) - _1n;
  }
  _SHA3_IOTA.push(t);
}
var IOTAS = split(_SHA3_IOTA, true);
var SHA3_IOTA_H = IOTAS[0];
var SHA3_IOTA_L = IOTAS[1];
var rotlH = (h, l, s) => s > 32 ? rotlBH(h, l, s) : rotlSH(h, l, s);
var rotlL = (h, l, s) => s > 32 ? rotlBL(h, l, s) : rotlSL(h, l, s);
function keccakP(s, rounds = 24) {
  const B = new Uint32Array(5 * 2);
  for (let round = 24 - rounds; round < 24; round++) {
    for (let x = 0; x < 10; x++)
      B[x] = s[x] ^ s[x + 10] ^ s[x + 20] ^ s[x + 30] ^ s[x + 40];
    for (let x = 0; x < 10; x += 2) {
      const idx1 = (x + 8) % 10;
      const idx0 = (x + 2) % 10;
      const B0 = B[idx0];
      const B1 = B[idx0 + 1];
      const Th = rotlH(B0, B1, 1) ^ B[idx1];
      const Tl = rotlL(B0, B1, 1) ^ B[idx1 + 1];
      for (let y = 0; y < 50; y += 10) {
        s[x + y] ^= Th;
        s[x + y + 1] ^= Tl;
      }
    }
    let curH = s[2];
    let curL = s[3];
    for (let t = 0; t < 24; t++) {
      const shift = SHA3_ROTL[t];
      const Th = rotlH(curH, curL, shift);
      const Tl = rotlL(curH, curL, shift);
      const PI = SHA3_PI[t];
      curH = s[PI];
      curL = s[PI + 1];
      s[PI] = Th;
      s[PI + 1] = Tl;
    }
    for (let y = 0; y < 50; y += 10) {
      for (let x = 0; x < 10; x++)
        B[x] = s[y + x];
      for (let x = 0; x < 10; x++)
        s[y + x] ^= ~B[(x + 2) % 10] & B[(x + 4) % 10];
    }
    s[0] ^= SHA3_IOTA_H[round];
    s[1] ^= SHA3_IOTA_L[round];
  }
  clean(B);
}
var Keccak = class _Keccak {
  state;
  pos = 0;
  posOut = 0;
  finished = false;
  state32;
  destroyed = false;
  blockLen;
  suffix;
  outputLen;
  enableXOF = false;
  rounds;
  // NOTE: we accept arguments in bytes instead of bits here.
  constructor(blockLen, suffix, outputLen, enableXOF = false, rounds = 24) {
    this.blockLen = blockLen;
    this.suffix = suffix;
    this.outputLen = outputLen;
    this.enableXOF = enableXOF;
    this.rounds = rounds;
    anumber(outputLen, "outputLen");
    if (!(0 < blockLen && blockLen < 200))
      throw new Error("only keccak-f1600 function is supported");
    this.state = new Uint8Array(200);
    this.state32 = u32(this.state);
  }
  clone() {
    return this._cloneInto();
  }
  keccak() {
    swap32IfBE(this.state32);
    keccakP(this.state32, this.rounds);
    swap32IfBE(this.state32);
    this.posOut = 0;
    this.pos = 0;
  }
  update(data) {
    aexists(this);
    abytes(data);
    const { blockLen, state } = this;
    const len = data.length;
    for (let pos = 0; pos < len; ) {
      const take = Math.min(blockLen - this.pos, len - pos);
      for (let i = 0; i < take; i++)
        state[this.pos++] ^= data[pos++];
      if (this.pos === blockLen)
        this.keccak();
    }
    return this;
  }
  finish() {
    if (this.finished)
      return;
    this.finished = true;
    const { state, suffix, pos, blockLen } = this;
    state[pos] ^= suffix;
    if ((suffix & 128) !== 0 && pos === blockLen - 1)
      this.keccak();
    state[blockLen - 1] ^= 128;
    this.keccak();
  }
  writeInto(out) {
    aexists(this, false);
    abytes(out);
    this.finish();
    const bufferOut = this.state;
    const { blockLen } = this;
    for (let pos = 0, len = out.length; pos < len; ) {
      if (this.posOut >= blockLen)
        this.keccak();
      const take = Math.min(blockLen - this.posOut, len - pos);
      out.set(bufferOut.subarray(this.posOut, this.posOut + take), pos);
      this.posOut += take;
      pos += take;
    }
    return out;
  }
  xofInto(out) {
    if (!this.enableXOF)
      throw new Error("XOF is not possible for this instance");
    return this.writeInto(out);
  }
  xof(bytes) {
    anumber(bytes);
    return this.xofInto(new Uint8Array(bytes));
  }
  digestInto(out) {
    aoutput(out, this);
    if (this.finished)
      throw new Error("digest() was already called");
    this.writeInto(out);
    this.destroy();
    return out;
  }
  digest() {
    return this.digestInto(new Uint8Array(this.outputLen));
  }
  destroy() {
    this.destroyed = true;
    clean(this.state);
  }
  _cloneInto(to) {
    const { blockLen, suffix, outputLen, rounds, enableXOF } = this;
    to ||= new _Keccak(blockLen, suffix, outputLen, enableXOF, rounds);
    to.state32.set(this.state32);
    to.pos = this.pos;
    to.posOut = this.posOut;
    to.finished = this.finished;
    to.rounds = rounds;
    to.suffix = suffix;
    to.outputLen = outputLen;
    to.enableXOF = enableXOF;
    to.destroyed = this.destroyed;
    return to;
  }
};
var genKeccak = (suffix, blockLen, outputLen, info = {}) => createHasher(() => new Keccak(blockLen, suffix, outputLen), info);
var sha3_224 = genKeccak(
  6,
  144,
  28,
  oidNist(7)
);
var sha3_256 = genKeccak(
  6,
  136,
  32,
  oidNist(8)
);
var sha3_384 = genKeccak(
  6,
  104,
  48,
  oidNist(9)
);
var sha3_512 = genKeccak(
  6,
  72,
  64,
  oidNist(10)
);
var keccak_224 = genKeccak(1, 144, 28);
var keccak_256 = genKeccak(1, 136, 32);
var keccak_384 = genKeccak(1, 104, 48);
var keccak_512 = genKeccak(1, 72, 64);
var genShake = (suffix, blockLen, outputLen, info = {}) => createHasher((opts = {}) => new Keccak(blockLen, suffix, opts.dkLen === void 0 ? outputLen : opts.dkLen, true), info);
var shake128 = genShake(31, 168, 16, oidNist(11));
var shake256 = genShake(31, 136, 32, oidNist(12));
var shake128_32 = genShake(31, 168, 32, oidNist(11));
var shake256_64 = genShake(31, 136, 64, oidNist(12));

// node_modules/@nktkas/hyperliquid/esm/deps/jsr.io/@std/bytes/1.0.6/concat.js
function concat(buffers) {
  let length2 = 0;
  for (const buffer of buffers) {
    length2 += buffer.length;
  }
  const output = new Uint8Array(length2);
  let index = 0;
  for (const buffer of buffers) {
    output.set(buffer, index);
    index += buffer.length;
  }
  return output;
}

// node_modules/@nktkas/hyperliquid/esm/deps/jsr.io/@std/msgpack/1.0.3/encode.js
var FOUR_BITS = 16;
var FIVE_BITS = 32;
var SEVEN_BITS = 128;
var EIGHT_BITS = 256;
var FIFTEEN_BITS = 32768;
var SIXTEEN_BITS = 65536;
var THIRTY_ONE_BITS = 2147483648;
var THIRTY_TWO_BITS = 4294967296;
var SIXTY_THREE_BITS = 9223372036854775808n;
var SIXTY_FOUR_BITS = 18446744073709551616n;
var encoder = new TextEncoder();
function encode(object2) {
  const byteParts = [];
  encodeSlice(object2, byteParts);
  return concat(byteParts);
}
function encodeFloat64(num) {
  const dataView = new DataView(new ArrayBuffer(9));
  dataView.setFloat64(1, num);
  dataView.setUint8(0, 203);
  return new Uint8Array(dataView.buffer);
}
function encodeNumber(num) {
  if (!Number.isInteger(num)) {
    return encodeFloat64(num);
  }
  if (num < 0) {
    if (num >= -FIVE_BITS) {
      return new Uint8Array([num]);
    }
    if (num >= -SEVEN_BITS) {
      return new Uint8Array([208, num]);
    }
    if (num >= -FIFTEEN_BITS) {
      const dataView = new DataView(new ArrayBuffer(3));
      dataView.setInt16(1, num);
      dataView.setUint8(0, 209);
      return new Uint8Array(dataView.buffer);
    }
    if (num >= -THIRTY_ONE_BITS) {
      const dataView = new DataView(new ArrayBuffer(5));
      dataView.setInt32(1, num);
      dataView.setUint8(0, 210);
      return new Uint8Array(dataView.buffer);
    }
    return encodeFloat64(num);
  }
  if (num <= 127) {
    return new Uint8Array([num]);
  }
  if (num < EIGHT_BITS) {
    return new Uint8Array([204, num]);
  }
  if (num < SIXTEEN_BITS) {
    const dataView = new DataView(new ArrayBuffer(3));
    dataView.setUint16(1, num);
    dataView.setUint8(0, 205);
    return new Uint8Array(dataView.buffer);
  }
  if (num < THIRTY_TWO_BITS) {
    const dataView = new DataView(new ArrayBuffer(5));
    dataView.setUint32(1, num);
    dataView.setUint8(0, 206);
    return new Uint8Array(dataView.buffer);
  }
  return encodeFloat64(num);
}
function encodeSlice(object2, byteParts) {
  if (object2 === null) {
    byteParts.push(new Uint8Array([192]));
    return;
  }
  if (object2 === false) {
    byteParts.push(new Uint8Array([194]));
    return;
  }
  if (object2 === true) {
    byteParts.push(new Uint8Array([195]));
    return;
  }
  if (typeof object2 === "number") {
    byteParts.push(encodeNumber(object2));
    return;
  }
  if (typeof object2 === "bigint") {
    if (object2 < 0) {
      if (object2 < -SIXTY_THREE_BITS) {
        throw new Error("Cannot safely encode bigint larger than 64 bits");
      }
      const dataView2 = new DataView(new ArrayBuffer(9));
      dataView2.setBigInt64(1, object2);
      dataView2.setUint8(0, 211);
      byteParts.push(new Uint8Array(dataView2.buffer));
      return;
    }
    if (object2 >= SIXTY_FOUR_BITS) {
      throw new Error("Cannot safely encode bigint larger than 64 bits");
    }
    const dataView = new DataView(new ArrayBuffer(9));
    dataView.setBigUint64(1, object2);
    dataView.setUint8(0, 207);
    byteParts.push(new Uint8Array(dataView.buffer));
    return;
  }
  if (typeof object2 === "string") {
    const encoded = encoder.encode(object2);
    const len = encoded.length;
    if (len < FIVE_BITS) {
      byteParts.push(new Uint8Array([160 | len]));
    } else if (len < EIGHT_BITS) {
      byteParts.push(new Uint8Array([217, len]));
    } else if (len < SIXTEEN_BITS) {
      const dataView = new DataView(new ArrayBuffer(3));
      dataView.setUint16(1, len);
      dataView.setUint8(0, 218);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else if (len < THIRTY_TWO_BITS) {
      const dataView = new DataView(new ArrayBuffer(5));
      dataView.setUint32(1, len);
      dataView.setUint8(0, 219);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else {
      throw new Error("Cannot safely encode string with size larger than 32 bits");
    }
    byteParts.push(encoded);
    return;
  }
  if (object2 instanceof Uint8Array) {
    if (object2.length < EIGHT_BITS) {
      byteParts.push(new Uint8Array([196, object2.length]));
    } else if (object2.length < SIXTEEN_BITS) {
      const dataView = new DataView(new ArrayBuffer(3));
      dataView.setUint16(1, object2.length);
      dataView.setUint8(0, 197);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else if (object2.length < THIRTY_TWO_BITS) {
      const dataView = new DataView(new ArrayBuffer(5));
      dataView.setUint32(1, object2.length);
      dataView.setUint8(0, 198);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else {
      throw new Error("Cannot safely encode Uint8Array with size larger than 32 bits");
    }
    byteParts.push(object2);
    return;
  }
  if (Array.isArray(object2)) {
    if (object2.length < FOUR_BITS) {
      byteParts.push(new Uint8Array([144 | object2.length]));
    } else if (object2.length < SIXTEEN_BITS) {
      const dataView = new DataView(new ArrayBuffer(3));
      dataView.setUint16(1, object2.length);
      dataView.setUint8(0, 220);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else if (object2.length < THIRTY_TWO_BITS) {
      const dataView = new DataView(new ArrayBuffer(5));
      dataView.setUint32(1, object2.length);
      dataView.setUint8(0, 221);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else {
      throw new Error("Cannot safely encode array with size larger than 32 bits");
    }
    for (const obj of object2) {
      encodeSlice(obj, byteParts);
    }
    return;
  }
  const prototype = Object.getPrototypeOf(object2);
  if (prototype === null || prototype === Object.prototype) {
    const numKeys = Object.keys(object2).length;
    if (numKeys < FOUR_BITS) {
      byteParts.push(new Uint8Array([128 | numKeys]));
    } else if (numKeys < SIXTEEN_BITS) {
      const dataView = new DataView(new ArrayBuffer(3));
      dataView.setUint16(1, numKeys);
      dataView.setUint8(0, 222);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else if (numKeys < THIRTY_TWO_BITS) {
      const dataView = new DataView(new ArrayBuffer(5));
      dataView.setUint32(1, numKeys);
      dataView.setUint8(0, 223);
      byteParts.push(new Uint8Array(dataView.buffer));
    } else {
      throw new Error("Cannot safely encode map with size larger than 32 bits");
    }
    for (const [key, value] of Object.entries(object2)) {
      encodeSlice(key, byteParts);
      encodeSlice(value, byteParts);
    }
    return;
  }
  throw new Error("Cannot safely encode value into messagepack");
}

// node_modules/@nktkas/hyperliquid/esm/signing/_abstractWallet.js
function isEthersV6Signer(wallet) {
  return "signTypedData" in wallet && typeof wallet.signTypedData === "function" && wallet.signTypedData.length === 3 && "getAddress" in wallet && typeof wallet.getAddress === "function";
}
function isEthersV5Signer(wallet) {
  return "_signTypedData" in wallet && typeof wallet._signTypedData === "function" && wallet._signTypedData.length === 3 && "getAddress" in wallet && typeof wallet.getAddress === "function";
}
function isViemJsonRpcAccount(wallet) {
  return "signTypedData" in wallet && typeof wallet.signTypedData === "function" && (wallet.signTypedData.length === 1 || wallet.signTypedData.length === 2) && "getAddresses" in wallet && typeof wallet.getAddresses === "function" && "getChainId" in wallet && typeof wallet.getChainId === "function";
}
function isViemLocalAccount(wallet) {
  return "signTypedData" in wallet && typeof wallet.signTypedData === "function" && (wallet.signTypedData.length === 1 || wallet.signTypedData.length === 2) && "address" in wallet && typeof wallet.address === "string";
}
var AbstractWalletError = class extends HyperliquidError {
  constructor(message, options) {
    super(message, options);
    this.name = "AbstractWalletError";
  }
};
async function signTypedData(args) {
  const { wallet, domain, types, primaryType, message } = args;
  const isViemWallet = isViemJsonRpcAccount(wallet) || isViemLocalAccount(wallet);
  const isEthersV6 = isEthersV6Signer(wallet);
  const isEthersV5 = isEthersV5Signer(wallet);
  let signature;
  try {
    if (isViemWallet) {
      signature = await wallet.signTypedData({
        domain,
        types: {
          EIP712Domain: [
            { name: "name", type: "string" },
            { name: "version", type: "string" },
            { name: "chainId", type: "uint256" },
            { name: "verifyingContract", type: "address" }
          ],
          ...types
        },
        primaryType,
        message
      });
    } else if (isEthersV6) {
      signature = await wallet.signTypedData(domain, types, message);
    } else if (isEthersV5) {
      signature = await wallet._signTypedData(domain, types, message);
    } else {
      throw new AbstractWalletError("Failed to sign typed data: unknown wallet type");
    }
  } catch (error) {
    if (error instanceof AbstractWalletError)
      throw error;
    const walletType = isViemWallet ? "viem" : isEthersV6 ? "ethers v6" : "ethers v5";
    throw new AbstractWalletError(`Failed to sign typed data with ${walletType} wallet`, { cause: error });
  }
  return splitSignature(signature);
}
function splitSignature(signature) {
  if (signature.length !== 132) {
    throw new AbstractWalletError(`Expected 65-byte signature (132 hex chars), got ${signature.length}`);
  }
  const r = `0x${signature.slice(2, 66)}`;
  const s = `0x${signature.slice(66, 130)}`;
  let v = parseInt(signature.slice(130, 132), 16);
  if (v === 0 || v === 1)
    v += 27;
  if (v !== 27 && v !== 28) {
    throw new AbstractWalletError(`Invalid signature recovery value: ${v}, expected 27 or 28`);
  }
  return { r, s, v };
}
async function getWalletChainId(wallet) {
  try {
    if (isViemJsonRpcAccount(wallet)) {
      const chainId = await wallet.getChainId();
      return `0x${chainId.toString(16)}`;
    }
    const isEthersSigner = isEthersV6Signer(wallet) || isEthersV5Signer(wallet);
    if (isEthersSigner && wallet.provider) {
      const network = await wallet.provider.getNetwork();
      return `0x${network.chainId.toString(16)}`;
    }
  } catch (error) {
    throw new AbstractWalletError("Failed to get chain ID from wallet", { cause: error });
  }
  return "0x1";
}
async function getWalletAddress(wallet) {
  try {
    if (isViemJsonRpcAccount(wallet)) {
      const addresses = await wallet.getAddresses();
      if (!addresses.length)
        throw new AbstractWalletError("Wallet returned no addresses");
      return addresses[0].toLowerCase();
    }
    if (isViemLocalAccount(wallet)) {
      return wallet.address.toLowerCase();
    }
    if (isEthersV6Signer(wallet) || isEthersV5Signer(wallet)) {
      const address = await wallet.getAddress();
      return address.toLowerCase();
    }
  } catch (error) {
    throw new AbstractWalletError("Failed to get address from wallet", { cause: error });
  }
  throw new AbstractWalletError("Failed to get wallet address: unknown wallet type");
}

// node_modules/@nktkas/hyperliquid/esm/signing/mod.js
var ZERO_ADDRESS = "0x0000000000000000000000000000000000000000";
function createL1ActionHash(args) {
  const { action, nonce, vaultAddress, expiresAfter } = args;
  const actionBytes = encode(largeIntToBigInt(removeUndefinedKeys(action)));
  const nonceBytes = toUint64Bytes(nonce);
  const vaultMarker = vaultAddress ? new Uint8Array([1]) : new Uint8Array([0]);
  const vaultBytes = vaultAddress ? hexToBytes(vaultAddress.slice(2)) : new Uint8Array();
  const expiresMarker = expiresAfter !== void 0 ? new Uint8Array([0]) : new Uint8Array();
  const expiresBytes = expiresAfter !== void 0 ? toUint64Bytes(expiresAfter) : new Uint8Array();
  const bytes = concatBytes(actionBytes, nonceBytes, vaultMarker, vaultBytes, expiresMarker, expiresBytes);
  const hash = keccak_256(bytes);
  return `0x${bytesToHex(hash)}`;
}
function toUint64Bytes(n) {
  const bytes = new Uint8Array(8);
  new DataView(bytes.buffer).setBigUint64(0, BigInt(n));
  return bytes;
}
function largeIntToBigInt(obj) {
  if (typeof obj === "number" && Number.isInteger(obj) && (obj >= 4294967296 || obj < -2147483648)) {
    return BigInt(obj);
  }
  if (Array.isArray(obj))
    return obj.map(largeIntToBigInt);
  if (typeof obj === "object" && obj !== null) {
    const result = {};
    for (const key in obj)
      result[key] = largeIntToBigInt(obj[key]);
    return result;
  }
  return obj;
}
function removeUndefinedKeys(obj) {
  if (Array.isArray(obj))
    return obj.map(removeUndefinedKeys);
  if (typeof obj === "object" && obj !== null) {
    const result = {};
    for (const key in obj) {
      if (obj[key] !== void 0) {
        result[key] = removeUndefinedKeys(obj[key]);
      }
    }
    return result;
  }
  return obj;
}
async function signL1Action(args) {
  const { wallet, action, nonce, isTestnet = false, vaultAddress, expiresAfter } = args;
  return await signTypedData({
    wallet,
    domain: {
      name: "Exchange",
      version: "1",
      chainId: 1337,
      // hyperliquid requires chainId to be 1337
      verifyingContract: ZERO_ADDRESS
    },
    types: {
      Agent: [
        { name: "source", type: "string" },
        { name: "connectionId", type: "bytes32" }
      ]
    },
    primaryType: "Agent",
    message: {
      source: isTestnet ? "b" : "a",
      connectionId: createL1ActionHash({ action, nonce, vaultAddress, expiresAfter })
    }
  });
}
async function signUserSignedAction(args) {
  let { wallet, action, types } = args;
  const primaryType = Object.keys(types)[0];
  if (action.type === "approveAgent" && !action.agentName) {
    action = { ...action, agentName: "" };
  }
  if ("payloadMultiSigUser" in action && "outerSigner" in action) {
    const primaryTypeArray = types[primaryType];
    types = {
      ...types,
      [primaryType]: [
        primaryTypeArray[0],
        // after `hyperliquidChain`
        { name: "payloadMultiSigUser", type: "address" },
        { name: "outerSigner", type: "address" },
        ...primaryTypeArray.slice(1)
      ]
    };
  }
  const knownKeys = new Set(types[primaryType].map((f) => f.name));
  const message = Object.fromEntries(Object.entries(action).filter(([k]) => knownKeys.has(k)));
  return await signTypedData({
    wallet,
    domain: {
      name: "HyperliquidSignTransaction",
      version: "1",
      chainId: parseInt(action.signatureChainId),
      verifyingContract: ZERO_ADDRESS
    },
    types,
    primaryType,
    message
  });
}
async function signMultiSigAction(args) {
  const { wallet, nonce, isTestnet = false, vaultAddress, expiresAfter } = args;
  const { type: _, signatureChainId, ...actionRest } = args.action;
  const actionForHash = { signatureChainId, ...actionRest };
  return await signTypedData({
    wallet,
    domain: {
      name: "HyperliquidSignTransaction",
      version: "1",
      chainId: parseInt(signatureChainId),
      verifyingContract: ZERO_ADDRESS
    },
    types: {
      "HyperliquidTransaction:SendMultiSig": [
        { name: "hyperliquidChain", type: "string" },
        { name: "multiSigActionHash", type: "bytes32" },
        { name: "nonce", type: "uint64" }
      ]
    },
    primaryType: "HyperliquidTransaction:SendMultiSig",
    message: {
      hyperliquidChain: isTestnet ? "Testnet" : "Mainnet",
      multiSigActionHash: createL1ActionHash({ action: actionForHash, nonce, vaultAddress, expiresAfter }),
      nonce
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/_base/_nonce.js
var NonceManager = class {
  _map = /* @__PURE__ */ new Map();
  getNonce(key) {
    const now = Date.now();
    this._cleanup(now);
    const lastNonce = this._map.get(key) ?? 0;
    const nonce = now > lastNonce ? now : lastNonce + 1;
    this._map.set(key, nonce);
    return nonce;
  }
  _cleanup(now) {
    for (const [key, lastNonce] of this._map) {
      if (now > lastNonce) {
        this._map.delete(key);
      }
    }
  }
};
var globalNonceManager = new NonceManager();

// node_modules/@nktkas/hyperliquid/esm/deps/jsr.io/@std/async/1.1.1/unstable_semaphore.js
var Semaphore = class {
  #max;
  /** Current number of available permits. */
  #count;
  /** Head of the waiting queue. */
  #head;
  /** Tail of the waiting queue. */
  #tail;
  /**
   * Creates a new semaphore with the specified number of permits.
   *
   * @param max Maximum concurrent permits. Defaults to 1 (mutex).
   * @throws {TypeError} If `max` is not a positive integer.
   */
  constructor(max = 1) {
    if (!Number.isInteger(max) || max < 1) {
      throw new TypeError(`Cannot create semaphore as 'max' must be a positive integer: received ${max}`);
    }
    this.#count = this.#max = max;
  }
  /**
   * Acquires a permit, waiting if none are available.
   *
   * @example Usage
   * ```ts no-assert
   * import { Semaphore } from "@std/async/unstable-semaphore";
   *
   * const sem = new Semaphore(1);
   * await sem.acquire();
   * try {
   *   // critical section
   * } finally {
   *   sem.release();
   * }
   * ```
   *
   * @example Using `using` statement
   * ```ts no-assert
   * import { Semaphore } from "@std/async/unstable-semaphore";
   *
   * const sem = new Semaphore(1);
   * {
   *   using _permit = await sem.acquire();
   *   // critical section
   * } // permit is automatically released when exiting the block
   * ```
   *
   * @returns A promise that resolves to a {@linkcode Disposable} when a permit is acquired.
   */
  acquire() {
    const disposable = { [Symbol.dispose]: () => this.release() };
    if (this.#count > 0) {
      this.#count--;
      return Promise.resolve(disposable);
    }
    return new Promise((res) => {
      const node = { res: () => res(disposable), next: void 0 };
      if (this.#tail) {
        this.#tail = this.#tail.next = node;
      } else {
        this.#head = this.#tail = node;
      }
    });
  }
  /**
   * Tries to acquire a permit without waiting.
   *
   * @example Usage
   * ```ts no-assert
   * import { Semaphore } from "@std/async/unstable-semaphore";
   *
   * const sem = new Semaphore(1);
   * const permit = sem.tryAcquire();
   * if (permit) {
   *   using _ = permit;
   *   // critical section
   * } else {
   *   // resource is busy
   * }
   * ```
   *
   * @returns A {@linkcode Disposable} if a permit was acquired, `undefined` otherwise.
   */
  tryAcquire() {
    if (this.#count > 0) {
      this.#count--;
      return { [Symbol.dispose]: () => this.release() };
    }
    return void 0;
  }
  /**
   * Releases a permit, allowing the next waiter to proceed.
   *
   * @example Usage
   * ```ts no-assert
   * import { Semaphore } from "@std/async/unstable-semaphore";
   *
   * const sem = new Semaphore(1);
   * await sem.acquire();
   * try {
   *   // critical section
   * } finally {
   *   sem.release();
   * }
   * ```
   */
  release() {
    if (this.#head) {
      this.#head.res();
      this.#head = this.#head.next;
      if (!this.#head)
        this.#tail = void 0;
    } else if (this.#count < this.#max) {
      this.#count++;
    }
  }
};

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/_base/_semaphore.js
var RefCountedRegistry = class {
  _map = /* @__PURE__ */ new Map();
  _factory;
  /**
   * Creates a new registry instance.
   *
   * @param factory Factory function used to create a new value when a key is first referenced.
   */
  constructor(factory) {
    this._factory = factory;
  }
  /**
   * Increments the reference count for a key and returns its value.
   *
   * If the key is not present, a new value is created via the factory.
   *
   * @param key Registry key.
   * @return The value associated with the key.
   */
  ref(key) {
    let entry = this._map.get(key);
    if (!entry) {
      entry = { value: this._factory(), refs: 0 };
      this._map.set(key, entry);
    }
    entry.refs++;
    return entry.value;
  }
  /**
   * Decrements the reference count for a key.
   *
   * When the count reaches zero, the entry is removed.
   *
   * @param key Registry key.
   */
  unref(key) {
    const entry = this._map.get(key);
    if (!entry)
      return;
    if (--entry.refs === 0) {
      this._map.delete(key);
    }
  }
};
var semaphores = new RefCountedRegistry(() => new Semaphore(1));
async function withLock(key, fn) {
  const semaphore = semaphores.ref(key);
  await semaphore.acquire();
  try {
    return await fn();
  } finally {
    semaphore.release();
    semaphores.unref(key);
  }
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/_base/errors.js
var ApiRequestError = class extends HyperliquidError {
  response;
  /**
   * @param response Raw API response that contains the error
   */
  constructor(response) {
    const message = extractErrorMessage(response) || "An unknown error occurred while processing an API request. See `response` for more details.";
    super(message);
    this.name = "ApiRequestError";
    this.response = response;
  }
};
function hasError(value) {
  return typeof value === "object" && value !== null && "error" in value && typeof value.error === "string";
}
function hasErrorStatus(response) {
  return typeof response === "object" && response !== null && "status" in response && response.status === "err";
}
function hasStatusesWithErrors(response) {
  if (typeof response !== "object" || response === null)
    return false;
  const r = response;
  const statuses = r.response?.data?.statuses;
  return Array.isArray(statuses) && statuses.some(hasError);
}
function hasSingleStatusWithError(response) {
  if (typeof response !== "object" || response === null)
    return false;
  const r = response;
  return hasError(r.response?.data?.status);
}
function extractErrorMessage(response) {
  if (hasErrorStatus(response)) {
    return response.response;
  }
  const r = response;
  if (Array.isArray(r.response?.data?.statuses)) {
    const errors = r.response.data.statuses.reduce((acc, status, index) => {
      if (hasError(status))
        acc.push(`Order ${index}: ${status.error}`);
      return acc;
    }, []);
    if (errors.length > 0)
      return errors.join(", ");
  }
  if (hasError(r.response?.data?.status)) {
    return r.response.data.status.error;
  }
  return void 0;
}
function assertSuccessResponse(response) {
  if (hasErrorStatus(response) || hasStatusesWithErrors(response) || hasSingleStatusWithError(response)) {
    throw new ApiRequestError(response);
  }
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/_base/execute.js
async function executeL1Action(config, action, options) {
  const { transport } = config;
  const leader = getLeader(config);
  const walletAddress = await getWalletAddress(leader);
  const key = `${walletAddress}:${transport.isTestnet}`;
  return await withLock(key, async () => {
    const nonce = await (config.nonceManager?.(walletAddress) ?? globalNonceManager.getNonce(key));
    const vaultAddress = parse2(optional(Address), options?.vaultAddress ?? config.defaultVaultAddress);
    const expiresAfter = parse2(optional(UnsignedInteger), options?.expiresAfter ?? (typeof config.defaultExpiresAfter === "number" ? config.defaultExpiresAfter : await config.defaultExpiresAfter?.()));
    const signal = options?.signal;
    const [finalAction, signature] = "wallet" in config ? [
      action,
      await signL1Action({
        wallet: leader,
        action,
        nonce,
        isTestnet: transport.isTestnet,
        vaultAddress,
        expiresAfter
      })
    ] : await signMultiSigL1(config, action, walletAddress, nonce, vaultAddress, expiresAfter);
    const response = await transport.request("exchange", {
      action: finalAction,
      signature,
      nonce,
      vaultAddress,
      expiresAfter
    }, signal);
    assertSuccessResponse(response);
    return response;
  });
}
function getNonceFieldName(types) {
  const primaryType = Object.keys(types)[0];
  const field = types[primaryType].find((f) => f.name === "nonce" || f.name === "time");
  return field?.name ?? "nonce";
}
async function executeUserSignedAction(config, action, types, options) {
  const { transport } = config;
  const leader = getLeader(config);
  const walletAddress = await getWalletAddress(leader);
  const key = `${walletAddress}:${transport.isTestnet}`;
  return withLock(key, async () => {
    const nonce = await (config.nonceManager?.(walletAddress) ?? globalNonceManager.getNonce(key));
    const signal = options?.signal;
    const { type, ...restAction } = action;
    const nonceFieldName = getNonceFieldName(types);
    const fullAction = {
      type,
      signatureChainId: await getSignatureChainId(config),
      hyperliquidChain: transport.isTestnet ? "Testnet" : "Mainnet",
      ...restAction,
      [nonceFieldName]: nonce
    };
    const [finalAction, signature] = "wallet" in config ? [fullAction, await signUserSignedAction({ wallet: leader, action: fullAction, types })] : await signMultiSigUserSigned(config, fullAction, types, walletAddress, nonce);
    const response = await transport.request("exchange", {
      action: finalAction,
      signature,
      nonce
    }, signal);
    assertSuccessResponse(response);
    return response;
  });
}
function trimSignature(sig) {
  return {
    r: sig.r.replace(/^0x0+/, "0x"),
    s: sig.s.replace(/^0x0+/, "0x"),
    v: sig.v
  };
}
async function signMultiSigL1(config, action, outerSigner, nonce, vaultAddress, expiresAfter) {
  const { transport: { isTestnet }, signers, multiSigUser } = config;
  const multiSigUser_ = parse2(Address, multiSigUser);
  const outerSigner_ = parse2(Address, outerSigner);
  const signatures = await Promise.all(signers.map(async (signer) => {
    const signature2 = await signL1Action({
      wallet: signer,
      action: [multiSigUser_, outerSigner_, action],
      nonce,
      isTestnet,
      vaultAddress,
      expiresAfter
    });
    return trimSignature(signature2);
  }));
  const multiSigAction = {
    type: "multiSig",
    signatureChainId: await getSignatureChainId(config),
    signatures,
    payload: {
      multiSigUser: multiSigUser_,
      outerSigner: outerSigner_,
      action
    }
  };
  const signature = await signMultiSigAction({
    wallet: signers[0],
    action: multiSigAction,
    nonce,
    isTestnet,
    vaultAddress,
    expiresAfter
  });
  return [multiSigAction, signature];
}
async function signMultiSigUserSigned(config, action, types, outerSigner, nonce) {
  const { signers, multiSigUser, transport: { isTestnet } } = config;
  const multiSigUser_ = parse2(Address, multiSigUser);
  const outerSigner_ = parse2(Address, outerSigner);
  const signatures = await Promise.all(signers.map(async (signer) => {
    const signature2 = await signUserSignedAction({
      wallet: signer,
      action: {
        payloadMultiSigUser: multiSigUser_,
        outerSigner: outerSigner_,
        ...action
      },
      types
    });
    return trimSignature(signature2);
  }));
  const multiSigAction = {
    type: "multiSig",
    signatureChainId: await getSignatureChainId(config),
    signatures,
    payload: {
      multiSigUser: multiSigUser_,
      outerSigner: outerSigner_,
      action
    }
  };
  const signature = await signMultiSigAction({
    wallet: signers[0],
    action: multiSigAction,
    nonce,
    isTestnet
  });
  return [multiSigAction, signature];
}
function getLeader(config) {
  return "wallet" in config ? config.wallet : config.signers[0];
}
async function getSignatureChainId(config) {
  if (config.signatureChainId) {
    const id = typeof config.signatureChainId === "function" ? await config.signatureChainId() : config.signatureChainId;
    return parse2(Hex, id);
  }
  return getWalletChainId(getLeader(config));
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/agentEnableDexAbstraction.js
var AgentEnableDexAbstractionRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("agentEnableDexAbstraction")
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var AgentEnableDexAbstractionActionSchema = (() => {
  return object(AgentEnableDexAbstractionRequest.entries.action.entries);
})();
function agentEnableDexAbstraction(config, opts) {
  const action = parse2(AgentEnableDexAbstractionActionSchema, { type: "agentEnableDexAbstraction" });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/agentSetAbstraction.js
var AgentSetAbstractionRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("agentSetAbstraction"),
      /**
       * User abstraction mode.
       * - `"i"`: disabled
       * - `"u"`: unifiedAccount
       * - `"p"`: portfolioMargin
       */
      abstraction: picklist(["i", "u", "p"])
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var AgentSetAbstractionActionSchema = (() => {
  return object(AgentSetAbstractionRequest.entries.action.entries);
})();
function agentSetAbstraction(config, params, opts) {
  const action = parse2(AgentSetAbstractionActionSchema, { type: "agentSetAbstraction", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/approveAgent.js
var ApproveAgentRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("approveAgent"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Agent address. */
      agentAddress: Address,
      /** Agent name (min 1 and max 16 characters) or null for unnamed agent. */
      agentName: nullish(pipe(string(), check((input) => {
        const baseName = input.replace(/ valid_until \d+$/, "");
        return baseName.length >= 1 && baseName.length <= 16;
      }, (issue) => {
        const baseName = issue.input.replace(/ valid_until \d+$/, "");
        return `Invalid length: Expected >= 1 and <= 16 but received ${baseName.length}`;
      })), null),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var ApproveAgentActionSchema = (() => {
  return omit(object(ApproveAgentRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var ApproveAgentTypes = {
  "HyperliquidTransaction:ApproveAgent": [
    { name: "hyperliquidChain", type: "string" },
    { name: "agentAddress", type: "address" },
    { name: "agentName", type: "string" },
    { name: "nonce", type: "uint64" }
  ]
};
function approveAgent(config, params, opts) {
  const action = parse2(ApproveAgentActionSchema, { type: "approveAgent", ...params });
  return executeUserSignedAction(config, action, ApproveAgentTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/approveBuilderFee.js
var ApproveBuilderFeeRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("approveBuilderFee"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Max fee rate (e.g., "0.01%"). */
      maxFeeRate: Percent,
      /** Builder address. */
      builder: Address,
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var ApproveBuilderFeeActionSchema = (() => {
  return omit(object(ApproveBuilderFeeRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var ApproveBuilderFeeTypes = {
  "HyperliquidTransaction:ApproveBuilderFee": [
    { name: "hyperliquidChain", type: "string" },
    { name: "maxFeeRate", type: "string" },
    { name: "builder", type: "address" },
    { name: "nonce", type: "uint64" }
  ]
};
function approveBuilderFee(config, params, opts) {
  const action = parse2(ApproveBuilderFeeActionSchema, { type: "approveBuilderFee", ...params });
  return executeUserSignedAction(config, action, ApproveBuilderFeeTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/batchModify.js
var BatchModifyRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("batchModify"),
      /** Order modifications. */
      modifies: array(object({
        /** Order ID or Client Order ID. */
        oid: union([UnsignedInteger, Cloid]),
        /** New order parameters. */
        order: object({
          /** Asset ID. */
          a: UnsignedInteger,
          /** Position side (`true` for long, `false` for short). */
          b: boolean(),
          /** Price. */
          p: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
          /** Size (in base currency units). */
          s: UnsignedDecimal,
          /** Is reduce-only? */
          r: boolean(),
          /** Order type (`limit` for limit orders, `trigger` for stop-loss/take-profit orders). */
          t: union([
            object({
              /** Limit order parameters. */
              limit: object({
                /**
                 * Time-in-force.
                 * - `"Gtc"`: Remains active until filled or canceled.
                 * - `"Ioc"`: Fills immediately or cancels any unfilled portion.
                 * - `"Alo"`: Adds liquidity only.
                 * - `"FrontendMarket"`: Similar to Ioc, but add a note that this is market order.
                 */
                tif: picklist(["Gtc", "Ioc", "Alo", "FrontendMarket"])
              })
            }),
            object({
              /** Trigger order parameters. */
              trigger: object({
                /** Is market order? */
                isMarket: boolean(),
                /** Trigger price. */
                triggerPx: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
                /** Indicates whether it is take profit or stop loss. */
                tpsl: picklist(["tp", "sl"])
              })
            })
          ]),
          /** Client Order ID. */
          c: optional(Cloid)
        })
      }))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var BatchModifyActionSchema = (() => {
  return object(BatchModifyRequest.entries.action.entries);
})();
function batchModify(config, params, opts) {
  const action = parse2(BatchModifyActionSchema, { type: "batchModify", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/borrowLend.js
var BorrowLendRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("borrowLend"),
      /** Operation type. */
      operation: picklist(["supply", "withdraw", "repay", "borrow"]),
      /** Token ID. */
      token: UnsignedInteger,
      /** Amount to supply/withdraw (null = full). */
      amount: nullable(UnsignedDecimal)
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var BorrowLendActionSchema = (() => {
  return object(BorrowLendRequest.entries.action.entries);
})();
function borrowLend(config, params, opts) {
  const action = parse2(BorrowLendActionSchema, { type: "borrowLend", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cancel.js
var CancelRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("cancel"),
      /** Orders to cancel by asset and order ID. */
      cancels: array(object({
        /** Asset ID. */
        a: UnsignedInteger,
        /** Order ID. */
        o: UnsignedInteger
      }))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CancelActionSchema = (() => {
  return object(CancelRequest.entries.action.entries);
})();
function cancel(config, params, opts) {
  const action = parse2(CancelActionSchema, { type: "cancel", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cancelByCloid.js
var CancelByCloidRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("cancelByCloid"),
      /** Orders to cancel by asset and client order ID. */
      cancels: array(object({
        /** Asset ID. */
        asset: UnsignedInteger,
        /** Client Order ID. */
        cloid: Cloid
      }))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CancelByCloidActionSchema = (() => {
  return object(CancelByCloidRequest.entries.action.entries);
})();
function cancelByCloid(config, params, opts) {
  const action = parse2(CancelByCloidActionSchema, { type: "cancelByCloid", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cDeposit.js
var CDepositRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("cDeposit"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Amount of wei to deposit into staking balance (float * 1e8). */
      wei: pipe(UnsignedInteger, minValue(1)),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var CDepositActionSchema = (() => {
  return omit(object(CDepositRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var CDepositTypes = {
  "HyperliquidTransaction:CDeposit": [
    { name: "hyperliquidChain", type: "string" },
    { name: "wei", type: "uint64" },
    { name: "nonce", type: "uint64" }
  ]
};
function cDeposit(config, params, opts) {
  const action = parse2(CDepositActionSchema, { type: "cDeposit", ...params });
  return executeUserSignedAction(config, action, CDepositTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/claimRewards.js
var ClaimRewardsRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("claimRewards")
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var ClaimRewardsActionSchema = (() => {
  return object(ClaimRewardsRequest.entries.action.entries);
})();
function claimRewards(config, opts) {
  const action = parse2(ClaimRewardsActionSchema, { type: "claimRewards" });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/convertToMultiSigUser.js
var ConvertToMultiSigUserRequestSignersSchema = (() => {
  return nullable(
    /** Multi-signature configuration. */
    object({
      /** List of authorized user addresses. */
      authorizedUsers: array(Address),
      /** Minimum number of signatures required. */
      threshold: pipe(UnsignedInteger, minValue(1), maxValue(10))
    })
  );
})();
var ConvertToMultiSigUserRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("convertToMultiSigUser"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /**
       * Signers configuration.
       *
       * Must be `ConvertToMultiSigUserRequestSignersSchema` converted to a string via `JSON.stringify(...)`.
       */
      signers: union([
        pipe(string(), parseJson(), ConvertToMultiSigUserRequestSignersSchema, stringifyJson()),
        pipe(ConvertToMultiSigUserRequestSignersSchema, stringifyJson())
      ]),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var ConvertToMultiSigUserActionSchema = (() => {
  return omit(object(ConvertToMultiSigUserRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var ConvertToMultiSigUserTypes = {
  "HyperliquidTransaction:ConvertToMultiSigUser": [
    { name: "hyperliquidChain", type: "string" },
    { name: "signers", type: "string" },
    { name: "nonce", type: "uint64" }
  ]
};
function convertToMultiSigUser(config, params, opts) {
  const action = parse2(ConvertToMultiSigUserActionSchema, { type: "convertToMultiSigUser", ...params });
  return executeUserSignedAction(config, action, ConvertToMultiSigUserTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/createSubAccount.js
var CreateSubAccountRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("createSubAccount"),
      /** Sub-account name. */
      name: pipe(string(), minLength(1), maxLength(16))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CreateSubAccountActionSchema = (() => {
  return object(CreateSubAccountRequest.entries.action.entries);
})();
function createSubAccount(config, params, opts) {
  const action = parse2(CreateSubAccountActionSchema, { type: "createSubAccount", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/createVault.js
var CreateVaultRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("createVault"),
      /** Vault name. */
      name: pipe(string(), minLength(3), maxLength(50)),
      /** Vault description. */
      description: pipe(string(), minLength(10), maxLength(250)),
      /** Initial balance (float * 1e6). */
      initialUsd: pipe(UnsignedInteger, minValue(100 * 1e6)),
      // 100 USD
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CreateVaultActionSchema = (() => {
  return object(CreateVaultRequest.entries.action.entries);
})();
function createVault(config, params, opts) {
  const action = parse2(CreateVaultActionSchema, { type: "createVault", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cSignerAction.js
var CSignerActionRequest = (() => {
  return object({
    /** Action to jail or unjail the signer. */
    action: variant("type", [
      object({
        /** Type of action. */
        type: literal("CSignerAction"),
        /** Jail the signer. */
        jailSelf: null_()
      }),
      object({
        /** Type of action. */
        type: literal("CSignerAction"),
        /** Unjail the signer. */
        unjailSelf: null_()
      })
    ]),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CSignerActionActionSchema = (() => {
  return variant("type", CSignerActionRequest.entries.action.options);
})();
function cSignerAction(config, params, opts) {
  const action = parse2(CSignerActionActionSchema, { type: "CSignerAction", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cValidatorAction.js
var CValidatorActionRequest = (() => {
  return object({
    /** Validator management action. */
    action: variant("type", [
      object({
        /** Type of action. */
        type: literal("CValidatorAction"),
        /** Profile changes to apply. */
        changeProfile: object({
          /** Validator node IP address. */
          node_ip: nullable(object({
            /** IP address. */
            Ip: pipe(string(), ip())
          })),
          /** Validator name. */
          name: nullable(string()),
          /** Validator description. */
          description: nullable(string()),
          /** Whether the validator is unjailed. */
          unjailed: boolean(),
          /** Enable or disable delegations. */
          disable_delegations: nullable(boolean()),
          /** Commission rate in basis points (1 = 0.0001%). */
          commission_bps: nullable(UnsignedInteger),
          /** Signer address. */
          signer: nullable(Address)
        })
      }),
      object({
        /** Type of action. */
        type: literal("CValidatorAction"),
        /** Registration parameters. */
        register: object({
          /** Validator profile information. */
          profile: object({
            /** Validator node IP address. */
            node_ip: object({
              /** IP address. */
              Ip: pipe(string(), ip())
            }),
            /** Validator name. */
            name: string(),
            /** Validator description. */
            description: string(),
            /** Whether delegations are disabled. */
            delegations_disabled: boolean(),
            /** Commission rate in basis points (1 = 0.0001%). */
            commission_bps: UnsignedInteger,
            /** Signer address. */
            signer: Address
          }),
          /** Initial jail status. */
          unjailed: boolean(),
          /** Initial stake amount in wei. */
          initial_wei: UnsignedInteger
        })
      }),
      object({
        /** Type of action. */
        type: literal("CValidatorAction"),
        /** Unregister the validator. */
        unregister: null_()
      })
    ]),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var CValidatorActionActionSchema = (() => {
  return variant("type", CValidatorActionRequest.entries.action.options);
})();
function cValidatorAction(config, params, opts) {
  const action = parse2(CValidatorActionActionSchema, { type: "CValidatorAction", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/cWithdraw.js
var CWithdrawRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("cWithdraw"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Amount of wei to withdraw from staking balance (float * 1e8). */
      wei: pipe(UnsignedInteger, minValue(1)),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var CWithdrawActionSchema = (() => {
  return omit(object(CWithdrawRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var CWithdrawTypes = {
  "HyperliquidTransaction:CWithdraw": [
    { name: "hyperliquidChain", type: "string" },
    { name: "wei", type: "uint64" },
    { name: "nonce", type: "uint64" }
  ]
};
function cWithdraw(config, params, opts) {
  const action = parse2(CWithdrawActionSchema, { type: "cWithdraw", ...params });
  return executeUserSignedAction(config, action, CWithdrawTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/evmUserModify.js
var EvmUserModifyRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("evmUserModify"),
      /** `true` for large blocks, `false` for small blocks. */
      usingBigBlocks: boolean()
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var EvmUserModifyActionSchema = (() => {
  return object(EvmUserModifyRequest.entries.action.entries);
})();
function evmUserModify(config, params, opts) {
  const action = parse2(EvmUserModifyActionSchema, { type: "evmUserModify", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/linkStakingUser.js
var LinkStakingUserRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("linkStakingUser"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /**
       * Target account address.
       * - Trading user initiating: enter staking account address.
       * - Staking user finalizing: enter trading account address.
       */
      user: Address,
      /**
       * Link phase.
       * - `false` = trading user initiates link request.
       * - `true` = staking user finalizes permanent link.
       */
      isFinalize: boolean(),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var LinkStakingUserActionSchema = (() => {
  return omit(object(LinkStakingUserRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var LinkStakingUserTypes = {
  "HyperliquidTransaction:LinkStakingUser": [
    { name: "hyperliquidChain", type: "string" },
    { name: "user", type: "address" },
    { name: "isFinalize", type: "bool" },
    { name: "nonce", type: "uint64" }
  ]
};
function linkStakingUser(config, params, opts) {
  const action = parse2(LinkStakingUserActionSchema, { type: "linkStakingUser", ...params });
  return executeUserSignedAction(config, action, LinkStakingUserTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/modify.js
var ModifyRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("modify"),
      /** Order ID or Client Order ID. */
      oid: union([UnsignedInteger, Cloid]),
      /** New order parameters. */
      order: object({
        /** Asset ID. */
        a: UnsignedInteger,
        /** Position side (`true` for long, `false` for short). */
        b: boolean(),
        /** Price. */
        p: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
        /** Size (in base currency units). */
        s: UnsignedDecimal,
        /** Is reduce-only? */
        r: boolean(),
        /** Order type (`limit` for limit orders, `trigger` for stop-loss/take-profit orders). */
        t: union([
          object({
            /** Limit order parameters. */
            limit: object({
              /**
               * Time-in-force.
               * - `"Gtc"`: Remains active until filled or canceled.
               * - `"Ioc"`: Fills immediately or cancels any unfilled portion.
               * - `"Alo"`: Adds liquidity only.
               * - `"FrontendMarket"`: Similar to Ioc, but add a note that this is market order.
               */
              tif: picklist(["Gtc", "Ioc", "Alo", "FrontendMarket"])
            })
          }),
          object({
            /** Trigger order parameters. */
            trigger: object({
              /** Is market order? */
              isMarket: boolean(),
              /** Trigger price. */
              triggerPx: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
              /** Indicates whether it is take profit or stop loss. */
              tpsl: picklist(["tp", "sl"])
            })
          })
        ]),
        /** Client Order ID. */
        c: optional(Cloid)
      })
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var ModifyActionSchema = (() => {
  return object(ModifyRequest.entries.action.entries);
})();
function modify(config, params, opts) {
  const action = parse2(ModifyActionSchema, { type: "modify", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/noop.js
var NoopRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("noop")
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var NoopActionSchema = (() => {
  return object(NoopRequest.entries.action.entries);
})();
function noop(config, opts) {
  const action = parse2(NoopActionSchema, { type: "noop" });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/order.js
var OrderRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("order"),
      /** Array of order parameters. */
      orders: array(object({
        /** Asset ID. */
        a: UnsignedInteger,
        /** Position side (`true` for long, `false` for short). */
        b: boolean(),
        /** Price. */
        p: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
        /** Size (in base currency units). */
        s: UnsignedDecimal,
        /** Is reduce-only? */
        r: boolean(),
        /** Order type (`limit` for limit orders, `trigger` for stop-loss/take-profit orders). */
        t: union([
          object({
            /** Limit order parameters. */
            limit: object({
              /**
               * Time-in-force.
               * - `"Gtc"`: Remains active until filled or canceled.
               * - `"Ioc"`: Fills immediately or cancels any unfilled portion.
               * - `"Alo"`: Adds liquidity only.
               * - `"FrontendMarket"`: Similar to Ioc, but add a note that this is market order.
               */
              tif: picklist(["Gtc", "Ioc", "Alo", "FrontendMarket"])
            })
          }),
          object({
            /** Trigger order parameters. */
            trigger: object({
              /** Is market order? */
              isMarket: boolean(),
              /** Trigger price. */
              triggerPx: pipe(UnsignedDecimal, check((input) => Number(input) > 0, "Value must be greater than zero")),
              /** Indicates whether it is take profit or stop loss. */
              tpsl: picklist(["tp", "sl"])
            })
          })
        ]),
        /** Client Order ID. */
        c: optional(Cloid)
      })),
      /**
       * Order grouping strategy:
       * - `na`: Standard order without grouping.
       * - `normalTpsl`: TP/SL order with fixed size that doesn't adjust with position changes.
       * - `positionTpsl`: TP/SL order that adjusts proportionally with the position size.
       */
      grouping: optional(picklist(["na", "normalTpsl", "positionTpsl"]), "na"),
      /** Builder fee. */
      builder: optional(object({
        /** Builder address. */
        b: Address,
        /** Builder fee in 0.1bps (1 = 0.0001%). Max 100 for perps (0.1%), 1000 for spot (1%). */
        f: pipe(UnsignedInteger, maxValue(1e3))
      }))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var OrderActionSchema = (() => {
  return object(OrderRequest.entries.action.entries);
})();
function order(config, params, opts) {
  const action = parse2(OrderActionSchema, { type: "order", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/perpDeploy.js
var PerpDeployRequest = (() => {
  return object({
    /** Action to perform. */
    action: variant("type", [
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for registering a new perpetual asset (v2). */
        registerAsset2: object({
          /** Max gas in native token wei. If not provided, then uses current deploy auction price. */
          maxGas: nullable(UnsignedInteger),
          /** Contains new asset listing parameters. */
          assetRequest: object({
            /** Coin symbol for the new asset. */
            coin: string(),
            /** Number of decimal places for size. */
            szDecimals: UnsignedInteger,
            /** Initial oracle price for the asset. */
            oraclePx: UnsignedDecimal,
            /** Margin table identifier for risk management. */
            marginTableId: UnsignedInteger,
            /** 'strictIsolated' does not allow withdrawing of isolated margin from open position. */
            marginMode: picklist(["strictIsolated", "noCross"])
          }),
          /** Name of the dex. */
          dex: string(),
          /** Contains new dex parameters. */
          schema: nullable(object({
            /** Full name of the dex. */
            fullName: string(),
            /** Collateral token index. */
            collateralToken: UnsignedInteger,
            /** User to update oracles. If not provided, then deployer is assumed to be oracle updater. */
            oracleUpdater: nullable(Address)
          }))
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for registering a new perpetual asset. */
        registerAsset: object({
          /** Max gas in native token wei. If not provided, then uses current deploy auction price. */
          maxGas: nullable(UnsignedInteger),
          /** Contains new asset listing parameters. */
          assetRequest: object({
            /** Coin symbol for the new asset. */
            coin: string(),
            /** Number of decimal places for size. */
            szDecimals: UnsignedInteger,
            /** Initial oracle price for the asset. */
            oraclePx: UnsignedDecimal,
            /** Margin table identifier for risk management. */
            marginTableId: UnsignedInteger,
            /** Whether the asset can only be traded with isolated margin. */
            onlyIsolated: boolean()
          }),
          /** Name of the dex. */
          dex: string(),
          /** Contains new dex parameters. */
          schema: nullable(object({
            /** Full name of the dex. */
            fullName: string(),
            /** Collateral token index. */
            collateralToken: UnsignedInteger,
            /** User to update oracles. If not provided, then deployer is assumed to be oracle updater. */
            oracleUpdater: nullable(Address)
          }))
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for setting oracle and mark prices for assets. */
        setOracle: object({
          /** Name of the dex. */
          dex: string(),
          /** A list (sorted by key) of asset and oracle prices. */
          oraclePxs: array(tuple([string(), UnsignedDecimal])),
          /** An outer list of inner lists (inner list sorted by key) of asset and mark prices. */
          markPxs: array(array(tuple([string(), UnsignedDecimal]))),
          /** A list (sorted by key) of asset and external prices which prevent sudden mark price deviations. */
          externalPerpPxs: array(tuple([string(), UnsignedDecimal]))
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and funding multiplier. */
        setFundingMultipliers: array(tuple([string(), UnsignedDecimal]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and 8-hour funding interest rate (between -0.01 and 0.01). */
        setFundingInterestRates: array(tuple([string(), Decimal]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for halting or resuming trading for an asset. */
        haltTrading: object({
          /** Coin symbol for the asset to halt or resume. */
          coin: string(),
          /** Whether trading should be halted (true) or resumed (false). */
          isHalted: boolean()
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and margin table ids. */
        setMarginTableIds: array(tuple([string(), UnsignedInteger]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for setting the fee recipient. */
        setFeeRecipient: object({
          /** Name of the DEX. */
          dex: string(),
          /** Address of the fee recipient. */
          feeRecipient: Address
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and open interest cap notionals. */
        setOpenInterestCaps: array(tuple([string(), UnsignedInteger]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A modification to sub-deployer permissions. */
        setSubDeployers: object({
          /** Name of the DEX. */
          dex: string(),
          /** A modification to sub-deployer permissions. */
          subDeployers: array(object({
            /** Corresponds to a variant of PerpDeployAction. */
            variant: string(),
            /** Sub-deployer address. */
            user: Address,
            /** Add or remove the subDeployer from the authorized set for the action variant. */
            allowed: boolean()
          }))
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and margin modes. */
        setMarginModes: array(tuple([string(), picklist(["strictIsolated", "noCross"])]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Set fee scale. */
        setFeeScale: object({
          /** Name of the dex. */
          dex: string(),
          /** Fee scale (between 0.0 and 3.0). */
          scale: UnsignedDecimal
        })
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** A list (sorted by key) of asset and growth modes. */
        setGrowthModes: array(tuple([string(), boolean()]))
      }),
      object({
        /** Type of action. */
        type: literal("perpDeploy"),
        /** Parameters for setting a perp annotation. */
        setPerpAnnotation: object({
          /** Coin symbol for the asset to annotate. */
          coin: string(),
          /** Classification label (max 15 characters). */
          category: string(),
          /** Detailed description (max 400 characters). */
          description: string(),
          /** Display name for frontends to use instead of the L1 name, or null to ignore. */
          displayName: nullable(string()),
          /** Keywords used as hints to match against searches. */
          keywords: array(string())
        })
      })
    ]),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var PerpDeployActionSchema = (() => {
  return variant("type", PerpDeployRequest.entries.action.options);
})();
function perpDeploy(config, params, opts) {
  const action = parse2(PerpDeployActionSchema, { type: "perpDeploy", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/registerReferrer.js
var RegisterReferrerRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("registerReferrer"),
      /** Referral code to create. */
      code: pipe(string(), minLength(1), maxLength(20))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var RegisterReferrerActionSchema = (() => {
  return object(RegisterReferrerRequest.entries.action.entries);
})();
function registerReferrer(config, params, opts) {
  const action = parse2(RegisterReferrerActionSchema, { type: "registerReferrer", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/reserveRequestWeight.js
var ReserveRequestWeightRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("reserveRequestWeight"),
      /** Amount of request weight to reserve. */
      weight: pipe(UnsignedInteger, maxValue(1844674407370955))
      // Truncated max uint64 / 1000
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var ReserveRequestWeightActionSchema = (() => {
  return object(ReserveRequestWeightRequest.entries.action.entries);
})();
function reserveRequestWeight(config, params, opts) {
  const action = parse2(ReserveRequestWeightActionSchema, { type: "reserveRequestWeight", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/scheduleCancel.js
var ScheduleCancelRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("scheduleCancel"),
      /**
       * Scheduled time (in ms since epoch).
       * Must be at least 5 seconds in the future.
       *
       * If not specified, will cause all scheduled cancel operations to be deleted.
       */
      time: optional(UnsignedInteger)
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var ScheduleCancelActionSchema = (() => {
  return object(ScheduleCancelRequest.entries.action.entries);
})();
function scheduleCancel(config, paramsOrOpts, maybeOpts) {
  const isFirstArgParams = paramsOrOpts && "time" in paramsOrOpts;
  const params = isFirstArgParams ? paramsOrOpts : {};
  const opts = isFirstArgParams ? maybeOpts : paramsOrOpts;
  const action = parse2(ScheduleCancelActionSchema, { type: "scheduleCancel", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/sendAsset.js
var SendAssetRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("sendAsset"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Destination address. */
      destination: Address,
      /** Source DEX ("" for default USDC perp DEX, "spot" for spot). */
      sourceDex: string(),
      /** Destination DEX ("" for default USDC perp DEX, "spot" for spot). */
      destinationDex: string(),
      /** Token identifier. */
      token: string(),
      /** Amount to send (not in wei). */
      amount: UnsignedDecimal,
      /** Source sub-account address ("" for main account). */
      fromSubAccount: optional(union([literal(""), Address]), ""),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var SendAssetActionSchema = (() => {
  return omit(object(SendAssetRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var SendAssetTypes = {
  "HyperliquidTransaction:SendAsset": [
    { name: "hyperliquidChain", type: "string" },
    { name: "destination", type: "string" },
    { name: "sourceDex", type: "string" },
    { name: "destinationDex", type: "string" },
    { name: "token", type: "string" },
    { name: "amount", type: "string" },
    { name: "fromSubAccount", type: "string" },
    { name: "nonce", type: "uint64" }
  ]
};
function sendAsset(config, params, opts) {
  const action = parse2(SendAssetActionSchema, { type: "sendAsset", ...params });
  return executeUserSignedAction(config, action, SendAssetTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/sendToEvmWithData.js
var SendToEvmWithDataRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("sendToEvmWithData"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Token identifier (e.g., "USDC"). */
      token: string(),
      /** Amount to send (not in wei). */
      amount: UnsignedDecimal,
      /** Source DEX name to transfer from. */
      sourceDex: string(),
      /** Recipient address in the specified encoding format. */
      destinationRecipient: string(),
      /** Address encoding format. */
      addressEncoding: picklist(["hex", "base58"]),
      /** Target blockchain chain ID. */
      destinationChainId: UnsignedInteger,
      /** Gas limit for execution on the destination chain. */
      gasLimit: UnsignedInteger,
      /** Additional data payload (hex-encoded bytes, "0x" for empty). */
      data: pipe(string(), regex(/^0[xX]([0-9a-fA-F]+)?$/)),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var SendToEvmWithDataActionSchema = (() => {
  return omit(object(SendToEvmWithDataRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var SendToEvmWithDataTypes = {
  "HyperliquidTransaction:SendToEvmWithData": [
    { name: "hyperliquidChain", type: "string" },
    { name: "token", type: "string" },
    { name: "amount", type: "string" },
    { name: "sourceDex", type: "string" },
    { name: "destinationRecipient", type: "string" },
    { name: "addressEncoding", type: "string" },
    { name: "destinationChainId", type: "uint32" },
    { name: "gasLimit", type: "uint64" },
    { name: "data", type: "bytes" },
    { name: "nonce", type: "uint64" }
  ]
};
function sendToEvmWithData(config, params, opts) {
  const action = parse2(SendToEvmWithDataActionSchema, { type: "sendToEvmWithData", ...params });
  return executeUserSignedAction(config, action, SendToEvmWithDataTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/setDisplayName.js
var SetDisplayNameRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("setDisplayName"),
      /**
       * Display name.
       * Set to an empty string to remove the display name.
       */
      displayName: pipe(string(), maxLength(20))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SetDisplayNameActionSchema = (() => {
  return object(SetDisplayNameRequest.entries.action.entries);
})();
function setDisplayName(config, params, opts) {
  const action = parse2(SetDisplayNameActionSchema, { type: "setDisplayName", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/setReferrer.js
var SetReferrerRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("setReferrer"),
      /** Referral code. */
      code: pipe(string(), minLength(1), maxLength(20))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SetReferrerActionSchema = (() => {
  return object(SetReferrerRequest.entries.action.entries);
})();
function setReferrer(config, params, opts) {
  const action = parse2(SetReferrerActionSchema, { type: "setReferrer", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/spotDeploy.js
var SpotDeployRequest = (() => {
  return object({
    /** Action to perform. */
    action: variant("type", [
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Register token parameters. */
        registerToken2: object({
          /** Token specifications. */
          spec: object({
            /** Token name. */
            name: string(),
            /** Number of decimals for token size. */
            szDecimals: UnsignedInteger,
            /** Number of decimals for token amounts in wei. */
            weiDecimals: UnsignedInteger
          }),
          /** Maximum gas allowed for registration. */
          maxGas: UnsignedInteger,
          /** Optional full token name. */
          fullName: optional(string())
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** User genesis parameters. */
        userGenesis: object({
          /** Token identifier. */
          token: UnsignedInteger,
          /** Array of tuples: [user address, genesis amount in wei]. */
          userAndWei: array(tuple([Address, UnsignedDecimal])),
          /** Array of tuples: [existing token identifier, genesis amount in wei]. */
          existingTokenAndWei: array(tuple([UnsignedInteger, UnsignedDecimal])),
          /** Array of tuples: [user address, blacklist status] (`true` for blacklist, `false` to remove existing blacklisted user). */
          blacklistUsers: optional(array(tuple([Address, boolean()])))
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Genesis parameters. */
        genesis: object({
          /** Token identifier. */
          token: UnsignedInteger,
          /** Maximum token supply. */
          maxSupply: UnsignedDecimal,
          /** Set hyperliquidity balance to 0. */
          noHyperliquidity: optional(literal(true))
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Register spot parameters. */
        registerSpot: object({
          /** Tuple containing base and quote token indices. */
          tokens: tuple([UnsignedInteger, UnsignedInteger])
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Register hyperliquidity parameters. */
        registerHyperliquidity: object({
          /** Spot index (distinct from base token index). */
          spot: UnsignedInteger,
          /** Starting price for liquidity seeding. */
          startPx: UnsignedDecimal,
          /** Order size as a float (not in wei). */
          orderSz: UnsignedDecimal,
          /** Total number of orders to place. */
          nOrders: UnsignedInteger,
          /** Number of levels to seed with USDC. */
          nSeededLevels: optional(UnsignedInteger)
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Set deployer trading fee share parameters. */
        setDeployerTradingFeeShare: object({
          /** Token identifier. */
          token: UnsignedInteger,
          /** The deployer trading fee share. Range is 0% to 100%. */
          share: Percent
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Enable quote token parameters. */
        enableQuoteToken: object({
          /** The token ID to convert to a quote token. */
          token: UnsignedInteger
        })
      }),
      object({
        /** Type of action. */
        type: literal("spotDeploy"),
        /** Enable aligned quote token parameters. */
        enableAlignedQuoteToken: object({
          /** Token identifier to enable as aligned quote token. */
          token: UnsignedInteger
        })
      })
    ]),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SpotDeployActionSchema = (() => {
  return variant("type", SpotDeployRequest.entries.action.options);
})();
function spotDeploy(config, params, opts) {
  const action = parse2(SpotDeployActionSchema, { type: "spotDeploy", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/spotSend.js
var SpotSendRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("spotSend"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Destination address. */
      destination: Address,
      /** Token identifier. */
      token: string(),
      /** Amount to send (not in wei). */
      amount: UnsignedDecimal,
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      time: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var SpotSendActionSchema = (() => {
  return omit(object(SpotSendRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "time"]);
})();
var SpotSendTypes = {
  "HyperliquidTransaction:SpotSend": [
    { name: "hyperliquidChain", type: "string" },
    { name: "destination", type: "string" },
    { name: "token", type: "string" },
    { name: "amount", type: "string" },
    { name: "time", type: "uint64" }
  ]
};
function spotSend(config, params, opts) {
  const action = parse2(SpotSendActionSchema, { type: "spotSend", ...params });
  return executeUserSignedAction(config, action, SpotSendTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/spotUser.js
var SpotUserRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("spotUser"),
      /** Spot dusting options. */
      toggleSpotDusting: object({
        /** Opt out of spot dusting. */
        optOut: boolean()
      })
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SpotUserActionSchema = (() => {
  return object(SpotUserRequest.entries.action.entries);
})();
function spotUser(config, params, opts) {
  const action = parse2(SpotUserActionSchema, { type: "spotUser", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/subAccountModify.js
var SubAccountModifyRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("subAccountModify"),
      /** Sub-account address to modify. */
      subAccountUser: Address,
      /** New sub-account name. */
      name: pipe(string(), minLength(1), maxLength(16))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SubAccountModifyActionSchema = (() => {
  return object(SubAccountModifyRequest.entries.action.entries);
})();
function subAccountModify(config, params, opts) {
  const action = parse2(SubAccountModifyActionSchema, { type: "subAccountModify", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/subAccountSpotTransfer.js
var SubAccountSpotTransferRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("subAccountSpotTransfer"),
      /** Sub-account address. */
      subAccountUser: Address,
      /** `true` for deposit, `false` for withdrawal. */
      isDeposit: boolean(),
      /** Token identifier. */
      token: string(),
      /** Amount to send (not in wei). */
      amount: UnsignedDecimal
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SubAccountSpotTransferActionSchema = (() => {
  return object(SubAccountSpotTransferRequest.entries.action.entries);
})();
function subAccountSpotTransfer(config, params, opts) {
  const action = parse2(SubAccountSpotTransferActionSchema, { type: "subAccountSpotTransfer", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/subAccountTransfer.js
var SubAccountTransferRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("subAccountTransfer"),
      /** Sub-account address. */
      subAccountUser: Address,
      /** `true` for deposit, `false` for withdrawal. */
      isDeposit: boolean(),
      /** Amount to transfer (float * 1e6). */
      usd: pipe(UnsignedInteger, minValue(1))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var SubAccountTransferActionSchema = (() => {
  return object(SubAccountTransferRequest.entries.action.entries);
})();
function subAccountTransfer(config, params, opts) {
  const action = parse2(SubAccountTransferActionSchema, { type: "subAccountTransfer", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/tokenDelegate.js
var TokenDelegateRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("tokenDelegate"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Validator address. */
      validator: Address,
      /** Amount for delegate/undelegate (float * 1e8). */
      wei: pipe(UnsignedInteger, minValue(1)),
      /** `true` for undelegate, `false` for delegate. */
      isUndelegate: boolean(),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var TokenDelegateActionSchema = (() => {
  return omit(object(TokenDelegateRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var TokenDelegateTypes = {
  "HyperliquidTransaction:TokenDelegate": [
    { name: "hyperliquidChain", type: "string" },
    { name: "validator", type: "address" },
    { name: "wei", type: "uint64" },
    { name: "isUndelegate", type: "bool" },
    { name: "nonce", type: "uint64" }
  ]
};
function tokenDelegate(config, params, opts) {
  const action = parse2(TokenDelegateActionSchema, { type: "tokenDelegate", ...params });
  return executeUserSignedAction(config, action, TokenDelegateTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/topUpIsolatedOnlyMargin.js
var TopUpIsolatedOnlyMarginRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("topUpIsolatedOnlyMargin"),
      /** Asset ID. */
      asset: UnsignedInteger,
      /** Target leverage (float string). */
      leverage: UnsignedDecimal
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var TopUpIsolatedOnlyMarginActionSchema = (() => {
  return object(TopUpIsolatedOnlyMarginRequest.entries.action.entries);
})();
function topUpIsolatedOnlyMargin(config, params, opts) {
  const action = parse2(TopUpIsolatedOnlyMarginActionSchema, { type: "topUpIsolatedOnlyMargin", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/twapCancel.js
var TwapCancelRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("twapCancel"),
      /** Asset ID. */
      a: UnsignedInteger,
      /** Twap ID. */
      t: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var TwapCancelActionSchema = (() => {
  return object(TwapCancelRequest.entries.action.entries);
})();
function twapCancel(config, params, opts) {
  const action = parse2(TwapCancelActionSchema, { type: "twapCancel", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/twapOrder.js
var TwapOrderRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("twapOrder"),
      /** Twap parameters. */
      twap: object({
        /** Asset ID. */
        a: UnsignedInteger,
        /** Position side (`true` for long, `false` for short). */
        b: boolean(),
        /** Size (in base currency units). */
        s: UnsignedDecimal,
        /** Is reduce-only? */
        r: boolean(),
        /** TWAP duration in minutes. */
        m: pipe(UnsignedInteger, minValue(5), maxValue(1440)),
        /** Enable random order timing. */
        t: boolean()
      })
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var TwapOrderActionSchema = (() => {
  return object(TwapOrderRequest.entries.action.entries);
})();
function twapOrder(config, params, opts) {
  const action = parse2(TwapOrderActionSchema, { type: "twapOrder", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/updateIsolatedMargin.js
var UpdateIsolatedMarginRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("updateIsolatedMargin"),
      /** Asset ID. */
      asset: UnsignedInteger,
      /** Position side (`true` for long, `false` for short). */
      isBuy: boolean(),
      /** Amount to adjust (float * 1e6). */
      ntli: Integer
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var UpdateIsolatedMarginActionSchema = (() => {
  return object(UpdateIsolatedMarginRequest.entries.action.entries);
})();
function updateIsolatedMargin(config, params, opts) {
  const action = parse2(UpdateIsolatedMarginActionSchema, { type: "updateIsolatedMargin", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/updateLeverage.js
var UpdateLeverageRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("updateLeverage"),
      /** Asset ID. */
      asset: UnsignedInteger,
      /** `true` for cross leverage, `false` for isolated leverage. */
      isCross: boolean(),
      /** New leverage value. */
      leverage: pipe(UnsignedInteger, minValue(1))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Vault address (for vault trading). */
    vaultAddress: optional(Address),
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var UpdateLeverageActionSchema = (() => {
  return object(UpdateLeverageRequest.entries.action.entries);
})();
function updateLeverage(config, params, opts) {
  const action = parse2(UpdateLeverageActionSchema, { type: "updateLeverage", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/usdClassTransfer.js
var UsdClassTransferRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("usdClassTransfer"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Amount to transfer (1 = $1). */
      amount: UnsignedDecimal,
      /** `true` for Spot to Perp, `false` for Perp to Spot. */
      toPerp: boolean(),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var UsdClassTransferActionSchema = (() => {
  return omit(object(UsdClassTransferRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var UsdClassTransferTypes = {
  "HyperliquidTransaction:UsdClassTransfer": [
    { name: "hyperliquidChain", type: "string" },
    { name: "amount", type: "string" },
    { name: "toPerp", type: "bool" },
    { name: "nonce", type: "uint64" }
  ]
};
function usdClassTransfer(config, params, opts) {
  const action = parse2(UsdClassTransferActionSchema, { type: "usdClassTransfer", ...params });
  return executeUserSignedAction(config, action, UsdClassTransferTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/usdSend.js
var UsdSendRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("usdSend"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Destination address. */
      destination: Address,
      /** Amount to send (1 = $1). */
      amount: UnsignedDecimal,
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      time: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var UsdSendActionSchema = (() => {
  return omit(object(UsdSendRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "time"]);
})();
var UsdSendTypes = {
  "HyperliquidTransaction:UsdSend": [
    { name: "hyperliquidChain", type: "string" },
    { name: "destination", type: "string" },
    { name: "amount", type: "string" },
    { name: "time", type: "uint64" }
  ]
};
function usdSend(config, params, opts) {
  const action = parse2(UsdSendActionSchema, { type: "usdSend", ...params });
  return executeUserSignedAction(config, action, UsdSendTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/userDexAbstraction.js
var UserDexAbstractionRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("userDexAbstraction"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** User address. */
      user: Address,
      /** Whether to enable or disable HIP-3 DEX abstraction. */
      enabled: boolean(),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var UserDexAbstractionActionSchema = (() => {
  return omit(object(UserDexAbstractionRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var UserDexAbstractionTypes = {
  "HyperliquidTransaction:UserDexAbstraction": [
    { name: "hyperliquidChain", type: "string" },
    { name: "user", type: "address" },
    { name: "enabled", type: "bool" },
    { name: "nonce", type: "uint64" }
  ]
};
function userDexAbstraction(config, params, opts) {
  const action = parse2(UserDexAbstractionActionSchema, { type: "userDexAbstraction", ...params });
  return executeUserSignedAction(config, action, UserDexAbstractionTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/userPortfolioMargin.js
var UserPortfolioMarginRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("userPortfolioMargin"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** User address. */
      user: Address,
      /** Whether to enable or disable user portfolio margin. */
      enabled: boolean(),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var UserPortfolioMarginActionSchema = (() => {
  return omit(object(UserPortfolioMarginRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var UserPortfolioMarginTypes = {
  "HyperliquidTransaction:UserPortfolioMargin": [
    { name: "hyperliquidChain", type: "string" },
    { name: "user", type: "address" },
    { name: "enabled", type: "bool" },
    { name: "nonce", type: "uint64" }
  ]
};
function userPortfolioMargin(config, params, opts) {
  const action = parse2(UserPortfolioMarginActionSchema, { type: "userPortfolioMargin", ...params });
  return executeUserSignedAction(config, action, UserPortfolioMarginTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/userSetAbstraction.js
var UserSetAbstractionRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("userSetAbstraction"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** User address. */
      user: Address,
      /** Abstraction mode to set. */
      abstraction: picklist(["dexAbstraction", "unifiedAccount", "portfolioMargin", "disabled"]),
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      nonce: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var UserSetAbstractionActionSchema = (() => {
  return omit(object(UserSetAbstractionRequest.entries.action.entries), ["signatureChainId", "hyperliquidChain", "nonce"]);
})();
var UserSetAbstractionTypes = {
  "HyperliquidTransaction:UserSetAbstraction": [
    { name: "hyperliquidChain", type: "string" },
    { name: "user", type: "address" },
    { name: "abstraction", type: "string" },
    { name: "nonce", type: "uint64" }
  ]
};
function userSetAbstraction(config, params, opts) {
  const action = parse2(UserSetAbstractionActionSchema, { type: "userSetAbstraction", ...params });
  return executeUserSignedAction(config, action, UserSetAbstractionTypes, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/validatorL1Stream.js
var ValidatorL1StreamRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("validatorL1Stream"),
      /** Risk-free rate as a decimal string (e.g., "0.05" for 5%). */
      riskFreeRate: UnsignedDecimal
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var ValidatorL1StreamActionSchema = (() => {
  return object(ValidatorL1StreamRequest.entries.action.entries);
})();
function validatorL1Stream(config, params, opts) {
  const action = parse2(ValidatorL1StreamActionSchema, { type: "validatorL1Stream", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/vaultDistribute.js
var VaultDistributeRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("vaultDistribute"),
      /** Vault address. */
      vaultAddress: Address,
      /**
       * Amount to distribute (float * 1e6).
       * Set to 0 to close the vault.
       */
      usd: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var VaultDistributeActionSchema = (() => {
  return object(VaultDistributeRequest.entries.action.entries);
})();
function vaultDistribute(config, params, opts) {
  const action = parse2(VaultDistributeActionSchema, { type: "vaultDistribute", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/vaultModify.js
var VaultModifyRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("vaultModify"),
      /** Vault address. */
      vaultAddress: Address,
      /** Allow deposits from followers. */
      allowDeposits: nullish(boolean(), null),
      /** Always close positions on withdrawal. */
      alwaysCloseOnWithdraw: nullish(boolean(), null)
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var VaultModifyActionSchema = (() => {
  return object(VaultModifyRequest.entries.action.entries);
})();
function vaultModify(config, params, opts) {
  const action = parse2(VaultModifyActionSchema, { type: "vaultModify", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/vaultTransfer.js
var VaultTransferRequest = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("vaultTransfer"),
      /** Vault address. */
      vaultAddress: Address,
      /** `true` for deposit, `false` for withdrawal. */
      isDeposit: boolean(),
      /** Amount for deposit/withdrawal (float * 1e6). */
      usd: pipe(UnsignedInteger, minValue(1))
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema,
    /** Expiration time of the action. */
    expiresAfter: optional(UnsignedInteger)
  });
})();
var VaultTransferActionSchema = (() => {
  return object(VaultTransferRequest.entries.action.entries);
})();
function vaultTransfer(config, params, opts) {
  const action = parse2(VaultTransferActionSchema, { type: "vaultTransfer", ...params });
  return executeL1Action(config, action, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/_methods/withdraw3.js
var Withdraw3Request = (() => {
  return object({
    /** Action to perform. */
    action: object({
      /** Type of action. */
      type: literal("withdraw3"),
      /** Chain ID in hex format for EIP-712 signing. */
      signatureChainId: Hex,
      /** HyperLiquid network type. */
      hyperliquidChain: HyperliquidChainSchema,
      /** Destination address. */
      destination: Address,
      /** Amount to withdraw (1 = $1). */
      amount: UnsignedDecimal,
      /** Nonce (timestamp in ms) used to prevent replay attacks. */
      time: UnsignedInteger
    }),
    /** Nonce (timestamp in ms) used to prevent replay attacks. */
    nonce: UnsignedInteger,
    /** ECDSA signature components. */
    signature: SignatureSchema
  });
})();
var Withdraw3ActionSchema = (() => {
  return omit(object(Withdraw3Request.entries.action.entries), ["signatureChainId", "hyperliquidChain", "time"]);
})();
var Withdraw3Types = {
  "HyperliquidTransaction:Withdraw": [
    { name: "hyperliquidChain", type: "string" },
    { name: "destination", type: "string" },
    { name: "amount", type: "string" },
    { name: "time", type: "uint64" }
  ]
};
function withdraw3(config, params, opts) {
  const action = parse2(Withdraw3ActionSchema, { type: "withdraw3", ...params });
  return executeUserSignedAction(config, action, Withdraw3Types, opts);
}

// node_modules/@nktkas/hyperliquid/esm/api/exchange/client.js
var ExchangeClient = class {
  config_;
  /**
   * Creates an instance of the ExchangeClient.
   *
   * @param config Configuration for Exchange API requests. See {@link ExchangeConfig}.
   *
   * @example [viem](https://viem.sh/docs/clients/wallet#local-accounts-private-key-mnemonic-etc)
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   *
   * const client = new hl.ExchangeClient({ transport, wallet });
   * ```
   *
   * @example [ethers.js](https://docs.ethers.org/v6/api/wallet/#Wallet)
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { ethers } from "npm:ethers";
   *
   * const wallet = new ethers.Wallet("0x...");
   * const transport = new hl.HttpTransport();
   *
   * const client = new hl.ExchangeClient({ transport, wallet });
   * ```
   *
   * @example Multi-sig
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   * import { ethers } from "npm:ethers";
   *
   * const signer1 = privateKeyToAccount("0x...");
   * const signer2 = new ethers.Wallet("0x...");
   * // ... and more signers
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   *
   * const client = new hl.ExchangeClient({
   *   transport,
   *   signers: [signer1, signer2],
   *   multiSigUser: "0x...",
   * });
   * ```
   */
  constructor(config) {
    this.config_ = config;
  }
  /**
   * Enable HIP-3 DEX abstraction.
   *
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.agentEnableDexAbstraction();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#enable-hip-3-dex-abstraction-agent
   *
   * @deprecated Use {@link agentSetAbstraction} instead.
   */
  agentEnableDexAbstraction(opts) {
    return agentEnableDexAbstraction(this.config_, opts);
  }
  /**
   * Set User abstraction mode (method for agent wallet).
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.agentSetAbstraction({ abstraction: "u" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#set-user-abstraction-agent
   */
  agentSetAbstraction(params, opts) {
    return agentSetAbstraction(this.config_, params, opts);
  }
  /**
   * Approve an agent to sign on behalf of the master account.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example Basic usage
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.approveAgent({ agentAddress: "0x...", agentName: "myAgent" });
   * ```
   * @example With expiration timestamp
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const expirationTimestamp = Date.now() + 24 * 60 * 60 * 1000;
   * await client.approveAgent({
   *   agentAddress: "0x...",
   *   agentName: `myAgent valid_until ${expirationTimestamp}`,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#approve-an-api-wallet
   */
  approveAgent(params, opts) {
    return approveAgent(this.config_, params, opts);
  }
  /**
   * Approve a maximum fee rate for a builder.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.approveBuilderFee({ maxFeeRate: "0.01%", builder: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#approve-a-builder-fee
   */
  approveBuilderFee(params, opts) {
    return approveBuilderFee(this.config_, params, opts);
  }
  /**
   * Modify multiple orders.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link OrderResponse} without error statuses.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const data = await client.batchModify({
   *   modifies: [
   *     {
   *       oid: 123,
   *       order: {
   *         a: 0,
   *         b: true,
   *         p: "31000",
   *         s: "0.2",
   *         r: false,
   *         t: { limit: { tif: "Gtc" } },
   *       },
   *     },
   *   ],
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#modify-multiple-orders
   */
  batchModify(params, opts) {
    return batchModify(this.config_, params, opts);
  }
  /**
   * Borrow or lend assets.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.borrowLend({ operation: "supply", token: 0, amount: "20" });
   * ```
   *
   * @see null
   */
  borrowLend(params, opts) {
    return borrowLend(this.config_, params, opts);
  }
  /**
   * Cancel order(s).
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link CancelResponse} without error statuses.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cancel({ cancels: [{ a: 0, o: 123 }] });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#cancel-order-s
   */
  cancel(params, opts) {
    return cancel(this.config_, params, opts);
  }
  /**
   * Cancel order(s) by cloid.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link CancelResponse} without error statuses.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cancelByCloid({
   *   cancels: [
   *     { asset: 0, cloid: "0x..." },
   *   ],
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#cancel-order-s-by-cloid
   */
  cancelByCloid(params, opts) {
    return cancelByCloid(this.config_, params, opts);
  }
  /**
   * Transfer native token from the user spot account into staking for delegating to validators.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cDeposit({ wei: 1 * 1e8 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#deposit-into-staking
   */
  cDeposit(params, opts) {
    return cDeposit(this.config_, params, opts);
  }
  /**
   * Claim rewards from referral program.
   *
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.claimRewards();
   * ```
   *
   * @see null
   */
  claimRewards(opts) {
    return claimRewards(this.config_, opts);
  }
  /**
   * Convert a single-signature account to a multi-signature account or vice versa.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example Convert to multi-sig
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.convertToMultiSigUser({
   *   signers: {
   *     authorizedUsers: ["0x...", "0x...", "0x..."],
   *     threshold: 2,
   *   },
   * });
   * ```
   *
   * @example Convert to single-sig
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.convertToMultiSigUser({ signers: null });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/hypercore/multi-sig
   */
  convertToMultiSigUser(params, opts) {
    return convertToMultiSigUser(this.config_, params, opts);
  }
  /**
   * Create a sub-account.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Response for creating a sub-account.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const data = await client.createSubAccount({ name: "..." });
   * ```
   *
   * @see null
   */
  createSubAccount(params, opts) {
    return createSubAccount(this.config_, params, opts);
  }
  /**
   * Create a vault.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Response for creating a vault.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const data = await client.createVault({
   *   name: "...",
   *   description: "...",
   *   initialUsd: 100 * 1e6,
   *   nonce: Date.now(),
   * });
   * ```
   *
   * @see null
   */
  createVault(params, opts) {
    return createVault(this.config_, params, opts);
  }
  /**
   * Jail or unjail self as a validator signer.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example Jail self
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cSignerAction({ jailSelf: null });
   * ```
   *
   * @example Unjail self
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cSignerAction({ unjailSelf: null });
   * ```
   *
   * @see null
   */
  cSignerAction(params, opts) {
    return cSignerAction(this.config_, params, opts);
  }
  /**
   * Action related to validator management.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cValidatorAction({
   *   changeProfile: {
   *     node_ip: { Ip: "1.2.3.4" },
   *     name: "...",
   *     description: "...",
   *     unjailed: true,
   *     disable_delegations: false,
   *     commission_bps: null,
   *     signer: null,
   *   },
   * });
   * ```
   *
   * @see null
   */
  cValidatorAction(params, opts) {
    return cValidatorAction(this.config_, params, opts);
  }
  /**
   * Transfer native token from staking into the user's spot account.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.cWithdraw({ wei: 1 * 1e8 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#withdraw-from-staking
   */
  cWithdraw(params, opts) {
    return cWithdraw(this.config_, params, opts);
  }
  /**
   * Configure block type for EVM transactions.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.evmUserModify({ usingBigBlocks: true });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/hyperevm/dual-block-architecture
   */
  evmUserModify(params, opts) {
    return evmUserModify(this.config_, params, opts);
  }
  /**
   * Link staking and trading accounts for fee discount attribution.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.linkStakingUser({ user: "0x...", isFinalize: false });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/trading/fees#staking-linking
   */
  linkStakingUser(params, opts) {
    return linkStakingUser(this.config_, params, opts);
  }
  /**
   * Modify an order.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.modify({
   *   oid: 123,
   *   order: {
   *     a: 0,
   *     b: true,
   *     p: "31000",
   *     s: "0.2",
   *     r: false,
   *     t: { limit: { tif: "Gtc" } },
   *   },
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#modify-an-order
   */
  modify(params, opts) {
    return modify(this.config_, params, opts);
  }
  /**
   * Place an order(s).
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link OrderResponse} without error statuses.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const data = await client.order({
   *   orders: [
   *     {
   *       a: 0,
   *       b: true,
   *       p: "30000",
   *       s: "0.1",
   *       r: false,
   *       t: { limit: { tif: "Gtc" } },
   *     },
   *   ],
   *   grouping: "na",
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#place-an-order
   */
  order(params, opts) {
    return order(this.config_, params, opts);
  }
  /**
   * This action does not do anything (no operation), but causes the nonce to be marked as used.
   *
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.noop();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#invalidate-pending-nonce-noop
   */
  noop(opts) {
    return noop(this.config_, opts);
  }
  /**
   * Deploying HIP-3 assets.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.perpDeploy({
   *   registerAsset: {
   *     maxGas: 1000000,
   *     assetRequest: {
   *       coin: "USDC",
   *       szDecimals: 8,
   *       oraclePx: "1",
   *       marginTableId: 1,
   *       onlyIsolated: false,
   *     },
   *     dex: "test",
   *     schema: null,
   *   },
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/hip-3-deployer-actions
   */
  perpDeploy(params, opts) {
    return perpDeploy(this.config_, params, opts);
  }
  /**
   * Create a referral code.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.registerReferrer({ code: "..." });
   * ```
   *
   * @see null
   */
  registerReferrer(params, opts) {
    return registerReferrer(this.config_, params, opts);
  }
  /**
   * Reserve additional rate-limited actions for a fee.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.reserveRequestWeight({ weight: 10 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#reserve-additional-actions
   */
  reserveRequestWeight(params, opts) {
    return reserveRequestWeight(this.config_, params, opts);
  }
  scheduleCancel(paramsOrOpts, maybeOpts) {
    const isFirstArgParams = paramsOrOpts && "time" in paramsOrOpts;
    const params = isFirstArgParams ? paramsOrOpts : {};
    const opts = isFirstArgParams ? maybeOpts : paramsOrOpts;
    return scheduleCancel(this.config_, params, opts);
  }
  /**
   * Transfer tokens between different perp DEXs, spot balance, users, and/or sub-accounts.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.sendAsset({
   *   destination: "0x0000000000000000000000000000000000000001",
   *   sourceDex: "",
   *   destinationDex: "test",
   *   token: "USDC:0xeb62eee3685fc4c43992febcd9e75443",
   *   amount: "1",
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#send-asset
   */
  sendAsset(params, opts) {
    return sendAsset(this.config_, params, opts);
  }
  /**
   * Transfer tokens from Core to EVM with an additional data payload for `ICoreReceiveWithData` contracts.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.sendToEvmWithData({
   *   token: "USDC",
   *   amount: "1",
   *   sourceDex: "spot",
   *   destinationRecipient: "0x...",
   *   addressEncoding: "hex",
   *   destinationChainId: 42161,
   *   gasLimit: 200000,
   *   data: "0x",
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#send-to-evm-with-data
   */
  sendToEvmWithData(params, opts) {
    return sendToEvmWithData(this.config_, params, opts);
  }
  /**
   * Set the display name in the leaderboard.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.setDisplayName({ displayName: "..." });
   * ```
   *
   * @see null
   */
  setDisplayName(params, opts) {
    return setDisplayName(this.config_, params, opts);
  }
  /**
   * Set a referral code.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.setReferrer({ code: "..." });
   * ```
   *
   * @see null
   */
  setReferrer(params, opts) {
    return setReferrer(this.config_, params, opts);
  }
  /**
   * Deploying HIP-1 and HIP-2 assets.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.spotDeploy({
   *   registerToken2: {
   *     spec: {
   *       name: "USDC",
   *       szDecimals: 8,
   *       weiDecimals: 8,
   *     },
   *     maxGas: 1000000,
   *     fullName: "USD Coin",
   *   },
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/deploying-hip-1-and-hip-2-assets
   */
  spotDeploy(params, opts) {
    return spotDeploy(this.config_, params, opts);
  }
  /**
   * Send spot assets to another address.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.spotSend({
   *   destination: "0x...",
   *   token: "USDC:0xeb62eee3685fc4c43992febcd9e75443",
   *   amount: "1",
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#core-spot-transfer
   */
  spotSend(params, opts) {
    return spotSend(this.config_, params, opts);
  }
  /**
   * Opt Out of Spot Dusting.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.spotUser({ toggleSpotDusting: { optOut: false } });
   * ```
   *
   * @see null
   */
  spotUser(params, opts) {
    return spotUser(this.config_, params, opts);
  }
  /**
   * Modify a sub-account.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.subAccountModify({ subAccountUser: "0x...", name: "..." });
   * ```
   *
   * @see null
   */
  subAccountModify(params, opts) {
    return subAccountModify(this.config_, params, opts);
  }
  /**
   * Transfer between sub-accounts (spot).
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.subAccountSpotTransfer({
   *   subAccountUser: "0x...",
   *   isDeposit: true,
   *   token: "USDC:0xeb62eee3685fc4c43992febcd9e75443",
   *   amount: "1",
   * });
   * ```
   *
   * @see null
   */
  subAccountSpotTransfer(params, opts) {
    return subAccountSpotTransfer(this.config_, params, opts);
  }
  /**
   * Transfer between sub-accounts (perpetual).
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.subAccountTransfer({
   *   subAccountUser: "0x...",
   *   isDeposit: true,
   *   usd: 1 * 1e6,
   * });
   * ```
   *
   * @see null
   */
  subAccountTransfer(params, opts) {
    return subAccountTransfer(this.config_, params, opts);
  }
  /**
   * Delegate or undelegate native tokens to or from a validator.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.tokenDelegate({
   *   validator: "0x...",
   *   isUndelegate: true,
   *   wei: 1 * 1e8,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#delegate-or-undelegate-stake-from-validator
   */
  tokenDelegate(params, opts) {
    return tokenDelegate(this.config_, params, opts);
  }
  /**
   * Top up isolated margin by targeting a specific leverage.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.topUpIsolatedOnlyMargin({ asset: 0, leverage: "0.5" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#update-isolated-margin
   */
  topUpIsolatedOnlyMargin(params, opts) {
    return topUpIsolatedOnlyMargin(this.config_, params, opts);
  }
  /**
   * Cancel a TWAP order.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link TwapCancelResponse} without error status.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.twapCancel({ a: 0, t: 1 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#cancel-a-twap-order
   */
  twapCancel(params, opts) {
    return twapCancel(this.config_, params, opts);
  }
  /**
   * Place a TWAP order.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful variant of {@link TwapOrderResponse} without error status.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * const data = await client.twapOrder({
   *   twap: {
   *     a: 0,
   *     b: true,
   *     s: "1",
   *     r: false,
   *     m: 10,
   *     t: true,
   *   },
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#place-a-twap-order
   */
  twapOrder(params, opts) {
    return twapOrder(this.config_, params, opts);
  }
  /**
   * Add or remove margin from isolated position.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.updateIsolatedMargin({ asset: 0, isBuy: true, ntli: 1 * 1e6 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#update-isolated-margin
   */
  updateIsolatedMargin(params, opts) {
    return updateIsolatedMargin(this.config_, params, opts);
  }
  /**
   * Update cross or isolated leverage on a coin.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.updateLeverage({ asset: 0, isCross: true, leverage: 5 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#update-leverage
   */
  updateLeverage(params, opts) {
    return updateLeverage(this.config_, params, opts);
  }
  /**
   * Transfer funds between Spot account and Perp account.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.usdClassTransfer({ amount: "1", toPerp: true });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#transfer-from-spot-account-to-perp-account-and-vice-versa
   */
  usdClassTransfer(params, opts) {
    return usdClassTransfer(this.config_, params, opts);
  }
  /**
   * Send usd to another address.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.usdSend({ destination: "0x...", amount: "1" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#core-usdc-transfer
   */
  usdSend(params, opts) {
    return usdSend(this.config_, params, opts);
  }
  /**
   * Enable/disable HIP-3 DEX abstraction.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.userDexAbstraction({ user: "0x...", enabled: true });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#enable-hip-3-dex-abstraction
   *
   * @deprecated Use {@link userSetAbstraction} instead.
   */
  userDexAbstraction(params, opts) {
    return userDexAbstraction(this.config_, params, opts);
  }
  /**
   * Set user abstraction mode.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.userSetAbstraction({ user: "0x...", abstraction: "dexAbstraction" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#set-user-abstraction
   */
  userSetAbstraction(params, opts) {
    return userSetAbstraction(this.config_, params, opts);
  }
  /**
   * Enable/disable user portfolio margin.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.userPortfolioMargin({ user: "0x...", enabled: true });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/trading/portfolio-margin
   */
  userPortfolioMargin(params, opts) {
    return userPortfolioMargin(this.config_, params, opts);
  }
  /**
   * Validator vote on risk-free rate for aligned quote asset.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.validatorL1Stream({ riskFreeRate: "0.05" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#validator-vote-on-risk-free-rate-for-aligned-quote-asset
   */
  validatorL1Stream(params, opts) {
    return validatorL1Stream(this.config_, params, opts);
  }
  /**
   * Distribute funds from a vault between followers.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.vaultDistribute({ vaultAddress: "0x...", usd: 10 * 1e6 });
   * ```
   *
   * @see null
   */
  vaultDistribute(params, opts) {
    return vaultDistribute(this.config_, params, opts);
  }
  /**
   * Modify a vault's configuration.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.vaultModify({
   *   vaultAddress: "0x...",
   *   allowDeposits: true,
   *   alwaysCloseOnWithdraw: false,
   * });
   * ```
   *
   * @see null
   */
  vaultModify(params, opts) {
    return vaultModify(this.config_, params, opts);
  }
  /**
   * Deposit or withdraw from a vault.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.vaultTransfer({
   *   vaultAddress: "0x...",
   *   isDeposit: true,
   *   usd: 10 * 1e6,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#deposit-or-withdraw-from-a-vault
   */
  vaultTransfer(params, opts) {
    return vaultTransfer(this.config_, params, opts);
  }
  /**
   * Initiate a withdrawal request.
   *
   * @param params Parameters specific to the API request.
   * @param opts Request execution options.
   * @return Successful response without specific data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   * @throws {ApiRequestError} When the API returns an unsuccessful response.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   * import { privateKeyToAccount } from "npm:viem/accounts";
   *
   * const wallet = privateKeyToAccount("0x..."); // viem or ethers
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.ExchangeClient({ transport, wallet });
   *
   * await client.withdraw3({ destination: "0x...", amount: "1" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint#initiate-a-withdrawal-request
   */
  withdraw3(params, opts) {
    return withdraw3(this.config_, params, opts);
  }
};

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/activeAssetData.js
var ActiveAssetDataRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("activeAssetData"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** User address. */
    user: Address
  });
})();
function activeAssetData(config, params, signal) {
  const request = parse2(ActiveAssetDataRequest, {
    type: "activeAssetData",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/alignedQuoteTokenInfo.js
var AlignedQuoteTokenInfoRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("alignedQuoteTokenInfo"),
    /** Token index. */
    token: UnsignedInteger
  });
})();
function alignedQuoteTokenInfo(config, params, signal) {
  const request = parse2(AlignedQuoteTokenInfoRequest, {
    type: "alignedQuoteTokenInfo",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/allBorrowLendReserveStates.js
var AllBorrowLendReserveStatesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("allBorrowLendReserveStates")
  });
})();
function allBorrowLendReserveStates(config, signal) {
  const request = parse2(AllBorrowLendReserveStatesRequest, {
    type: "allBorrowLendReserveStates"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/allMids.js
var AllMidsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("allMids"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function allMids(config, paramsOrSignal, maybeSignal) {
  const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
  const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
  const request = parse2(AllMidsRequest, {
    type: "allMids",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/allPerpMetas.js
var AllPerpMetasRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("allPerpMetas")
  });
})();
function allPerpMetas(config, signal) {
  const request = parse2(AllPerpMetasRequest, {
    type: "allPerpMetas"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/approvedBuilders.js
var ApprovedBuildersRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("approvedBuilders"),
    /** User address. */
    user: Address
  });
})();
function approvedBuilders(config, params, signal) {
  const request = parse2(ApprovedBuildersRequest, {
    type: "approvedBuilders",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/blockDetails.js
var BlockDetailsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("blockDetails"),
    /** Block height. */
    height: UnsignedInteger
  });
})();
function blockDetails(config, params, signal) {
  const request = parse2(BlockDetailsRequest, {
    type: "blockDetails",
    ...params
  });
  return config.transport.request("explorer", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/borrowLendReserveState.js
var BorrowLendReserveStateRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("borrowLendReserveState"),
    /** Token index. */
    token: UnsignedInteger
  });
})();
function borrowLendReserveState(config, params, signal) {
  const request = parse2(BorrowLendReserveStateRequest, {
    type: "borrowLendReserveState",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/borrowLendUserState.js
var BorrowLendUserStateRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("borrowLendUserState"),
    /** User address. */
    user: Address
  });
})();
function borrowLendUserState(config, params, signal) {
  const request = parse2(BorrowLendUserStateRequest, {
    type: "borrowLendUserState",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/candleSnapshot.js
var CandleSnapshotRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("candleSnapshot"),
    /** Request parameters. */
    req: object({
      /** Asset symbol (e.g., BTC). */
      coin: string(),
      /** Time interval. */
      interval: picklist(["1m", "3m", "5m", "15m", "30m", "1h", "2h", "4h", "8h", "12h", "1d", "3d", "1w", "1M"]),
      /** Start time (in ms since epoch). */
      startTime: UnsignedInteger,
      /** End time (in ms since epoch). */
      endTime: nullish(UnsignedInteger)
    })
  });
})();
function candleSnapshot(config, params, signal) {
  const request = parse2(CandleSnapshotRequest, {
    type: "candleSnapshot",
    req: params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/clearinghouseState.js
var ClearinghouseStateRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("clearinghouseState"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function clearinghouseState(config, params, signal) {
  const request = parse2(ClearinghouseStateRequest, {
    type: "clearinghouseState",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/delegations.js
var DelegationsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("delegations"),
    /** User address. */
    user: Address
  });
})();
function delegations(config, params, signal) {
  const request = parse2(DelegationsRequest, {
    type: "delegations",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/delegatorHistory.js
var DelegatorHistoryRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("delegatorHistory"),
    /** User address. */
    user: Address
  });
})();
function delegatorHistory(config, params, signal) {
  const request = parse2(DelegatorHistoryRequest, {
    type: "delegatorHistory",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/delegatorRewards.js
var DelegatorRewardsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("delegatorRewards"),
    /** User address. */
    user: Address
  });
})();
function delegatorRewards(config, params, signal) {
  const request = parse2(DelegatorRewardsRequest, {
    type: "delegatorRewards",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/delegatorSummary.js
var DelegatorSummaryRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("delegatorSummary"),
    /** User address. */
    user: Address
  });
})();
function delegatorSummary(config, params, signal) {
  const request = parse2(DelegatorSummaryRequest, {
    type: "delegatorSummary",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/exchangeStatus.js
var ExchangeStatusRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("exchangeStatus")
  });
})();
function exchangeStatus(config, signal) {
  const request = parse2(ExchangeStatusRequest, {
    type: "exchangeStatus"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/extraAgents.js
var ExtraAgentsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("extraAgents"),
    /** User address. */
    user: Address
  });
})();
function extraAgents(config, params, signal) {
  const request = parse2(ExtraAgentsRequest, {
    type: "extraAgents",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/frontendOpenOrders.js
var FrontendOpenOrdersRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("frontendOpenOrders"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function frontendOpenOrders(config, params, signal) {
  const request = parse2(FrontendOpenOrdersRequest, {
    type: "frontendOpenOrders",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/fundingHistory.js
var FundingHistoryRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("fundingHistory"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** Start time (in ms since epoch). */
    startTime: UnsignedInteger,
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger)
  });
})();
function fundingHistory(config, params, signal) {
  const request = parse2(FundingHistoryRequest, {
    type: "fundingHistory",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/gossipRootIps.js
var GossipRootIpsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("gossipRootIps")
  });
})();
function gossipRootIps(config, signal) {
  const request = parse2(GossipRootIpsRequest, {
    type: "gossipRootIps"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/historicalOrders.js
var HistoricalOrdersRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("historicalOrders"),
    /** User address. */
    user: Address
  });
})();
function historicalOrders(config, params, signal) {
  const request = parse2(HistoricalOrdersRequest, {
    type: "historicalOrders",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/isVip.js
var IsVipRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("isVip"),
    /** User address. */
    user: Address
  });
})();
function isVip(config, params, signal) {
  const request = parse2(IsVipRequest, {
    type: "isVip",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/l2Book.js
var L2BookRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("l2Book"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** Number of significant figures. */
    nSigFigs: nullish(picklist([2, 3, 4, 5])),
    /** Mantissa for aggregation (if `nSigFigs` is 5). */
    mantissa: nullish(picklist([2, 5]))
  });
})();
function l2Book(config, params, signal) {
  const request = parse2(L2BookRequest, {
    type: "l2Book",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/leadingVaults.js
var LeadingVaultsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("leadingVaults"),
    /** User address. */
    user: Address
  });
})();
function leadingVaults(config, params, signal) {
  const request = parse2(LeadingVaultsRequest, {
    type: "leadingVaults",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/legalCheck.js
var LegalCheckRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("legalCheck"),
    /** User address. */
    user: Address
  });
})();
function legalCheck(config, params, signal) {
  const request = parse2(LegalCheckRequest, {
    type: "legalCheck",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/liquidatable.js
var LiquidatableRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("liquidatable")
  });
})();
function liquidatable(config, signal) {
  const request = parse2(LiquidatableRequest, {
    type: "liquidatable"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/marginTable.js
var MarginTableRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("marginTable"),
    /** Margin requirements table. */
    id: UnsignedInteger
  });
})();
function marginTable(config, params, signal) {
  const request = parse2(MarginTableRequest, {
    type: "marginTable",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/maxBuilderFee.js
var MaxBuilderFeeRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("maxBuilderFee"),
    /** User address. */
    user: Address,
    /** Builder address. */
    builder: Address
  });
})();
function maxBuilderFee(config, params, signal) {
  const request = parse2(MaxBuilderFeeRequest, {
    type: "maxBuilderFee",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/maxMarketOrderNtls.js
var MaxMarketOrderNtlsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("maxMarketOrderNtls")
  });
})();
function maxMarketOrderNtls(config, signal) {
  const request = parse2(MaxMarketOrderNtlsRequest, {
    type: "maxMarketOrderNtls"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/meta.js
var MetaRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("meta"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function meta(config, paramsOrSignal, maybeSignal) {
  const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
  const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
  const request = parse2(MetaRequest, {
    type: "meta",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/metaAndAssetCtxs.js
var MetaAndAssetCtxsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("metaAndAssetCtxs"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function metaAndAssetCtxs(config, paramsOrSignal, maybeSignal) {
  const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
  const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
  const request = parse2(MetaAndAssetCtxsRequest, {
    type: "metaAndAssetCtxs",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/openOrders.js
var OpenOrdersRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("openOrders"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function openOrders(config, params, signal) {
  const request = parse2(OpenOrdersRequest, {
    type: "openOrders",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/orderStatus.js
var OrderStatusRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("orderStatus"),
    /** User address. */
    user: Address,
    /** Order ID or Client Order ID. */
    oid: union([UnsignedInteger, Cloid])
  });
})();
function orderStatus(config, params, signal) {
  const request = parse2(OrderStatusRequest, {
    type: "orderStatus",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/outcomeMeta.js
var OutcomeMetaRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("outcomeMeta")
  });
})();
function outcomeMeta(config, signal) {
  const request = parse2(OutcomeMetaRequest, {
    type: "outcomeMeta"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpAnnotation.js
var PerpAnnotationRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpAnnotation"),
    /** Coin symbol for the perpetual asset. */
    coin: string()
  });
})();
function perpAnnotation(config, params, signal) {
  const request = parse2(PerpAnnotationRequest, {
    type: "perpAnnotation",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpCategories.js
var PerpCategoriesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpCategories")
  });
})();
function perpCategories(config, signal) {
  const request = parse2(PerpCategoriesRequest, {
    type: "perpCategories"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpConciseAnnotations.js
var PerpConciseAnnotationsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpConciseAnnotations")
  });
})();
function perpConciseAnnotations(config, signal) {
  const request = parse2(PerpConciseAnnotationsRequest, {
    type: "perpConciseAnnotations"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpDeployAuctionStatus.js
var PerpDeployAuctionStatusRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpDeployAuctionStatus")
  });
})();
function perpDeployAuctionStatus(config, signal) {
  const request = parse2(PerpDeployAuctionStatusRequest, {
    type: "perpDeployAuctionStatus"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpDexLimits.js
var PerpDexLimitsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpDexLimits"),
    /** DEX name (empty string for main dex). */
    dex: string()
  });
})();
function perpDexLimits(config, params, signal) {
  const request = parse2(PerpDexLimitsRequest, {
    type: "perpDexLimits",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpDexs.js
var PerpDexsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpDexs")
  });
})();
function perpDexs(config, signal) {
  const request = parse2(PerpDexsRequest, {
    type: "perpDexs"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpDexStatus.js
var PerpDexStatusRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpDexStatus"),
    /** Perp dex name of builder-deployed dex market. The empty string represents the first perp dex. */
    dex: string()
  });
})();
function perpDexStatus(config, params, signal) {
  const request = parse2(PerpDexStatusRequest, {
    type: "perpDexStatus",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/perpsAtOpenInterestCap.js
var PerpsAtOpenInterestCapRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("perpsAtOpenInterestCap"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function perpsAtOpenInterestCap(config, paramsOrSignal, maybeSignal) {
  const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
  const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
  const request = parse2(PerpsAtOpenInterestCapRequest, {
    type: "perpsAtOpenInterestCap",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/portfolio.js
var PortfolioRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("portfolio"),
    /** User address. */
    user: Address
  });
})();
function portfolio(config, params, signal) {
  const request = parse2(PortfolioRequest, {
    type: "portfolio",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/predictedFundings.js
var PredictedFundingsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("predictedFundings")
  });
})();
function predictedFundings(config, signal) {
  const request = parse2(PredictedFundingsRequest, {
    type: "predictedFundings"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/preTransferCheck.js
var PreTransferCheckRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("preTransferCheck"),
    /** User address. */
    user: Address,
    /** Source address. */
    source: Address
  });
})();
function preTransferCheck(config, params, signal) {
  const request = parse2(PreTransferCheckRequest, {
    type: "preTransferCheck",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/recentTrades.js
var RecentTradesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("recentTrades"),
    /** Asset symbol (e.g., BTC). */
    coin: string()
  });
})();
function recentTrades(config, params, signal) {
  const request = parse2(RecentTradesRequest, {
    type: "recentTrades",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/referral.js
var ReferralRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("referral"),
    /** User address. */
    user: Address
  });
})();
function referral(config, params, signal) {
  const request = parse2(ReferralRequest, {
    type: "referral",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/spotClearinghouseState.js
var SpotClearinghouseStateRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("spotClearinghouseState"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function spotClearinghouseState(config, params, signal) {
  const request = parse2(SpotClearinghouseStateRequest, {
    type: "spotClearinghouseState",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/spotDeployState.js
var SpotDeployStateRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("spotDeployState"),
    /** User address. */
    user: Address
  });
})();
function spotDeployState(config, params, signal) {
  const request = parse2(SpotDeployStateRequest, {
    type: "spotDeployState",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/spotMeta.js
var SpotMetaRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("spotMeta")
  });
})();
function spotMeta(config, signal) {
  const request = parse2(SpotMetaRequest, {
    type: "spotMeta"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/spotMetaAndAssetCtxs.js
var SpotMetaAndAssetCtxsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("spotMetaAndAssetCtxs")
  });
})();
function spotMetaAndAssetCtxs(config, signal) {
  const request = parse2(SpotMetaAndAssetCtxsRequest, {
    type: "spotMetaAndAssetCtxs"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/spotPairDeployAuctionStatus.js
var SpotPairDeployAuctionStatusRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("spotPairDeployAuctionStatus")
  });
})();
function spotPairDeployAuctionStatus(config, signal) {
  const request = parse2(SpotPairDeployAuctionStatusRequest, {
    type: "spotPairDeployAuctionStatus"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/subAccounts.js
var SubAccountsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("subAccounts"),
    /** User address. */
    user: Address
  });
})();
function subAccounts(config, params, signal) {
  const request = parse2(SubAccountsRequest, {
    type: "subAccounts",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/subAccounts2.js
var SubAccounts2Request = (() => {
  return object({
    /** Type of request. */
    type: literal("subAccounts2"),
    /** User address. */
    user: Address
  });
})();
function subAccounts2(config, params, signal) {
  const request = parse2(SubAccounts2Request, {
    type: "subAccounts2",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/tokenDetails.js
var TokenDetailsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("tokenDetails"),
    /** Token ID. */
    tokenId: pipe(Hex, length(34))
  });
})();
function tokenDetails(config, params, signal) {
  const request = parse2(TokenDetailsRequest, {
    type: "tokenDetails",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/twapHistory.js
var TwapHistoryRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("twapHistory"),
    /** User address. */
    user: Address
  });
})();
function twapHistory(config, params, signal) {
  const request = parse2(TwapHistoryRequest, {
    type: "twapHistory",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/txDetails.js
var TxDetailsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("txDetails"),
    /** Transaction hash. */
    hash: pipe(Hex, length(66))
  });
})();
function txDetails(config, params, signal) {
  const request = parse2(TxDetailsRequest, {
    type: "txDetails",
    ...params
  });
  return config.transport.request("explorer", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userAbstraction.js
var UserAbstractionRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userAbstraction"),
    /** User address. */
    user: Address
  });
})();
function userAbstraction(config, params, signal) {
  const request = parse2(UserAbstractionRequest, {
    type: "userAbstraction",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userBorrowLendInterest.js
var UserBorrowLendInterestRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userBorrowLendInterest"),
    /** User address. */
    user: Address,
    /** Start time (in ms since epoch). */
    startTime: UnsignedInteger,
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger)
  });
})();
function userBorrowLendInterest(config, params, signal) {
  const request = parse2(UserBorrowLendInterestRequest, {
    type: "userBorrowLendInterest",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userDetails.js
var UserDetailsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userDetails"),
    /** User address. */
    user: Address
  });
})();
function userDetails(config, params, signal) {
  const request = parse2(UserDetailsRequest, {
    type: "userDetails",
    ...params
  });
  return config.transport.request("explorer", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userDexAbstraction.js
var UserDexAbstractionRequest2 = (() => {
  return object({
    /** Type of request. */
    type: literal("userDexAbstraction"),
    /** User address. */
    user: Address
  });
})();
function userDexAbstraction2(config, params, signal) {
  const request = parse2(UserDexAbstractionRequest2, {
    type: "userDexAbstraction",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userFees.js
var UserFeesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userFees"),
    /** User address. */
    user: Address
  });
})();
function userFees(config, params, signal) {
  const request = parse2(UserFeesRequest, {
    type: "userFees",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userFills.js
var UserFillsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userFills"),
    /** User address. */
    user: Address,
    /** If true, partial fills are aggregated when a crossing order fills multiple resting orders. */
    aggregateByTime: optional(boolean())
  });
})();
function userFills(config, params, signal) {
  const request = parse2(UserFillsRequest, {
    type: "userFills",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userFillsByTime.js
var UserFillsByTimeRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userFillsByTime"),
    /** User address. */
    user: Address,
    /** Start time (in ms since epoch). */
    startTime: UnsignedInteger,
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger),
    /** If true, partial fills are aggregated when a crossing order fills multiple resting orders. */
    aggregateByTime: optional(boolean()),
    /** If true, fills are returned in reverse chronological order (newest first). */
    reversed: optional(boolean())
  });
})();
function userFillsByTime(config, params, signal) {
  const request = parse2(UserFillsByTimeRequest, {
    type: "userFillsByTime",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userFunding.js
var UserFundingRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userFunding"),
    /** User address. */
    user: Address,
    /** Start time (in ms since epoch). */
    startTime: nullish(UnsignedInteger),
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger)
  });
})();
function userFunding(config, params, signal) {
  const request = parse2(UserFundingRequest, {
    type: "userFunding",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userNonFundingLedgerUpdates.js
var UserNonFundingLedgerUpdatesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userNonFundingLedgerUpdates"),
    /** User address. */
    user: Address,
    /** Start time (in ms since epoch). */
    startTime: optional(UnsignedInteger),
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger)
  });
})();
function userNonFundingLedgerUpdates(config, params, signal) {
  const request = parse2(UserNonFundingLedgerUpdatesRequest, {
    type: "userNonFundingLedgerUpdates",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userRateLimit.js
var UserRateLimitRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userRateLimit"),
    /** User address. */
    user: Address
  });
})();
function userRateLimit(config, params, signal) {
  const request = parse2(UserRateLimitRequest, {
    type: "userRateLimit",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userRole.js
var UserRoleRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userRole"),
    /** User address. */
    user: Address
  });
})();
function userRole(config, params, signal) {
  const request = parse2(UserRoleRequest, {
    type: "userRole",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userToMultiSigSigners.js
var UserToMultiSigSignersRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userToMultiSigSigners"),
    /** User address. */
    user: Address
  });
})();
function userToMultiSigSigners(config, params, signal) {
  const request = parse2(UserToMultiSigSignersRequest, {
    type: "userToMultiSigSigners",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userTwapSliceFills.js
var UserTwapSliceFillsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userTwapSliceFills"),
    /** User address. */
    user: Address
  });
})();
function userTwapSliceFills(config, params, signal) {
  const request = parse2(UserTwapSliceFillsRequest, {
    type: "userTwapSliceFills",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userTwapSliceFillsByTime.js
var UserTwapSliceFillsByTimeRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userTwapSliceFillsByTime"),
    /** User address. */
    user: Address,
    /** Start time (in ms since epoch). */
    startTime: UnsignedInteger,
    /** End time (in ms since epoch). */
    endTime: nullish(UnsignedInteger),
    /** If true, partial fills are aggregated when a crossing order fills multiple resting orders. */
    aggregateByTime: optional(boolean())
  });
})();
function userTwapSliceFillsByTime(config, params, signal) {
  const request = parse2(UserTwapSliceFillsByTimeRequest, {
    type: "userTwapSliceFillsByTime",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/userVaultEquities.js
var UserVaultEquitiesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("userVaultEquities"),
    /** User address. */
    user: Address
  });
})();
function userVaultEquities(config, params, signal) {
  const request = parse2(UserVaultEquitiesRequest, {
    type: "userVaultEquities",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/validatorL1Votes.js
var ValidatorL1VotesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("validatorL1Votes")
  });
})();
function validatorL1Votes(config, signal) {
  const request = parse2(ValidatorL1VotesRequest, {
    type: "validatorL1Votes"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/validatorSummaries.js
var ValidatorSummariesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("validatorSummaries")
  });
})();
function validatorSummaries(config, signal) {
  const request = parse2(ValidatorSummariesRequest, {
    type: "validatorSummaries"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/vaultDetails.js
var VaultDetailsRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("vaultDetails"),
    /** Vault address. */
    vaultAddress: Address,
    /** User address. */
    user: nullish(Address)
  });
})();
function vaultDetails(config, params, signal) {
  const request = parse2(VaultDetailsRequest, {
    type: "vaultDetails",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/vaultSummaries.js
var VaultSummariesRequest = (() => {
  return object({
    /** Type of request. */
    type: literal("vaultSummaries")
  });
})();
function vaultSummaries(config, signal) {
  const request = parse2(VaultSummariesRequest, {
    type: "vaultSummaries"
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/_methods/webData2.js
var WebData2Request = (() => {
  return object({
    /** Type of request. */
    type: literal("webData2"),
    /** User address. */
    user: Address
  });
})();
function webData2(config, params, signal) {
  const request = parse2(WebData2Request, {
    type: "webData2",
    ...params
  });
  return config.transport.request("info", request, signal);
}

// node_modules/@nktkas/hyperliquid/esm/api/info/client.js
var InfoClient = class {
  config_;
  /**
   * Creates an instance of the InfoClient.
   *
   * @param config Configuration for Info API requests. See {@link InfoConfig}.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   *
   * const infoClient = new hl.InfoClient({ transport });
   * ```
   */
  constructor(config) {
    this.config_ = config;
  }
  /**
   * Request user active asset data.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User active asset data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.activeAssetData({ user: "0x...", coin: "ETH" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-users-active-asset-data
   */
  activeAssetData(params, signal) {
    return activeAssetData(this.config_, params, signal);
  }
  /**
   * Request supply, rate, and pending payment information for an aligned quote token.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Supply, rate, and pending payment information for an aligned quote token.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.alignedQuoteTokenInfo({ token: 1328 });
   * ```
   *
   * @see null
   */
  alignedQuoteTokenInfo(params, signal) {
    return alignedQuoteTokenInfo(this.config_, params, signal);
  }
  /**
   * Request all borrow/lend reserve states.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of tuples of reserve IDs and their borrow/lend reserve state.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.allBorrowLendReserveStates();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-all-borrow-lend-reserve-states
   */
  allBorrowLendReserveStates(signal) {
    return allBorrowLendReserveStates(this.config_, signal);
  }
  allMids(paramsOrSignal, maybeSignal) {
    const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
    const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
    return allMids(this.config_, params, signal);
  }
  /**
   * Request trading metadata for all DEXes.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Metadata for perpetual assets across all DEXes.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.allPerpMetas();
   * ```
   *
   * @see null
   */
  allPerpMetas(signal) {
    return allPerpMetas(this.config_, signal);
  }
  /**
   * Request approved builders for a user.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of approved builder addresses.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.approvedBuilders({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-approved-builders-for-user
   */
  approvedBuilders(params, signal) {
    return approvedBuilders(this.config_, params, signal);
  }
  /**
   * Request block details by block height.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Response containing block information.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.blockDetails({ height: 123 });
   * ```
   *
   * @see null
   */
  blockDetails(params, signal) {
    return blockDetails(this.config_, params, signal);
  }
  /**
   * Request borrow/lend reserve state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Borrow/lend reserve state.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.borrowLendReserveState({ token: 0 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-borrow-lend-reserve-state
   */
  borrowLendReserveState(params, signal) {
    return borrowLendReserveState(this.config_, params, signal);
  }
  /**
   * Request borrow/lend user state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User's borrow/lend state.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.borrowLendUserState({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-borrow-lend-user-state
   */
  borrowLendUserState(params, signal) {
    return borrowLendUserState(this.config_, params, signal);
  }
  /**
   * Request candlestick snapshots.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of candlestick data points.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.candleSnapshot({
   *   coin: "ETH",
   *   interval: "1h",
   *   startTime: Date.now() - 1000 * 60 * 60 * 24,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#candle-snapshot
   */
  candleSnapshot(params, signal) {
    return candleSnapshot(this.config_, params, signal);
  }
  /**
   * Request clearinghouse state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Account summary for perpetual trading.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.clearinghouseState({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-users-perpetuals-account-summary
   */
  clearinghouseState(params, signal) {
    return clearinghouseState(this.config_, params, signal);
  }
  /**
   * Request user staking delegations.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's delegations to validators.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.delegations({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-staking-delegations
   */
  delegations(params, signal) {
    return delegations(this.config_, params, signal);
  }
  /**
   * Request user staking history.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of records of staking events by a delegator.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.delegatorHistory({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-staking-history
   */
  delegatorHistory(params, signal) {
    return delegatorHistory(this.config_, params, signal);
  }
  /**
   * Request user staking rewards.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of rewards received from staking activities.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.delegatorRewards({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-staking-rewards
   */
  delegatorRewards(params, signal) {
    return delegatorRewards(this.config_, params, signal);
  }
  /**
   * Request user's staking summary.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User's staking summary.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.delegatorSummary({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-staking-summary
   */
  delegatorSummary(params, signal) {
    return delegatorSummary(this.config_, params, signal);
  }
  /**
   * Request exchange system status information.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Exchange system status information.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.exchangeStatus();
   * ```
   *
   * @see null
   */
  exchangeStatus(signal) {
    return exchangeStatus(this.config_, signal);
  }
  /**
   * Request user extra agents.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of extra agent details for a user.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.extraAgents({ user: "0x..." });
   * ```
   *
   * @see null
   */
  extraAgents(params, signal) {
    return extraAgents(this.config_, params, signal);
  }
  /**
   * Request frontend open orders.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of open orders with additional display information.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.frontendOpenOrders({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-open-orders-with-additional-frontend-info
   */
  frontendOpenOrders(params, signal) {
    return frontendOpenOrders(this.config_, params, signal);
  }
  /**
   * Request funding history.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of historical funding rate records for an asset.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.fundingHistory({
   *   coin: "ETH",
   *   startTime: Date.now() - 1000 * 60 * 60 * 24,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-historical-funding-rates
   */
  fundingHistory(params, signal) {
    return fundingHistory(this.config_, params, signal);
  }
  /**
   * Request gossip root IPs.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of gossip root IPs.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.gossipRootIps();
   * ```
   *
   * @see null
   */
  gossipRootIps(signal) {
    return gossipRootIps(this.config_, signal);
  }
  /**
   * Request user historical orders.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of frontend orders with current processing status.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.historicalOrders({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-historical-orders
   */
  historicalOrders(params, signal) {
    return historicalOrders(this.config_, params, signal);
  }
  /**
   * Request to check if a user is a VIP.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Boolean indicating user's VIP status.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.isVip({ user: "0x..." });
   * ```
   *
   * @see null
   */
  isVip(params, signal) {
    return isVip(this.config_, params, signal);
  }
  /**
   * Request L2 order book.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return L2 order book snapshot.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.l2Book({ coin: "ETH", nSigFigs: 2 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#l2-book-snapshot
   */
  l2Book(params, signal) {
    return l2Book(this.config_, params, signal);
  }
  /**
   * Request leading vaults for a user.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of leading vaults for a user.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.leadingVaults({ user: "0x..." });
   * ```
   *
   * @see null
   */
  leadingVaults(params, signal) {
    return leadingVaults(this.config_, params, signal);
  }
  /**
   * Request legal verification status of a user.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Legal verification status for a user.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.legalCheck({ user: "0x..." });
   * ```
   *
   * @see null
   */
  legalCheck(params, signal) {
    return legalCheck(this.config_, params, signal);
  }
  /**
   * Request liquidatable.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Unknown array.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.liquidatable();
   * ```
   *
   * @see null
   */
  liquidatable(signal) {
    return liquidatable(this.config_, signal);
  }
  /**
   * Request margin table data.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Margin requirements table with multiple tiers.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.marginTable({ id: 1 });
   * ```
   *
   * @see null
   */
  marginTable(params, signal) {
    return marginTable(this.config_, params, signal);
  }
  /**
   * Request builder fee approval.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Maximum builder fee approval.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.maxBuilderFee({ user: "0x...", builder: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#check-builder-fee-approval
   */
  maxBuilderFee(params, signal) {
    return maxBuilderFee(this.config_, params, signal);
  }
  /**
   * Request maximum market order notionals.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Maximum market order notionals.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.maxMarketOrderNtls();
   * ```
   *
   * @see null
   */
  maxMarketOrderNtls(signal) {
    return maxMarketOrderNtls(this.config_, signal);
  }
  meta(paramsOrSignal, maybeSignal) {
    const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
    const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
    return meta(this.config_, params, signal);
  }
  metaAndAssetCtxs(paramsOrSignal, maybeSignal) {
    const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
    const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
    return metaAndAssetCtxs(this.config_, params, signal);
  }
  /**
   * Request open orders.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of open orders.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.openOrders({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-open-orders
   */
  openOrders(params, signal) {
    return openOrders(this.config_, params, signal);
  }
  /**
   * Request order status.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Order status response.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.orderStatus({ user: "0x...", oid: 12345 });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-order-status-by-oid-or-cloid
   */
  orderStatus(params, signal) {
    return orderStatus(this.config_, params, signal);
  }
  /**
   * Request prediction market outcome metadata.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Prediction market outcome metadata including outcomes and questions.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.outcomeMeta();
   * ```
   *
   * @see null
   */
  outcomeMeta(signal) {
    return outcomeMeta(this.config_, signal);
  }
  /**
   * Request perp annotation.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Perp annotation for an asset.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpAnnotation({ coin: "BTC" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-perp-annotation
   */
  perpAnnotation(params, signal) {
    return perpAnnotation(this.config_, params, signal);
  }
  /**
   * Request all perpetual asset categories.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of tuples mapping coin names to their categories.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpCategories();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-perpetual-asset-categories
   */
  perpCategories(signal) {
    return perpCategories(this.config_, signal);
  }
  /**
   * Request concise annotations for all perpetual assets.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of tuples mapping coin names to their concise annotations.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpConciseAnnotations();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-concise-perp-annotations
   */
  perpConciseAnnotations(signal) {
    return perpConciseAnnotations(this.config_, signal);
  }
  /**
   * Request for the status of the perpetual deploy auction.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Status of the perpetual deploy auction.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpDeployAuctionStatus();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-information-about-the-perp-deploy-auction
   */
  perpDeployAuctionStatus(signal) {
    return perpDeployAuctionStatus(this.config_, signal);
  }
  /**
   * Request builder deployed perpetual market limits.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Builder deployed perpetual market limits.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpDexLimits({ dex: "test" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-builder-deployed-perp-market-limits
   */
  perpDexLimits(params, signal) {
    return perpDexLimits(this.config_, params, signal);
  }
  /**
   * Request all perpetual dexs.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of perpetual dexes (null is main dex).
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpDexs();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-all-perpetual-dexs
   */
  perpDexs(signal) {
    return perpDexs(this.config_, signal);
  }
  /**
   * Request perp DEX status.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Status of a perp DEX.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.perpDexStatus({ dex: "test" });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#get-perp-market-status
   */
  perpDexStatus(params, signal) {
    return perpDexStatus(this.config_, params, signal);
  }
  perpsAtOpenInterestCap(paramsOrSignal, maybeSignal) {
    const params = paramsOrSignal instanceof AbortSignal ? {} : paramsOrSignal;
    const signal = paramsOrSignal instanceof AbortSignal ? paramsOrSignal : maybeSignal;
    return perpsAtOpenInterestCap(this.config_, params, signal);
  }
  /**
   * Request user portfolio.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Portfolio metrics grouped by time periods.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.portfolio({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-portfolio
   */
  portfolio(params, signal) {
    return portfolio(this.config_, params, signal);
  }
  /**
   * Request predicted funding rates.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of predicted funding rates.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.predictedFundings();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-predicted-funding-rates-for-different-venues
   */
  predictedFundings(signal) {
    return predictedFundings(this.config_, signal);
  }
  /**
   * Request user existence check before transfer.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Pre-transfer user existence check result.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.preTransferCheck({ user: "0x...", source: "0x..." });
   * ```
   *
   * @see null
   */
  preTransferCheck(params, signal) {
    return preTransferCheck(this.config_, params, signal);
  }
  /**
   * Request recent trades.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of recent trades.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.recentTrades({ coin: "ETH" });
   * ```
   *
   * @see null
   */
  recentTrades(params, signal) {
    return recentTrades(this.config_, params, signal);
  }
  /**
   * Request user referral.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Referral details for a user.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.referral({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-referral-information
   */
  referral(params, signal) {
    return referral(this.config_, params, signal);
  }
  /**
   * Request spot clearinghouse state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Account summary for spot trading.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.spotClearinghouseState({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-a-users-token-balances
   */
  spotClearinghouseState(params, signal) {
    return spotClearinghouseState(this.config_, params, signal);
  }
  /**
   * Request spot deploy state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Deploy state for spot tokens.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.spotDeployState({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-information-about-the-spot-deploy-auction
   */
  spotDeployState(params, signal) {
    return spotDeployState(this.config_, params, signal);
  }
  /**
   * Request spot trading metadata.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Metadata for spot assets.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.spotMeta();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-spot-metadata
   */
  spotMeta(signal) {
    return spotMeta(this.config_, signal);
  }
  /**
   * Request spot metadata and asset contexts.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Metadata and context for spot assets.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.spotMetaAndAssetCtxs();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-spot-asset-contexts
   */
  spotMetaAndAssetCtxs(signal) {
    return spotMetaAndAssetCtxs(this.config_, signal);
  }
  /**
   * Request for the status of the spot deploy auction.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Status of the spot deploy auction.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.spotPairDeployAuctionStatus();
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-information-about-the-spot-pair-deploy-auction
   */
  spotPairDeployAuctionStatus(signal) {
    return spotPairDeployAuctionStatus(this.config_, signal);
  }
  /**
   * Request user sub-accounts.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user sub-account or null if the user does not have any sub-accounts.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.subAccounts({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-subaccounts
   */
  subAccounts(params, signal) {
    return subAccounts(this.config_, params, signal);
  }
  /**
   * Request user sub-accounts V2.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user sub-account or null if the user does not have any sub-accounts.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.subAccounts2({ user: "0x..." });
   * ```
   *
   * @see null
   */
  subAccounts2(params, signal) {
    return subAccounts2(this.config_, params, signal);
  }
  /**
   * Request token details.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Details of a token.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.tokenDetails({ tokenId: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot#retrieve-information-about-a-token
   */
  tokenDetails(params, signal) {
    return tokenDetails(this.config_, params, signal);
  }
  /**
   * Request TWAP history of a user.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's TWAP history.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.twapHistory({ user: "0x..." });
   * ```
   *
   * @see null
   */
  twapHistory(params, signal) {
    return twapHistory(this.config_, params, signal);
  }
  /**
   * Request transaction details by transaction hash.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Transaction details.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.txDetails({ hash: "0x..." });
   * ```
   *
   * @see null
   */
  txDetails(params, signal) {
    return txDetails(this.config_, params, signal);
  }
  /**
   * Request user abstraction state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User abstraction state.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userAbstraction({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-abstraction-state
   */
  userAbstraction(params, signal) {
    return userAbstraction(this.config_, params, signal);
  }
  /**
   * Request borrow/lend user interest.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User's borrow/lend interest.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userBorrowLendInterest({
   *   user: "0x...",
   *   startTime: Date.now() - 1000 * 60 * 60 * 24,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-borrow-lend-user-state
   */
  userBorrowLendInterest(params, signal) {
    return userBorrowLendInterest(this.config_, params, signal);
  }
  /**
   * Request array of user transaction details.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user transaction details.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userDetails({ user: "0x..." });
   * ```
   *
   * @see null
   */
  userDetails(params, signal) {
    return userDetails(this.config_, params, signal);
  }
  /**
   * Request user HIP-3 DEX abstraction state.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User HIP-3 DEX abstraction state.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userDexAbstraction({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-hip-3-dex-abstraction-state
   */
  userDexAbstraction(params, signal) {
    return userDexAbstraction2(this.config_, params, signal);
  }
  /**
   * Request user fees.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User fees.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userFees({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-fees
   */
  userFees(params, signal) {
    return userFees(this.config_, params, signal);
  }
  /**
   * Request array of user fills.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user trade fills.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userFills({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-fills
   */
  userFills(params, signal) {
    return userFills(this.config_, params, signal);
  }
  /**
   * Request array of user fills by time.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user trade fills by time.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userFillsByTime({
   *   user: "0x...",
   *   startTime: Date.now() - 1000 * 60 * 60 * 24,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-fills-by-time
   */
  userFillsByTime(params, signal) {
    return userFillsByTime(this.config_, params, signal);
  }
  /**
   * Request array of user funding ledger updates.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user funding ledger updates.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userFunding({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-a-users-funding-history-or-non-funding-ledger-updates
   */
  userFunding(params, signal) {
    return userFunding(this.config_, params, signal);
  }
  /**
   * Request user non-funding ledger updates.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's non-funding ledger update.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userNonFundingLedgerUpdates({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals#retrieve-a-users-funding-history-or-non-funding-ledger-updates
   */
  userNonFundingLedgerUpdates(params, signal) {
    return userNonFundingLedgerUpdates(this.config_, params, signal);
  }
  /**
   * Request user rate limits.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User rate limits.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userRateLimit({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-user-rate-limits
   */
  userRateLimit(params, signal) {
    return userRateLimit(this.config_, params, signal);
  }
  /**
   * Request user role.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return User role.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userRole({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-a-users-role
   */
  userRole(params, signal) {
    return userRole(this.config_, params, signal);
  }
  /**
   * Request multi-sig signers for a user.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Multi-sig signers for a user or null if the user does not have any multi-sig signers.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userToMultiSigSigners({ user: "0x..." });
   * ```
   *
   * @see null
   */
  userToMultiSigSigners(params, signal) {
    return userToMultiSigSigners(this.config_, params, signal);
  }
  /**
   * Request user TWAP slice fills.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's TWAP slice fills.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userTwapSliceFills({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-twap-slice-fills
   */
  userTwapSliceFills(params, signal) {
    return userTwapSliceFills(this.config_, params, signal);
  }
  /**
   * Request user TWAP slice fills by time.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's TWAP slice fill by time.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userTwapSliceFillsByTime({
   *   user: "0x...",
   *   startTime: Date.now() - 1000 * 60 * 60 * 24,
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-twap-slice-fills
   */
  userTwapSliceFillsByTime(params, signal) {
    return userTwapSliceFillsByTime(this.config_, params, signal);
  }
  /**
   * Request user vault deposits.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of user's vault deposits.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.userVaultEquities({ user: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-a-users-vault-deposits
   */
  userVaultEquities(params, signal) {
    return userVaultEquities(this.config_, params, signal);
  }
  /**
   * Request validator L1 votes.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of L1 governance votes cast by validators.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.validatorL1Votes();
   * ```
   *
   * @see null
   */
  validatorL1Votes(signal) {
    return validatorL1Votes(this.config_, signal);
  }
  /**
   * Request validator summaries.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of validator performance statistics.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.validatorSummaries();
   * ```
   *
   * @see null
   */
  validatorSummaries(signal) {
    return validatorSummaries(this.config_, signal);
  }
  /**
   * Request details of a vault.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Details of a vault or null if the vault does not exist.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.vaultDetails({ vaultAddress: "0x..." });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#retrieve-details-for-a-vault
   */
  vaultDetails(params, signal) {
    return vaultDetails(this.config_, params, signal);
  }
  /**
   * Request a list of vaults less than 2 hours old.
   *
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Array of vaults less than 2 hours old.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.vaultSummaries();
   * ```
   *
   * @see null
   */
  vaultSummaries(signal) {
    return vaultSummaries(this.config_, signal);
  }
  /**
   * Request comprehensive user and market data.
   *
   * @param params Parameters specific to the API request.
   * @param signal {@link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal | AbortSignal} to cancel the request.
   * @return Comprehensive user and market data.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.HttpTransport(); // or `WebSocketTransport`
   * const client = new hl.InfoClient({ transport });
   *
   * const data = await client.webData2({ user: "0x..." });
   * ```
   *
   * @see null
   */
  webData2(params, signal) {
    return webData2(this.config_, params, signal);
  }
};

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/activeAssetCtx.js
var ActiveAssetCtxRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("activeAssetCtx"),
    /** Asset symbol (e.g., BTC). */
    coin: string()
  });
})();
function activeAssetCtx(config, params, listener) {
  const payload = parse2(ActiveAssetCtxRequest, { type: "activeAssetCtx", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.coin === payload.coin) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/activeAssetData.js
var ActiveAssetDataRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("activeAssetData"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** User address. */
    user: Address
  });
})();
function activeAssetData2(config, params, listener) {
  const payload = parse2(ActiveAssetDataRequest2, { type: "activeAssetData", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.coin === payload.coin && e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/activeSpotAssetCtx.js
var ActiveSpotAssetCtxRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("activeAssetCtx"),
    /** Asset ID (e.g., @1). */
    coin: string()
  });
})();
function activeSpotAssetCtx(config, params, listener) {
  const payload = parse2(ActiveSpotAssetCtxRequest, { type: "activeAssetCtx", ...params });
  return config.transport.subscribe("activeSpotAssetCtx", payload, (e) => {
    if (e.detail.coin === payload.coin) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/allDexsAssetCtxs.js
var AllDexsAssetCtxsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("allDexsAssetCtxs")
  });
})();
function allDexsAssetCtxs(config, listener) {
  const payload = parse2(AllDexsAssetCtxsRequest, {
    type: "allDexsAssetCtxs"
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/allDexsClearinghouseState.js
var AllDexsClearinghouseStateRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("allDexsClearinghouseState"),
    /** User address. */
    user: Address
  });
})();
function allDexsClearinghouseState(config, params, listener) {
  const payload = parse2(AllDexsClearinghouseStateRequest, {
    type: "allDexsClearinghouseState",
    ...params
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/allMids.js
var AllMidsRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("allMids"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function allMids2(config, paramsOrListener, maybeListener) {
  const params = typeof paramsOrListener === "function" ? {} : paramsOrListener;
  const listener = typeof paramsOrListener === "function" ? paramsOrListener : maybeListener;
  const payload = parse2(AllMidsRequest2, {
    type: "allMids",
    ...params,
    dex: params.dex || void 0
    // Same value as in response
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.dex === payload.dex) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/assetCtxs.js
var AssetCtxsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("assetCtxs"),
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function assetCtxs(config, paramsOrListener, maybeListener) {
  const params = typeof paramsOrListener === "function" ? {} : paramsOrListener;
  const listener = typeof paramsOrListener === "function" ? paramsOrListener : maybeListener;
  const payload = parse2(AssetCtxsRequest, {
    type: "assetCtxs",
    ...params,
    dex: params.dex ?? ""
    // Same value as in response
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.dex === payload.dex) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/bbo.js
var BboRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("bbo"),
    /** Asset symbol (e.g., BTC). */
    coin: string()
  });
})();
function bbo(config, params, listener) {
  const payload = parse2(BboRequest, { type: "bbo", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.coin === payload.coin) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/candle.js
var CandleRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("candle"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** Time interval. */
    interval: picklist(["1m", "3m", "5m", "15m", "30m", "1h", "2h", "4h", "8h", "12h", "1d", "3d", "1w", "1M"])
  });
})();
function candle(config, params, listener) {
  const payload = parse2(CandleRequest, { type: "candle", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.s === payload.coin && e.detail.i === payload.interval) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/clearinghouseState.js
var ClearinghouseStateRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("clearinghouseState"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function clearinghouseState2(config, params, listener) {
  const payload = parse2(ClearinghouseStateRequest2, {
    type: "clearinghouseState",
    ...params,
    dex: params.dex ?? ""
    // Same value as in response
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user && e.detail.dex === payload.dex) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/explorerBlock.js
var ExplorerBlockRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("explorerBlock")
  });
})();
function explorerBlock(config, listener) {
  const payload = parse2(ExplorerBlockRequest, { type: "explorerBlock" });
  return config.transport.subscribe("explorerBlock_", payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/explorerTxs.js
var ExplorerTxsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("explorerTxs")
  });
})();
function explorerTxs(config, listener) {
  const payload = parse2(ExplorerTxsRequest, { type: "explorerTxs" });
  return config.transport.subscribe("explorerTxs_", payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/l2Book.js
var L2BookRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("l2Book"),
    /** Asset symbol (e.g., BTC). */
    coin: string(),
    /** Number of significant figures. */
    nSigFigs: nullish(picklist([2, 3, 4, 5])),
    /** Mantissa for aggregation (if `nSigFigs` is 5). */
    mantissa: nullish(picklist([2, 5]))
  });
})();
function l2Book2(config, params, listener) {
  const payload = parse2(L2BookRequest2, {
    type: "l2Book",
    ...params,
    nSigFigs: params.nSigFigs ?? null,
    mantissa: params.mantissa ?? null
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.coin === payload.coin) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/notification.js
var NotificationRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("notification"),
    /** User address. */
    user: Address
  });
})();
function notification(config, params, listener) {
  const payload = parse2(NotificationRequest, { type: "notification", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/openOrders.js
var OpenOrdersRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("openOrders"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function openOrders2(config, params, listener) {
  const payload = parse2(OpenOrdersRequest2, {
    type: "openOrders",
    ...params,
    dex: params.dex ?? ""
    // Same value as in response
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user && e.detail.dex === payload.dex) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/orderUpdates.js
var OrderUpdatesRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("orderUpdates"),
    /** User address. */
    user: Address
  });
})();
function orderUpdates(config, params, listener) {
  const payload = parse2(OrderUpdatesRequest, { type: "orderUpdates", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/spotAssetCtxs.js
var SpotAssetCtxsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("spotAssetCtxs")
  });
})();
function spotAssetCtxs(config, listener) {
  const payload = parse2(SpotAssetCtxsRequest, { type: "spotAssetCtxs" });
  return config.transport.subscribe(payload.type, payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/spotState.js
var SpotStateRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("spotState"),
    /** User address. */
    user: Address,
    /** Whether to ignore portfolio margin calculations. */
    ignorePortfolioMargin: optional(boolean())
  });
})();
function spotState(config, params, listener) {
  const payload = parse2(SpotStateRequest, { type: "spotState", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/trades.js
var TradesRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("trades"),
    /** Asset symbol (e.g., BTC). */
    coin: string()
  });
})();
function trades(config, params, listener) {
  const payload = parse2(TradesRequest, { type: "trades", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail[0]?.coin === payload.coin) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/twapStates.js
var TwapStatesRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("twapStates"),
    /** User address. */
    user: Address,
    /** DEX name (empty string for main dex). */
    dex: optional(string())
  });
})();
function twapStates(config, params, listener) {
  const payload = parse2(TwapStatesRequest, {
    type: "twapStates",
    ...params,
    dex: params.dex ?? ""
    // Same value as in response
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user && e.detail.dex === payload.dex) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userEvents.js
var UserEventsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userEvents"),
    /** User address. */
    user: Address
  });
})();
function userEvents(config, params, listener) {
  const payload = parse2(UserEventsRequest, { type: "userEvents", ...params });
  return config.transport.subscribe("user", payload, (e) => {
    listener(e.detail);
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userFills.js
var UserFillsRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userFills"),
    /** User address. */
    user: Address,
    /** If true, partial fills are aggregated when a crossing order fills multiple resting orders. */
    aggregateByTime: optional(boolean())
  });
})();
function userFills2(config, params, listener) {
  const payload = parse2(UserFillsRequest2, {
    type: "userFills",
    ...params,
    aggregateByTime: params.aggregateByTime ?? false
  });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userFundings.js
var UserFundingsRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userFundings"),
    /** User address. */
    user: Address
  });
})();
function userFundings(config, params, listener) {
  const payload = parse2(UserFundingsRequest, { type: "userFundings", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userHistoricalOrders.js
var UserHistoricalOrdersRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userHistoricalOrders"),
    /** User address. */
    user: Address
  });
})();
function userHistoricalOrders(config, params, listener) {
  const payload = parse2(UserHistoricalOrdersRequest, { type: "userHistoricalOrders", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userNonFundingLedgerUpdates.js
var UserNonFundingLedgerUpdatesRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userNonFundingLedgerUpdates"),
    /** User address. */
    user: Address
  });
})();
function userNonFundingLedgerUpdates2(config, params, listener) {
  const payload = parse2(UserNonFundingLedgerUpdatesRequest2, { type: "userNonFundingLedgerUpdates", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userTwapHistory.js
var UserTwapHistoryRequest = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userTwapHistory"),
    /** User address. */
    user: Address
  });
})();
function userTwapHistory(config, params, listener) {
  const payload = parse2(UserTwapHistoryRequest, { type: "userTwapHistory", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/userTwapSliceFills.js
var UserTwapSliceFillsRequest2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("userTwapSliceFills"),
    /** User address. */
    user: Address
  });
})();
function userTwapSliceFills2(config, params, listener) {
  const payload = parse2(UserTwapSliceFillsRequest2, { type: "userTwapSliceFills", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/webData2.js
var WebData2Request2 = (() => {
  return object({
    /** Type of subscription. */
    type: literal("webData2"),
    /** User address. */
    user: Address
  });
})();
function webData22(config, params, listener) {
  const payload = parse2(WebData2Request2, { type: "webData2", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/_methods/webData3.js
var WebData3Request = (() => {
  return object({
    /** Type of subscription. */
    type: literal("webData3"),
    /** User address. */
    user: Address
  });
})();
function webData3(config, params, listener) {
  const payload = parse2(WebData3Request, { type: "webData3", ...params });
  return config.transport.subscribe(payload.type, payload, (e) => {
    if (e.detail.userState.user === payload.user) {
      listener(e.detail);
    }
  });
}

// node_modules/@nktkas/hyperliquid/esm/api/subscription/client.js
var SubscriptionClient = class {
  config_;
  /**
   * Creates an instance of the SubscriptionClient.
   *
   * @param config Configuration for Subscription API requests. See {@link SubscriptionConfig}.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   *
   * const subsClient = new hl.SubscriptionClient({ transport });
   * ```
   */
  constructor(config) {
    this.config_ = config;
  }
  /**
   * Subscribe to context updates for a specific perpetual asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.activeAssetCtx({ coin: "ETH" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  activeAssetCtx(params, listener) {
    return activeAssetCtx(this.config_, params, listener);
  }
  /**
   * Subscribe to trading data updates for a specific asset and user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.activeAssetData({ coin: "ETH", user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  activeAssetData(params, listener) {
    return activeAssetData2(this.config_, params, listener);
  }
  /**
   * Subscribe to context updates for a specific spot asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.activeSpotAssetCtx({ coin: "@1" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  activeSpotAssetCtx(params, listener) {
    return activeSpotAssetCtx(this.config_, params, listener);
  }
  /**
   * Subscribe to asset contexts for all DEXs.
   *
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.allDexsAssetCtxs((data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  allDexsAssetCtxs(listener) {
    return allDexsAssetCtxs(this.config_, listener);
  }
  /**
   * Subscribe to clearinghouse states for all DEXs for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.allDexsClearinghouseState({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  allDexsClearinghouseState(params, listener) {
    return allDexsClearinghouseState(this.config_, params, listener);
  }
  allMids(paramsOrListener, maybeListener) {
    const params = typeof paramsOrListener === "function" ? {} : paramsOrListener;
    const listener = typeof paramsOrListener === "function" ? paramsOrListener : maybeListener;
    return allMids2(this.config_, params, listener);
  }
  assetCtxs(paramsOrListener, maybeListener) {
    const params = typeof paramsOrListener === "function" ? {} : paramsOrListener;
    const listener = typeof paramsOrListener === "function" ? paramsOrListener : maybeListener;
    return assetCtxs(this.config_, params, listener);
  }
  /**
   * Subscribe to best bid and offer updates for a specific asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.bbo({ coin: "ETH" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  bbo(params, listener) {
    return bbo(this.config_, params, listener);
  }
  /**
   * Subscribe to candlestick data updates for a specific asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.candle({ coin: "ETH", interval: "1h" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  candle(params, listener) {
    return candle(this.config_, params, listener);
  }
  /**
   * Subscribe to clearinghouse state updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.clearinghouseState({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  clearinghouseState(params, listener) {
    return clearinghouseState2(this.config_, params, listener);
  }
  /**
   * Subscribe to explorer block updates.
   *
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport({ url: "wss://rpc.hyperliquid.xyz/ws" }); // RPC endpoint
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.explorerBlock((data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  explorerBlock(listener) {
    return explorerBlock(this.config_, listener);
  }
  /**
   * Subscribe to explorer transaction updates.
   *
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport({ url: "wss://rpc.hyperliquid.xyz/ws" }); // RPC endpoint
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.explorerTxs((data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  explorerTxs(listener) {
    return explorerTxs(this.config_, listener);
  }
  /**
   * Subscribe to L2 order book updates for a specific asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.l2Book({ coin: "ETH" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  l2Book(params, listener) {
    return l2Book2(this.config_, params, listener);
  }
  /**
   * Subscribe to notification updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.notification({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  notification(params, listener) {
    return notification(this.config_, params, listener);
  }
  /**
   * Subscribe to open orders updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.openOrders({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  openOrders(params, listener) {
    return openOrders2(this.config_, params, listener);
  }
  /**
   * Subscribe to order status updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.orderUpdates({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  orderUpdates(params, listener) {
    return orderUpdates(this.config_, params, listener);
  }
  /**
   * Subscribe to context updates for all spot assets.
   *
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.spotAssetCtxs((data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  spotAssetCtxs(listener) {
    return spotAssetCtxs(this.config_, listener);
  }
  /**
   * Subscribe to spot state updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.spotState({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  spotState(params, listener) {
    return spotState(this.config_, params, listener);
  }
  /**
   * Subscribe to real-time trade updates for a specific asset.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.trades({ coin: "ETH" }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  trades(params, listener) {
    return trades(this.config_, params, listener);
  }
  /**
   * Subscribe to TWAP states updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.twapStates({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  twapStates(params, listener) {
    return twapStates(this.config_, params, listener);
  }
  /**
   * Subscribe to non-order events for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userEvents({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userEvents(params, listener) {
    return userEvents(this.config_, params, listener);
  }
  /**
   * Subscribe to trade fill updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userFills({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userFills(params, listener) {
    return userFills2(this.config_, params, listener);
  }
  /**
   * Subscribe to funding payment updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userFundings({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userFundings(params, listener) {
    return userFundings(this.config_, params, listener);
  }
  /**
   * Subscribe to historical order updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userHistoricalOrders({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userHistoricalOrders(params, listener) {
    return userHistoricalOrders(this.config_, params, listener);
  }
  /**
   * Subscribe to non-funding ledger updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userNonFundingLedgerUpdates({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userNonFundingLedgerUpdates(params, listener) {
    return userNonFundingLedgerUpdates2(this.config_, params, listener);
  }
  /**
   * Subscribe to TWAP order history updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userTwapHistory({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userTwapHistory(params, listener) {
    return userTwapHistory(this.config_, params, listener);
  }
  /**
   * Subscribe to TWAP execution updates for a specific user.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.userTwapSliceFills({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  userTwapSliceFills(params, listener) {
    return userTwapSliceFills2(this.config_, params, listener);
  }
  /**
   * Subscribe to comprehensive user and market data updates.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.webData2({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  webData2(params, listener) {
    return webData22(this.config_, params, listener);
  }
  /**
   * Subscribe to comprehensive user and market data updates.
   *
   * @param params Parameters specific to the API subscription.
   * @param listener A callback function to be called when the event is received.
   * @return A request-promise that resolves with a {@link ISubscription} object to manage the subscription lifecycle.
   *
   * @throws {ValidationError} When the request parameters fail validation (before sending).
   * @throws {TransportError} When the transport layer throws an error.
   *
   * @example
   * ```ts
   * import * as hl from "@nktkas/hyperliquid";
   *
   * const transport = new hl.WebSocketTransport();
   * const client = new hl.SubscriptionClient({ transport });
   *
   * const sub = await client.webData3({ user: "0x..." }, (data) => {
   *   console.log(data);
   * });
   * ```
   *
   * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
   */
  webData3(params, listener) {
    return webData3(this.config_, params, listener);
  }
};
export {
  ApiRequestError,
  ExchangeClient,
  HttpRequestError,
  HttpTransport,
  HyperliquidError,
  InfoClient,
  MAINNET_API_URL,
  MAINNET_API_WS_URL,
  MAINNET_RPC_URL,
  MAINNET_RPC_WS_URL,
  SubscriptionClient,
  TESTNET_API_URL,
  TESTNET_API_WS_URL,
  TESTNET_RPC_URL,
  TESTNET_RPC_WS_URL,
  TransportError,
  ValidationError,
  WebSocketRequestError,
  WebSocketTransport,
  parse2 as parse
};
/*! Bundled license information:

@noble/hashes/utils.js:
  (*! noble-hashes - MIT License (c) 2022 Paul Miller (paulmillr.com) *)
*/
//# sourceMappingURL=@nktkas_hyperliquid.js.map
